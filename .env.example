# 도깨비 배달 — 환경 변수 예시
# 실 운영 서버에서 이 파일을 .env 로 복사한 뒤 값을 채우세요.
# Apache: SetEnv 지시어 또는 php-fpm 풀 설정(env[]) 에 넣거나,
#         별도 .env 로더(vlucas/phpdotenv 등)를 bootstrap.php 앞에 추가하세요.

DB_HOST=13.124.45.107
DB_PORT=3306
DB_NAME=my_web_db
DB_USER=dev_user
DB_PASS=ehRoql1!
DB_CHARSET=utf8mb4

# 개발·라이브 동일 DB 사용 중 (당분간)
# 운영 분리 시 아래 값으로 교체하세요
# DB_HOST=127.0.0.1
# DB_USER=baedal_prod
# DB_PASS=강력한패스워드
# DB_NAME=baedal_prod

# 정산 엑셀 파일 열기 암호 (선택, DB 설정보다 우선 시도)
# SETTLEMENT_EXCEL_PASSWORD=공통비밀번호
# SETTLEMENT_EXCEL_PASSWORD_BAEMIN=배민전용
# SETTLEMENT_PYTHON_BIN=/usr/bin/python3
