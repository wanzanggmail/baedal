# 데모 시연 안내

**PG·펌뱅킹 모두 모의 모드**입니다. 시연 중 실제 결제·송금은 일어나지 않습니다.
데이터 초기화는 테스트가 끝나고 실사용에 들어갈 때 하시면 됩니다.

---

## 1. 계정

### 관리자 — 비밀번호 `Admin1234!` (공통)

| 로그인 ID | 이름 | 권한 | 소속 |
|---|---|---|---|
| `admin` | 최고관리자 | super | 본사 |
| `mshconnect` | 민세훈대표 | manager | 민세훈커넥트(총판) |
| `dkkb` | OXPAY총괄 | manager | 팀OXPAY(대리점) |
| `tax` | 세무대리 | manager | 세무대리(독립·고용산재 예수금) |

### 라이더 — 비밀번호 `0000` · 주소는 `/rider/login`

| 로그인 ID | 이름 | 지갑 |
|---|---|---|
| `R260809375D1` | 이인용9205 | 1,183,515원 |
| `R26080924A8B` | 이효섭0268 | 1,006,585원 |

> 이 둘만 비밀번호 변경 강제를 풀어 뒀습니다. 다른 라이더는 `0000`으로 로그인하면
> 비밀번호 변경 화면이 먼저 뜹니다(정상 동작).

---

## 2. 업로드 파일

`demo/` 폴더에 준비했습니다.

| 파일 | 정산일 |
|---|---|
| `데모_쿠팡이츠_일일정산서_20260829.xlsx` | 2026-08-29 |
| `데모_쿠팡이츠_일일정산서_20260830.xlsx` | 2026-08-30 (예비) |

**내용** — 종합 / 시간제보험 / 차감내역 3개 탭, 라이더 7명, 실지급 합계 608,905원

**7명 중 6명은 자동 매칭되고, `한지훈0000` 한 명만 일부러 미매칭**입니다.
「연결/등록」 흐름을 보여주기 위한 것입니다.

| 라이더 | 오더 | 총액 | 실지급 |
|---|---|---|---|
| 이인용9205 | 34 | 121,200 | 118,897 |
| 최진수8604 | 28 | 99,100 | 97,217 |
| 권성진4418 | 31 | 110,900 | 108,793 |
| 이효섭0268 | 26 | 90,300 | 88,584 |
| 이충회4339 | 22 | 76,500 | 75,046 |
| 노동현0647 | 19 | 65,200 | 63,961 |
| **한지훈0000** | 17 | 57,500 | 56,407 ← **미매칭** |

차감내역 2건(이인용 오배달 12,300원 / 최진수 배달미완료 9,600원)도 들어 있어
정산 반영 시 공제로 잡힙니다.


---

## 3. 시연 순서

### ① 라이더 앱 — "라이더가 보는 화면" (4분)

브라우저에서 **`/rider/login`**

1. 로그인 ID `R260809375D1` / 비밀번호 `0000` → **로그인**
2. **홈** 화면에서 짚을 것
   - 상단 **출금 가능 금액 1,138,746원**
   - 그 아래 **지갑 잔액 1,183,515원** · 보증금 30,000원
   - *"보증금은 남겨 두고 그 위만 출금됩니다"*
3. 왼쪽 위 **☰ 메뉴** → **정산 → 정산 달력**
   - 날짜 칸에 금액이 깔려 있습니다. **아무 날짜나 클릭** → 그날 정산 상세
4. **☰ → 정산 → 원천징수 내역**
   - 지급액 1,249,525원 / 실수령 1,183,515원 / 월별 3개월
   - *"5월 종합소득세 신고 때 이 화면을 보고 씁니다"*
5. **☰ → 내 정보 → 대여금·리스**
   - 미수금 잔액과 차감 이력
6. **☰ → 출금 → 출금 신청**
   - 달력에서 **날짜를 하나 클릭** → 실지급 예정액이 즉시 다시 계산됨
   - **여기서 「출금 신청하기」는 누르지 마세요** — ③에서 쓸 대기 건이 늘어납니다

### ② 관리자 — 정산 업로드 (5분)

새 탭에서 **`/admin/login`** → `admin` / `Admin1234!`

1. 로그인 직후 **헤더의 「출금 대기 3」** 배지를 짚어 주세요
2. 왼쪽 메뉴 **정산 → 일간 정산 업로드**
3. **대리점** 선택 → **팀OXPAY**
4. **플랫폼** → **쿠팡이츠**
5. **정산 귀속일** → **2026-08-29**
6. **파일 선택** → 준비한 쿠팡이츠 일일정산서 xlsx (데모 파일은 2026-09-05 저장소에서 제거됨)
7. **「미리보기」** 클릭
   - 라이더 7명이 뜨고 **6명은 「매칭」, 한지훈0000 만 「미매칭」**
   - *"이름이 시스템에 없으면 여기서 걸러집니다"*
8. 한지훈0000 행의 **「연결/등록」** 클릭
   - 모달에서 **기존 라이더 검색**으로 연결하거나, **신규 등록**
   - 데모에서는 **신규 등록**이 보기 좋습니다 — 이름·휴대전화만 넣으면 됩니다
   - 은행·계좌는 비워도 되고, 넣으면 출금 대행까지 이어집니다
9. **「업로드 확정」** 클릭
10. **「정산 반영」** 클릭
    - 라이더별로 대행수수료·보험·차감내역이 공제되고 **지갑에 적립**됩니다
11. 반영 후 **상세 화면**에서 라이더별 반영 여부와 공제 내역을 보여주세요

### ③ 관리자 — 출금 대행 (4분)

1. 메뉴 **출금 → 출금 신청 목록**
2. 대기 3건이 보입니다 (최진수8604 111만원 / 이충회4339 80만원 / 권성진4418 69만원)
3. **한 건만 체크** → **「선택 출금 확정」**
4. 확인 창에서 **확인**
   - 모의 모드라 **즉시 「처리 완료」**로 바뀝니다
5. 여기서 짚을 것
   - *"라이더 지갑과 대리점 지갑이 **동시에** 줄어듭니다"*
   - **출금 → 대리점 지갑 원장**에서 방금 빠져나간 기록 확인
6. *"실제 연동에서는 「이체 접수중」을 거쳐, 은행 처리 결과가 통보되면 완료로 바뀝니다.
   접수만 되고 결과를 못 받은 건은 따로 조회해 맞춥니다"*

### ④ 관리자 — 수수료·채권 (2분)

1. 메뉴 **정산 → 수수료·차감 통합 조회**
   - 라이더별로 **출금 시점 수수료**와 **정산 반영 시점 공제**가 나뉘어 집계됩니다
   - 라이더 행의 **「내역」** 클릭 → 건별 시계열
2. 메뉴 **수수료·채권 → 대여금·리스**
   - *"리스는 정산 반영할 때 계약기간에 맞춰 자동 차감됩니다"*

### ⑤ 관리자 — 조직·권한 (2분)

1. 메뉴 **시스템 관리 → 조직 관리(총판·대리점)**
   - 본사 > 총판 > 대리점 3계층
2. 오른쪽 위 **로그아웃** → `dkkb` / `Admin1234!` 로 재로그인
3. 메뉴 **라이더 관리 → 라이더 목록**
   - 같은 화면인데 **팀OXPAY 라이더만** 보입니다
   - *"대리점은 자기 라이더만, 총판은 하위 대리점까지만 봅니다"*

### ⑥ 마무리 — 운영 안전장치 (2분)

`admin`으로 다시 로그인

1. 메뉴 **시스템 관리 → 연동 모드**
   - PG(돈이 들어옴) / 펌뱅킹(돈이 나감)이 나란히
   - 지금은 **전부 모의** — 초록 배너
   - *"실연동을 켜면 헤더에 붉은 「실연동」 배지가 떠서, 어느 화면에 있든 보입니다"*
   - *"사고가 나면 「전체 모의로 되돌리기」 한 번이면 멈춥니다"*
2. 메뉴 **시스템 관리 → 감사 로그**
   - 방금 한 업로드·정산 반영·출금 확정이 전부 기록돼 있습니다

---

## 4. 시연 중 피하실 것

| 하지 마세요 | 이유 |
|---|---|
| 연동 모드에서 **실연동 켜기** | PG는 실제 카드가 결제됩니다 |
| 펌뱅킹 **「연결 테스트」** | 허용 IP 미갱신으로 403이 뜹니다 |
| 라이더 앱 **비밀번호 변경** | 데모 계정이 막힙니다 |
| 출금 **「반려」** | 되돌리려면 정산 사이클 점유를 손봐야 합니다 |
| ①에서 **출금 신청하기** | ③에서 쓸 대기 건이 4건이 됩니다 |

**예금주 조회 버튼이 안 보이는 게 정상입니다** — 펌뱅킹 실연동이 꺼져 있어 숨김 상태입니다.

---

## 5. 나올 만한 질문

**"실제 송금은 언제 됩니까?"**
→ 펌뱅킹(바움P&S) 연동은 코드가 완성돼 있고 자격증명도 받았습니다. 바움에 등록된
허용 IP가 이전 서버 주소라, 그것만 갱신되면 바로 켤 수 있습니다.

**"돈이 두 번 나가면요?"**
→ 이체 건마다 고유 ID를 부여하고, 결과 통보가 여러 번 와도 한 번만 반영됩니다.
접수는 됐는데 결과를 못 받은 건은 따로 조회해 맞춥니다.

**"계좌를 잘못 입력하면요?"**
→ 펌뱅킹을 켜면 계좌 입력 화면에 「계좌 확인」 버튼이 생겨 예금주를 조회합니다.
지금은 연동이 꺼져 있어 숨겨 둔 상태입니다.

**"개인정보는 어떻게 보관합니까?"**
→ 계좌번호·결제키는 DB에 AES-256으로 암호화 저장하고 키는 DB 밖에 둡니다.
비밀번호는 bcrypt이며 주민등록번호는 아예 받지 않습니다.

**"정산서 형식이 바뀌면요?"**
→ 헤더 문구로 컬럼을 찾으므로 열 순서가 바뀌어도 읽습니다. 못 읽는 항목이 있으면
업로드 미리보기에서 걸러집니다.

---

## 6. 실사용 전 초기화

테스트가 끝나고 실제 운영에 들어갈 때 정리할 것들입니다.

```sql
-- 데모용 출금 신청 (아직 확정 안 한 것만)
DELETE FROM withdrawal_request_cycles
 WHERE request_id IN (SELECT id FROM withdrawal_requests WHERE note LIKE '%[DEMO]%' AND status='pending');
DELETE FROM withdrawal_requests WHERE note LIKE '%[DEMO]%' AND status='pending';

-- 데모용 공지
DELETE FROM content_notices WHERE public_id LIKE 'DEMO%';

-- 라이더 비밀번호 변경 강제 복원
UPDATE riders SET must_change_password = 1 WHERE id IN (29, 37);
```

> ⚠️ **출금을 확정한 건은 위 쿼리로 지우지 마세요.** 지갑이 이미 차감돼 있어서
> 행만 지우면 잔액이 어긋납니다.

전체를 새로 시작하실 거면 `php migrate.php` 가 깨끗합니다(본사 조직·최고관리자·시스템 코드까지 함께 만들어집니다).
그때 **`.env` 의 `APP_ENC_KEY` 는 그대로 두셔야** 기존 암호화 데이터를 읽을 수 있습니다.
