# PG사(위루트) API 분석 — Mock→Real 전환 준비

> # 🔴 2026-08-25 — 문서가 통째로 바뀌었다
>
> 지금까지 이 파일이 분석한 `https://api.weroutefincorp.com/docs/index.html`(위루트)은
> **우리 가맹점의 API가 아니었다.** 실제는 아래다.
>
> | | |
> |---|---|
> | 문서 | `https://api.routeup.kr/docs/routeup.html` (**루트업**, powered by payvery) |
> | Host | `https://api.routeup.kr` |
> | 결제통지 발신 IP | **`221.168.33.227`** (기존 문서의 `221.168.33.162` 아님) |
>
> **다행히 경로·필드·인증 방식은 두 문서가 동일**해서 코드 변경은 `PgConfig::HOST` 와
> 허용 IP 두 값뿐이었다. 아래 §1~§9 의 분석은 그대로 유효하다(호스트·IP만 위 값으로 읽을 것).
>
> ✅ **이것이 `RV406 가맹점을 찾을 수 없습니다` 의 진짜 원인이었다.** 호스트만 바꾸니 같은 MID 로
> 가맹점 조회가 통과했다(`9999 해외카드는 빌키 발급이 불가합니다` = 카드 단계까지 도달).
> 앞서 "계약 시작일·종료일 공란이 원인" 이라고 적었던 추정은 **틀렸다** — 그쪽은 문제가 아니다.
> 갑에게 보낸 문의도 이 내용으로 정정해야 한다.
>
> 자세한 확정 사항은 **§11**.

> **출처:** `https://api.weroutefincorp.com` — 문서가 **2개**다.
> - 거래(결제) API: `/docs/index.html` (2025-12-10 03:06 갱신)
> - **대사(정산) API: `/docs/reconciliation`** (2025-12-10 19:42 갱신)
>
> **성격:** 실 스펙 도착 → 코드 대조 분석. **코드 미변경.**
> **작성:** 2026-08-14 · **재작성:** 2026-08-15 (2차 정밀 검토 — 대사 API·인증체계·자금흐름 추가)

---

## 0. 요약

### ✅ 2026-08-15 갑 답변으로 해소된 것 (설계 전제 확정)

| 쟁점 | 갑 답변 | 결론 |
|---|---|---|
| PG 수수료 3.3% 부담 | *"우리가 플랫폼 수수료를 받는 이유가 이것. PG 수수료는 알아서 할 테니 걱정 말 것.* ***PG 결제는 플랫폼수수료를 더한 만큼 결제하고, 대리점에는 라이더 총금액만큼 지급***\*"\* | **현재 코드가 이미 맞다.** `total_charged = net + service_fee`로 청구하고 지갑엔 `net`을 올리는 지금 로직 그대로. PG 수수료는 본사가 플랫폼 수수료 수입에서 흡수 → 우리 시스템은 관여하지 않는다 |
| 정산 입금 지연 | *"입금은 즉시. 그리고 계좌에 미리 돈을 넣어둘 것"* | **정산대사로 `deposit_status`를 추적할 필요 없음.** 잔고 부족 리스크는 본사가 선충전으로 흡수 |
| MID·계좌가 대리점별인가 | *"결제하는 상점은 하나. PG로 돈 받는 계좌 = 라이더에게 출금하는 계좌 = 하나."* | **자격증명·계좌 전부 본사 단일 세트.** → §3 |
| 코드체계 | *"알아서 맞춰줘"* | ✅ **완료** — `system_codes.card_issuer` 42종 추가 → §6 |

> 즉 1차·2차 검토에서 최대 리스크로 올렸던 **자금 모델 충돌은 실재하지 않는다.** 우리가 몰랐던 건 "PG 수수료를 플랫폼 수수료로 이미 커버하도록 설계돼 있었다"는 사업 구조였다.

### 남은 것

1. **🟡 자격증명 6개 저장소** — 본사 단일이라 구조는 단순해졌지만 여전히 저장할 곳이 없다 → §2
2. **🟡 `pg_payments.ord_num` 신설** — 위루트는 결제 *전에* 주문번호를 요구하는데 우리는 결제 *후에* id가 생긴다 → §4-3
3. **🟡 카드 등록 폼·웹훅 엔드포인트 부재** → §4-1, §5
4. **🔴 PG사 문의 4건** — sign_key 발급처 등 → §9

---

## 1. 위루트가 제공하는 API 전체 지도

### 거래 API (`/docs/index.html`)

| API | 엔드포인트 | 우리 용도 |
|---|---|---|
| 결제통지(Webhook) | 우리가 URL 제공 | ✅ **필수** |
| 거래조회(ACK) | `GET /api/v2/transactions/{ord_num}` | 🟡 단건 재확인용 |
| 수기결제 | `POST /api/v2/pay/hand` | ❌ 매번 카드정보 필요 |
| 인증결제 | `POST /v2/pay/auth` (폼) | ❌ 사용자 결제창 필요 |
| 간편결제 | `POST /v2/pay/simple` (폼) | ⛔ **"현재 지원하지 않습니다"** 명시 |
| **빌키 생성** | `POST /api/v2/pay/bill-key` | ✅ **우리 "카드 등록"** |
| **빌키 결제** | `POST /api/v2/pay/bill-key/hand` | ✅ **우리 `charge()`** |
| **빌키 삭제** | `DELETE /api/v2/pay/bill-key` | ✅ 카드 삭제 |
| 결제취소 | `POST /api/v2/pay/cancel` | 🟡 §7 |
| 망상취소 | `POST /api/v2/pay/net-cancel` | 🟡 응답 유실 시 복구용 |

### 대사 API (`/docs/reconciliation`) — 1차 분석에서 통째로 누락

| API | 엔드포인트 | 우리 용도 |
|---|---|---|
| 로그인 | `POST /api/v1/sign-in` | ✅ 대사 API 토큰 발급 |
| 일별요약 거래대사 | `GET /api/v1/reconciliations/transactions/day-summary` | 🟡 일 단위 검증 |
| 실시간 거래대사 | `GET /api/v1/reconciliations/transactions/realtime` | ✅ 거래 목록 대사 |
| 일별요약 정산대사 | `GET /api/v1/reconciliations/settle-histories/day-summary` | ✅ **입금액 확인** |
| 실시간 정산대사 | `GET /api/v1/reconciliations/settle-histories/realtime` | ✅ **입금 예정/완료** |

> 💡 **경로 함정:** 폼 전송형(인증·간편)만 `/api` 접두어가 **없다**(`/v2/pay/auth`). JSON API는 전부 `/api/v2/...`. 대사는 `/api/v1/...`(버전도 다름). 상수로 관리하지 않으면 반드시 틀린다.

---

## 2. 인증 체계 — 두 API가 완전히 다르다

### 거래(결제) API
```
Authorization: {pay_key}        ← Bearer 아님! 키 원문 그대로
Accept: application/json
Content-Type: application/json
```

### 대사 API
```
External-Api:  Bearer {API_KEY}        ← 별도 API 키
Authorization: Bearer {ACCESS_TOKEN}   ← 로그인으로 받은 토큰
```
- `POST /api/v1/sign-in` 에 `user_name`/`user_pw` → `access_token` 발급
- **토큰 유효기간 30시간** → 만료 시 재로그인 필요(캐시·갱신 로직 필요)
- 토큰은 계정 권한(가맹점/영업라인)에 따라 접근 API가 자동 제한됨

### 그래서 필요한 자격증명 = **6개**

| # | 값 | 쓰이는 곳 | 현재 저장소 |
|---|---|---|---|
| 1 | `mid` (가맹점 ID) | 거래·대사 전부 | ❌ 없음 |
| 2 | `tid` (단말기 ID) | 거래 전부(없으면 빈값) | ❌ 없음 |
| 3 | `pay_key` | 거래 API 인증 | ❌ 없음 |
| 4 | `sign_key` | 웹훅 서명 검증 | ❌ 없음 (**발급처 문서에 없음**) |
| 5 | `API_KEY` | 대사 API `External-Api` | ❌ 없음 |
| 6 | `user_name`/`user_pw` | 대사 API 로그인 | ❌ 없음 |

→ **`.env`가 아니라 DB에 둬야 할 가능성이 높다.** 대리점별로 MID가 다르면 org 단위 저장이 필요하고, 토큰 캐시(30시간)도 어딘가 저장해야 한다. `user_pw`는 평문 보관이 불가피하므로(로그인에 원문 필요) 암호화 저장 + 접근 제한 검토 필요.

---

## 3. 자금 모델 — ✅ 해소 (2026-08-15)

> **아래 §3-1~§3-4는 해소 전 분석이다. 결론부터:**
>
> **갑 확답으로 우리 코드가 이미 맞다는 게 확인됐다.** PG 수수료는 본사가 플랫폼 수수료 수입에서 흡수하고, 입금은 즉시 + 본사가 계좌에 미리 충전해 둔다. 우리 시스템은 **카드에 `net + 플랫폼수수료`를 청구하고 지갑에 `net`을 올리는 지금 로직 그대로** 가면 된다. `deposit_status` 추적·정산 지연 대응 **불필요**.

### 3-0. 확정된 구조 — 「단일 가맹점 · 단일 계좌 · 지갑은 내부 장부」

갑 답변 3번이 설계 전제를 명확히 했다:

> *"결제하는 상점은 하나야. 우리가 실제로 PG를 통해 돈을 받는 계좌, 라이더에게 출금하는 계좌는 하나야. 그걸 내부에서 지갑이라고 칭하는 대리점·총판·본사의 금액에서 나눠서 보여주는 것뿐이야."*

```
[대리점 카드] ──결제(net+플랫폼수수료)──▶ [PG] ──입금(즉시)──▶ ┌─────────────────┐
                                                              │ 본사 단일 실계좌 │
[라이더 계좌] ◀────────펌뱅킹 이체────────────────────────────│ (+ 본사 선충전)  │
                                                              └─────────────────┘
                                                                       ▲
   agency_wallets = 이 하나의 계좌 잔액을 조직별로 나눠 보여주는 **내부 장부**
   (대리점 지갑 / 총판 지갑 / 본사 지갑은 물리적 계좌가 아니다)
```

**코드에 미치는 영향:**

| 항목 | 판정 |
|---|---|
| `agency_wallets`를 장부로 쓰는 현재 구조 | ✅ 그대로 유효 — 원래 의도가 맞았다 |
| MID·TID·pay_key·sign_key·API_KEY·로그인 | ✅ **본사 단일 세트**(org별 저장 불필요) |
| `agency_bank_accounts`(대리점별 출금계좌) | ⚠️ **실제로는 전부 같은 계좌를 가리키게 된다.** 대리점마다 다른 계좌를 넣을 수 있는 현재 스키마는 실 연동에서 오설정 위험 → §8-11 |
| `agency_cards`(대리점별 카드) | ✅ 유효 — 수취(가맹점)가 하나일 뿐 **결제하는 카드는 대리점별**이 맞다 |
| 정산대사 API | 🟡 필수 아님(입금 즉시) — 회계 대조용으로만 선택적 |

---

## 3-1~3-4. (해소 전 분석 — 이력 보존)

### 🔴 자금 모델 충돌 — 2차 검토 시점의 우려

### 현재 우리 코드가 가정하는 것

`inc/PgPayment.php`:
```php
AgencyWallet::credit($agencyId, $netAmount, 'pg_fund', ...);  // 결제 성공 즉시 전액 충전
```
→ **카드 승인 = 그 순간 대리점 지갑에 현금이 있다**고 본다. 라이더 출금(펌뱅킹 실이체)도 이 잔액을 근거로 나간다.

### 위루트 정산대사가 말하는 현실

`settle-histories/day-summary` 응답 예시:
```json
{ "amount": 1000, "settle_amount": 960, "deposit_amount": 960 }
```
`transactions/day-summary` 응답 예시:
```json
{ "amount": 1000, "count": 1, "profit": 960 }
```
`settle-histories/realtime` 응답에는 이런 필드가 있다:
```
total_amount 1000 → settle_amount 967 / deposit_amount 967
settle_dt "2025-12-15" / deposit_dt "2025-12-15" / deposit_status 1
acct_bank_code "003" / acct_num "142..." / acct_name "홍길동"
settle_fee, deduct_amount, cancel_deposit_amount,
settle_withdraw_fee_amount, payment_withdraw_fee_amount, under_sales_amount
```

**두 가지가 드러난다:**

**(1) PG가 자기 수수료를 뗀다.** 1,000원 승인 → 실입금 960~967원. **약 3.3~4.0%**가 PG 몫이다.

**(2) 입금은 나중이다.** `settle_dt`·`deposit_dt`·`deposit_status`가 따로 있다는 건 승인과 입금이 별개 시점이라는 뜻이다.

### 무엇이 깨지는가

라이더에게 100,000원을 주려고 카드로 100,000원을 긁으면, 대리점 계좌에 실제로 들어오는 건 **약 96,700원**이고 그것도 **며칠 뒤**다. 그런데 우리 시스템은 지갑에 100,000원을 즉시 올리고 라이더에게 100,000원을 이체하려 한다.

- **금액 부족**: 매 결제마다 3.3% 구멍이 누적된다
- **시점 불일치**: 입금 전에 이체하면 실제 은행 잔고가 모자라 **펌뱅킹 이체가 실패**한다

### 플랫폼 수수료가 이걸 메우는 구조였나?

의심스러운 대목이 있다. 우리 `org_fee_config` 기본값이 **본사·총판·대리점 각 1% = 합 3%**로 설계돼 있는데, PG 수수료 3.3%와 거의 같다. 카드 청구액이 `net + 플랫폼수수료`이므로 형태상으로는 상쇄가 된다.

**그런데 2026-08-12 갑 확정으로 "플랫폼 수수료는 지갑에 적립하지 않고 본사가 계산서로 따로 정산"하기로 했다.** 즉 플랫폼 수수료는 **본사·총판·대리점의 수익**이지 PG 원가 보전분이 아니다. 그렇다면 PG 수수료 3.3%는 **아무도 부담을 지정하지 않은 상태**다.

> ⚠️ 참고: 현재 실 DB의 `org_fee_config`는 전 조직 **0%**로 세팅돼 있어(2026-08-15 확인) 실제 결제 기록도 `service_fee = 0`이다. 즉 지금은 카드 청구액 = net 그대로라, PG 수수료를 흡수할 여지가 아예 없다.

**→ 이건 코드 문제가 아니라 계약·정책 문제다. §9-1에서 반드시 확정해야 한다.**

---

## 4. 빌링 API ↔ 현재 코드 갭

### 4-1. 빌키 생성 vs `AgencyCard::create()`

| 위루트 요구 | 현재 | |
|---|---|---|
| `mid`, `tid`, `ord_num` | 없음 | ❌ |
| `buyer_name`, `buyer_phone` | 없음 | ❌ |
| **`card_num`, `yymm`, `auth_num`, `card_pw`** | **없음** | ❌ **핵심** |
| `Authorization: {pay_key}` | 없음 | ❌ |

현재 `inc/AgencyCard.php`는 `billing_key`가 비면 `MOCK-BK-...`를 **스스로 만든다.** 실 연동에서는 반대다 — **카드정보를 위루트에 보내고 응답의 `bill_key`를 저장**한다.

응답 필드 `bill_code`·`issuer`·`issuer_code`·`trx_id`를 담을 컬럼이 `agency_cards`에 없다.

> 🔒 **카드번호·비밀번호는 우리 DB에 저장하면 안 된다.** 위루트로 전달만 하고 `bill_key`만 보관해야 PCI-DSS 범위를 피한다. 화면에서 받아 즉시 전송·폐기하는 구조가 필요하다.

### 4-2. 빌키 결제 vs `PgGateway::charge()`

```php
// 현재 — 너무 얇다
public function charge(string $billingKey, int $amount, array $meta = []): PgChargeResult;
```

위루트 요구: `mid`, `tid`, `ord_num`, `buyer_name`, `buyer_phone`, `item_name`, `bill_key`, `installment`, `amount`

→ **인터페이스 확장 불가피.** `meta`에 밀어넣으면 계약이 무의미해지므로 전용 DTO(예: `PgChargeRequest`)를 만드는 편이 낫다. `MockPgGateway`도 함께 수정해야 한다.

### 4-3. `ord_num` 채번 — 숨은 난관

위루트는 결제 **요청 시** `ord_num`(주문번호, 최대 30byte)을 요구한다. 그런데 우리는 결제가 **성공해야** `pg_payments.id`가 생긴다. 순서가 반대다.

→ 결제 전에 유일한 주문번호를 만들어야 하고(예: `PG{agency_id}-{Ymd His}-{rand}`), 그 값을 `pg_payments`에 컬럼으로 저장해야 웹훅·대사·취소 때 우리 레코드를 찾을 수 있다. **`pg_payments.ord_num` 컬럼 신설이 필수다.**

### 4-4. 빌키 삭제

현재 `AgencyCard::delete()`는 `DELETE FROM agency_cards`만 한다. 실 연동 시 위루트에 삭제 요청을 **먼저** 보내고 성공해야 우리 DB에서 지워야 한다 — 안 그러면 위루트엔 살아있는 빌키가 우리 쪽에서만 사라진다(유령 빌키).

---

## 5. 결제통지(Webhook) — ✅ 구현 완료 (2026-08-23)

**엔드포인트 `POST /pg/noti.php`** · 처리 로직 `inc/PgWebhook.php` · 관리 화면 `system/pg-integration`.

⚠️ **이 경로는 돈을 움직이지 않는다.** 우리 결제는 요청→응답 동기 흐름이라 승인 응답에서 이미 지갑이
반영된다. 웹훅에서 또 충전하면 같은 결제가 두 번 반영되므로, 여기서는 **받은 통지를 기록하고
`pg_payments`에 붙이는 대사(확인)만** 한다(`pg_webhook_events.match_state`).

검증한 것: IP 차단(403) · 서명 정상/위조(200/401) · 재전송 멱등(1건만 저장) · 금액 불일치 표시 ·
미대조 표시 · 빌링 아닌 module_type 무시 · 깨진 JSON/`trx_id` 누락(400) · 성공 응답이 `{}`인지.

### (원문 요건 — 참고)

우리 자금 흐름은 전부 동기(요청→응답)다. 위루트는 그와 **별개로** 결제 발생 시 우리 서버를 호출한다.

**구현 요건:**
1. **공개 엔드포인트** — 예: `POST /webhook/weroute` (세션 인증 없음)
2. **방화벽 허용** — `221.168.33.162`
3. **서명 검증** — `signature = sha256("sign_key={값}&timestamp={값}&mid={값}")`
   - ⚠️ `sign_key` **발급처가 문서에 없다** → §9-2
4. **응답 규격 엄수** — 성공 `200` + body `{}` / 실패 `200 외 코드` + `{"message":"사유"}`
   - 규격이 다르면 **1분 간격 재전송**(횟수는 가맹점 설정)
   - → **멱등 처리 필수.** 같은 `trx_id`가 여러 번 와도 지갑이 중복 충전되면 안 된다
5. **`module_type` = `4`(빌링)** 로 우리 건을 식별
6. **`temp` 필드 활용** — 승인 요청 시 넣은 값이 웹훅으로 되돌아온다(base64 JSON 권장, 예제가 `btoa(JSON.stringify(...))`). 여기에 `pg_payments.id`나 `upload_id`를 실어두면 대사가 쉬워진다
   - ⚠️ 길이 제한이 제각각: 수기결제 1000 / 인증·간편 180 / 웹훅 반환 500

---

## 6. 코드 체계 2종 — ✅ 반영 완료 (2026-08-15)

갑 지시("코드체계는 알아서 맞춰줘")에 따라 아래를 적용했다.

- **`system_codes`에 `card_issuer` 카테고리 42종 추가**(위루트 「발급사/매입사 정의」 전문). 「코드/마스터」 화면에 "카드 발급사/매입사 코드(PG)"로 노출된다.
- **조회 헬퍼를 분리**했다 — `SystemCode::bankLabel()` / `SystemCode::cardIssuerLabel()`. 같은 함수로 두 체계를 조회하면 섞이므로 아예 나눠뒀다.
- `cardIssuerLabel()`은 **2자리/3자리 혼용을 흡수**한다(PG는 `04`, 우리 컬럼이 3자리일 수 있음).
- **은행코드는 손댈 필요 없었다** — 우리 `bank` 13종이 위루트 은행코드표와 전부 일치(대조 완료).

검증:
```
발급사 04 = 삼성    |  은행 004 = 국민은행     ← 같은 숫자, 다른 뜻
발급사 16 = 하나    |  은행 081 = 하나은행
cardIssuerLabel("6") = "06" = 신한 (혼용 흡수)
```

### 원본 정의 (참고)

위루트는 **서로 다른 코드표 2개**를 쓴다.

| 구분 | 필드 | 예시 |
|---|---|---|
| **발급사/매입사** (카드사) | `issuer_code`, `acquirer_code` | `01`비씨 `02`국민 `06`신한 `07`현대 `16`하나 `46`카카오 `48`토스 |
| **은행** (정산 입금계좌) | `acct_bank_code` | `003`기업 `004`국민 `088`신한 `090`카카오뱅크 `092`토스뱅크 |

같은 `04`라도 발급사면 삼성카드, 은행이면 국민은행이다. 섞으면 조용히 틀린다.

✅ **좋은 소식:** 우리 `system_codes(category='bank')` 13종을 위루트 은행코드표와 대조한 결과 **전부 일치**한다(2026-08-15 확인). 라이더 계좌·대리점 정산계좌 코드를 그대로 쓸 수 있다.

---

## 7. 취소 API — 우리 정책과의 관계

`LOGIC.md` §8-A 이력에 "정산 반영 후 정정 = 본사 전용 수동조정 화면"으로 확정돼 있다. **PG 자동환불을 안 만들기로 한 결정**이다.

다만 위루트에는 두 종류가 있고, 그중 하나는 성격이 다르다:

- **결제취소**(`/pay/cancel`, `trx_id` 기준) — 환불. 현 정책상 미사용
- **망상취소**(`/pay/net-cancel`, `ord_num` 기준) — **통신 오류로 응답을 못 받았을 때 승인을 무효화**하는 안전장치

**망상취소는 정책과 무관하게 필요할 수 있다.** 카드는 승인됐는데 우리가 응답을 못 받아 실패로 처리하면, 고객 카드에는 청구가 남고 우리 지갑엔 반영이 안 된 채 어긋난다. 타임아웃 시 망상취소를 부르는 게 표준 방어다.

⚠️ 그리고 수동조정은 **우리 DB 숫자만** 고친다 — 카드사에 청구된 실제 돈은 돌아오지 않는다.

---

## 8. 착수 체크리스트 (순서)

*(분석만 — 착수 아님)*

| # | 작업 | 상태 / 선행조건 |
|---|---|---|
| 0 | §9 확인사항 | 🟡 **3건 해소, 4건 남음**(전부 PG사 문의) |
| — | 코드체계(`card_issuer`) 정비 | ✅ **완료** (§6) |
| 1 | 자격증명 저장소 + 토큰 30h 캐시 | ✅ **완료** — `pg_config` 단일행 + `inc/PgConfig.php` |
| 2 | `pg_payments.ord_num` + 채번 규칙 | ✅ **완료** — `PgPayment::makeOrderNo()` |
| 3 | 카드 등록 폼 + `agency_cards.bill_code`/`issuer_code` | ✅ **완료** |
| 4 | `PgGateway` 인터페이스 확장(DTO) + Mock 동반 수정 | ✅ **완료** |
| 8 | 카드 삭제를 PG 호출 후로 변경 | ✅ **완료** — `AgencyCard::delete()`가 해지 성공해야 DB 삭제 |
| 10 | `PgGatewayFactory::make()` 설정 기반 분기 | ✅ **골격 완료** — `driver`·`realAvailable()`·`isMock()` |
| **5** | **`RealPgGateway` 구현(cURL)** | ⛔ **자격증명 실물 + §9-5·9-6** |
| **6** | **웹훅 엔드포인트 + 서명검증 + 멱등 처리** | ⛔ **§9-2(sign_key)·§9-4(Noti URL)** |
| **7** | 망상취소(타임아웃 방어) | 5 이후 |
| ~~9~~ | ~~정산대사 배치(`deposit_status` 추적)~~ | ❌ **불필요** — 입금 즉시 + 본사 선충전 확정 |
| 11 | `agency_bank_accounts` 단일계좌 전제 반영 검토 | 🟡 미착수 — 대리점마다 다른 계좌를 넣을 수 있는 현 스키마가 오설정 위험(§3-0) |

### 2026-08-15 구현 상세 (1~4·8·10)

**자격증명 (`pg_config` · `inc/PgConfig.php`)**
단일 가맹점 확정에 따라 **전역 1행(id=1)**. `driver`(mock|weroute)·mid·tid·pay_key·sign_key·api_key·login_id/pw + `access_token`/`token_expires_at`(30h 캐시, 만료 5분 전부터 재발급 유도). 엔드포인트 경로도 상수화(`EP_*`) — 폼 전송형만 `/api`가 없고 대사는 `/api/v1`이라 하드코딩하면 반드시 틀린다.
- 비밀값은 `publicView()`로 **마스킹만 노출**하고, 저장 시 **빈 값이면 기존 값 유지**(마스킹된 화면값으로 덮어쓰는 사고 방지).
- 🔒 현재 **평문 저장**. 대사 로그인에 비밀번호 원문이 필요해 해시가 불가하고, 키 관리 체계 없는 양방향 암호화는 실효가 없어 보류했다. **운영 전환 시 재검토 대상.**

**주문번호 (`PgPayment::makeOrderNo()`)**
`PG{agency}-{YmdHis}-{rand4}` (24byte, 위루트 30byte 제한 내). 중복 시 최대 5회 재추첨.
⚠️ **카드를 바꿔 재시도할 때마다 새로 채번**한다 — 같은 `ord_num`으로 두 번 승인 요청이 나가면 PG가 중복 주문으로 막거나 대사가 꼬인다.

**인터페이스 (`inc/PgGateway.php`)**
`charge(PgChargeRequest)` / `issueBillingKey(PgBillingKeyRequest)` / `deleteBillingKey()` / `label()`.
`PgChargeResult`에 `apprNum`·`issuer`·`issuerCode`·`cardNum` 추가(대사·리포트용).

**카드 등록**
카드번호·유효기간·생년월일/사업자번호·카드비번을 받아 **PG로 전달만 하고 저장하지 않는다.** DB에는 `bill_key`·`bill_code`·`issuer_code`·끝4자리·카드사명만 남는다. 성공/실패와 무관하게 화면 입력칸도 즉시 비운다.

**검증**: 모의 발급→저장 확인(카드번호 미저장), 필수값·유효기간 형식·PG 실패 거부 3종, 삭제 시 해지 선행, 결제 시 `ord_num` 기록(24byte)·지갑 충전·원장 정합성.

---

## 9. 확인해야 할 것

### ✅ 9-1. PG 수수료·정산지연 — **해소 (2026-08-15 갑)**
> *"우리가 플랫폼 수수료를 받는 이유가 이런 이유야. PG수수료를 알아서 할 테니 걱정 말고, PG결제는 플랫폼수수료를 더한 만큼 결제하면 되고 대리점에는 라이더 총금액만큼 지급하면 돼."*
> *"입금은 즉시 입금을 할 거야. 그리고 미리 계좌에는 돈을 넣어둘 거니까 그것도 걱정하지 마."*

→ **현재 코드가 이미 맞다.** 변경 불필요. 정산대사 추적도 불필요.

### ✅ 9-3. MID·계좌 단위 — **해소 (2026-08-15 갑)**
> *"결제하는 상점은 하나야. 우리가 실제로 PG를 통해 돈을 받는 계좌, 라이더에게 출금하는 계좌는 하나야. 그걸 내부에서 지갑이라고 칭하는 대리점·총판·본사의 금액에서 나눠서 보여주는 것뿐이야."*

→ **본사 단일 가맹점 · 단일 실계좌.** 자격증명·계좌는 org별이 아니라 시스템 전역 1세트(§3-0).

### ✅ 9-7. 코드체계 — **해소·반영 완료 (2026-08-15)**
> *"코드체계는 알아서 맞춰줘"* → §6 참고. `card_issuer` 42종 추가 + 조회 헬퍼 분리.

---

### ❌ 남은 확인 4건 — 전부 **PG사(위루트) 문의** 사항

#### ✅ 9-2. sign_key 발급처 — **해소 (2026-08-23 갑 전달)**
서명 KEY를 받아 `pg_config.sign_key`에 등록했다. `PgWebhook::verifySignature()`가 문서식 그대로
`sha256("sign_key=..&timestamp=..&mid=..")`로 검증하며, 실 키로 정상/위조 서명 테스트를 통과했다.
키가 비어 있으면 검증을 건너뛰고 `verified=0`으로 남기므로, 키를 넣는 순간 검증이 켜진다.

#### 🔧 9-4. Noti URL 등록 방법 — **우리 쪽 준비 완료 (2026-08-23)**
엔드포인트를 만들었다: `POST /pg/noti.php` (`pg/noti.php` → `inc/PgWebhook.php`).
「시스템 관리 › PG 연동·결제통지」 화면이 배포 도메인 기준 Noti URL을 만들어 보여주므로 복사해 등록하면 된다.
가맹점 관리자 계정(아이디 `오엑스테스트`)도 받았다 — **등록 작업 자체는 사람이 해야 한다**(로그인 필요).
⏳ 재전송 횟수 설정값은 아직 미확인.

#### 9-5. `auth_num` 기준
"생년월일 6자리 **또는** 사업자번호". 대리점 카드가 법인카드면 사업자번호일 텐데 확정 필요.
→ 데이터는 이미 있다(2026-08-12에 `organizations.biz_reg_no` 추가함).

#### 9-6. 테스트(샌드박스) 환경
문서에 **"Try it out ⚡"** 인터랙티브 테스터가 있어 시험 호출은 가능해 보이나, **별도 테스트 MID/카드가 있는지** 확인 필요. 실 카드로만 테스트해야 하면 개발 검증이 곤란하다.

> 💡 ~~9-2·9-4는 **웹훅 구현의 선행조건**~~ → **2026-08-23 해소, 웹훅 구현 완료.**
> `RealPgGateway`도 **구현 완료**(§10). 다만 **빌키 발급에서 가맹점 조회가 안 돼**(RV406) 카드 등록이 막혀 있다 —
> 9-5·9-6과 함께 PG사 문의가 필요하다.

---

## 부록. 문서 자체의 오류 (그대로 따라 쓰면 안 되는 것)

- **axios 예제가 틀렸다.** `axios.get(url, {params}, headers)` — axios는 3번째 인자를 안 받는다. 헤더가 **누락된 채 호출**된다. `axios.post(url, params, headers)`도 config가 아니라 헤더 객체를 그대로 넘겨 잘못이다.
- **Python 예제도 이상하다.** `requests.get(url, json=params, ...)` — GET에 body를 싣는다. 실제로는 `params=`여야 한다.
- **PHP 예제는 Laravel `Http` 파사드 / 대사는 Guzzle** — 우리는 순수 PHP라 **cURL로 재작성**해야 한다.
- 거래조회 응답의 `cxl_seq` 설명이 "응답 포멧: 2023-04-30 11:11:11"로 되어 있다(회차 숫자인데 날짜 설명이 붙음 — 복붙 오류로 보임).

→ **예제 코드를 신뢰하지 말고 필드 표를 기준으로 구현할 것.**

---

## 10. 실 드라이버 구현 — 2026-08-23

`inc/RealPgGateway.php` 구현. `PgGatewayFactory`가 **driver=weroute 이고 mid·pay_key 가 있을 때만** 실
드라이버를 태우고, 하나라도 비면 Mock 으로 폴백한다(설정을 덜 넣은 채 실 결제가 나가는 것 방지).

### ✅ 실 호출로 확인한 것

- **응답 봉투** — 모든 응답이 `{"result_cd":"...","result_msg":"...","temp":[]}`.
  실패는 HTTP 4xx(RV422=검증오류, RV406/PV406=대상없음, RV470=빌키없음)와 함께 온다.
  → **성공 판정은 HTTP 2xx**를 기준으로 하고 `result_cd`는 사유 표기에 쓴다.
- **인증** — `Authorization: {pay_key}` **원문**(Bearer 아님)이 맞다. 401이 아니라 업무 오류가 돌아왔다.
- **빌키 삭제·빌키 결제** — 요청 규격이 통과한다(없는 빌키로 RV470 정상 수신, 승인 안 남).
- ⚠️ **거래조회는 `cxl_seq` 필수** — 없으면 RV422. 문서 표에 안 드러나 있다.

### 🔴 막힌 것 — PG 쪽 설정

**빌키 발급(`POST /api/v2/pay/bill-key`)에서 `RV406 가맹점을 찾을 수 없습니다`.**
MID `M842420` 으로 가맹점이 조회되지 않는다. 빈 mid 는 RV422(필수값)로 갈리므로 값 전달 자체는 정상이다.

> ⚠️ **결제 엔드포인트는 mid 를 검사하지 않는다** — 가짜 mid `M000000` 을 넣어도 RV470(빌키없음)으로
> 같은 응답이 온다. 즉 "결제는 되는데 발급만 안 된다"가 아니라, **mid 를 실제로 검증하는 유일한
> 경로가 빌키 발급**이고 거기서 막힌 것이다. 결제가 통과한 것처럼 보인 건 착시다.

#### 원인 좁히기 (2026-08-23, 가맹점 관리자 계약정보 확인 후)

갑이 보내준 가맹점 관리자 화면 기준:

| 항목 | 값 |
|---|---|
| 결제모듈 타입 | **빌키결제** ← 빌링은 이미 계약돼 있다 |
| MID / TID | `M842420` / `4026080094` — 우리 설정과 일치 |
| 결제 KEY / 서명 KEY | 우리 설정과 일치 |
| **계약 시작일 / 종료일** | **둘 다 비어 있음** 🔴 |
| 결제한도(단건·일·월·연) | **전부 `0만원`** 🔴 |

→ **"빌링 미활성화"라는 앞선 추정은 틀렸다.** 모듈은 빌키결제로 잡혀 있다.
유력한 원인은 **계약 시작일·종료일 공란**이다. 가맹점 조회가 "오늘이 계약기간 안인가"로 거르면
시작일이 없을 때 어떤 날짜로도 유효 계약을 못 찾아 RV406 이 난다. 결제한도 0 도 함께 확인이 필요하다.

#### 일반결제(수기결제)로 우회 테스트 — 불가

`POST /api/v2/pay/hand` 도 **똑같이 RV406** 이다. 이 MID 는 **빌키결제 전용 모듈**이라 수기결제
모듈 자체가 없다. 즉 일반결제로 갈아타 검증하는 우회로는 없고, 계약 설정을 고쳐야만 진행된다.
(가짜 mid 로 보내도 같은 RV406 이라 이 경로로는 mid 유효성도 못 가린다.)

→ **PG사/영업 문의:** 계약 시작일·종료일을 채워 달라. 결제한도 0 이 결제를 막는 값인지도 확인.
그 전까지는 카드 등록이 불가능하므로 `driver=mock` 을 유지한다.

### ⏳ 아직 확정 못 한 것

1. **성공 응답 필드명** — 실패 응답만 받아봤다. `bill_key`·`trx_id` 등은 후보 키를 훑는
   `pick()` 으로 처리해 뒀고, 실 성공 응답을 한 번 받으면 그 키만 남기고 정리할 것.
2. **카드 필드 암호화 여부** — AES 키/IV 를 받았고 `PgCrypto`(AES-256-CBC, 키 32·IV 16 검증)를
   준비했으나 어느 필드에 쓰는지 미확인. 지금은 평문(HTTPS). 요구되면 `buildCardFields()` 한 곳만 고친다.
3. **재시도 대상 코드표** — 한도초과 등 "다음 카드로 폴백" 판정을 `result_msg` 문구로 하고 있다.
   코드표를 받으면 **반드시 코드 기준으로 바꿀 것** — 문구는 PG가 바꾸면 폴백이 조용히 멈춘다.

### 자가진단

「PG 연동·결제통지」의 **「연결 테스트」** 버튼(`admin/api/pg_selftest.php`)이 위 확인을 자동으로 돈다.
**돈이 움직이지 않는 호출만** 쓴다(없는 빌키·더미 카드번호). PG 설정이 바뀐 뒤 다시 눌러 확인하면 된다.
2026-08-23 기준 **2/3 통과**(빌키 발급만 실패).

---

## 11. 루트업 문서로 확정된 것 — 2026-08-25

### ✅ 추측이었던 것들이 확정됐다

| 항목 | 이전 상태 | 확정 |
|---|---|---|
| **성공 판정** | HTTP 2xx 로 추정 | **`result_cd === '0000'`** (문서 명시). 실패는 그 외 코드 + HTTP 4xx |
| **빌키 생성 응답 필드** | 후보 키를 훑는 중 | `bill_code` · **`bill_key`** · `issuer` · `issuer_code` · `trx_id` · `trx_dttm` · `ord_num` |
| **승인 응답 필드** | 후보 키를 훑는 중 | `trx_id` · `appr_num` · `issuer` · `issuer_code` · `acquirer(_code)` · `card_num` · `installment` · `method` |
| **카드 필드 암호화** | AES 키를 받아 필요 여부 불명 | **암호화 요구 없음.** 평문 + HTTPS. 받아둔 AES 키/IV 는 이 거래 API 용이 아니다(대사/SAMW 쪽 추정) |
| **빌키 생성 요청 필드** | 동일 추정 | 동일 확인 — `mid` `tid` `ord_num` `buyer_name` `buyer_phone` `card_num` `yymm` `auth_num` `card_pw` |
| **인증 헤더** | 실호출로 확인 | 문서 일치 — `Authorization: {pay_key}` (Bearer 아님) |
| **엔드포인트 경로** | — | **전부 동일**. `/api/v2/pay/{hand,bill-key,bill-key/hand,cancel,net-cancel}`, `/api/v2/transactions/{ord_num}`, 폼형만 `/v2/pay/{auth,simple}` |
| **결제통지 규격** | — | 동일. 성공 `200`+`{}`, 실패 `200 외`+`{"message":...}`, 규격 위반 시 1분 간격 재전송 |

`auth_num` 은 여전히 **"생년월일 6자리(YYMMDD) 또는 사업자번호"** 둘 다 허용이다(§9-5 는 사실상 무의미 — 둘 다 된다).

### ⏳ 아직 없는 것

**에러 코드표.** 루트업 문서에도 목록이 없다("성공시 0000, 이외 에러코드"뿐).
실측한 코드: `PV470`(빌키 없음) · `9999`(해외카드) · `RV422`(필수값 누락).
→ 한도초과 등 **"다음 카드로 폴백" 판정을 아직 `result_msg` 문구로** 하고 있다.
코드표를 받으면 **반드시 코드 기준으로 교체할 것** — 문구는 PG 가 바꾸면 폴백이 조용히 멈춘다.

### 연결 테스트 결과 (2026-08-25)

「PG 연동·결제통지」 → 연결 테스트 **3/3 통과**.
- 빌키 삭제(없는 키) → `PV470 빌키가 존재하지 않습니다`
- 빌키 결제(없는 빌키) → `PV470` (승인 안 남)
- 빌키 발급(더미 카드) → `9999 해외카드는 빌키 발급이 불가합니다` = **가맹점 조회 통과**

> 에러 메시지 접미사가 기존 문서 환경의 `(카드사 문의요망)` 에서 `(운영사 문의요망)` 로 바뀌었다 —
> 서로 다른 환경이라는 또 하나의 방증이다.

### ✅ 실 빌키 발급 성공 — 2026-08-25

`driver=weroute` 로 전환 후 실 카드(하나) 등록 성공. 응답:

```json
{"result_cd":"0000","result_msg":"성공","bill_code":"..(10자)","bill_key":"..(64자)",
 "issuer":"하나(구외환)","issuer_code":"03","trx_id":"","trx_dttm":"2026-08-25 19:30:09",
 "ord_num":"PG13-20260825193009-53EC"}
```

- **필드명이 문서·우리 `pick()` 1순위와 완전히 일치**했다. 추측 구간 해소.
- ⚠️ 문서엔 `trx_id` 가 필수(O)인데 **빌키 발급 응답에서는 빈 값**이다. 빌키 발급은 거래가
  아니라서인 듯 — 우리는 저장하지 않으므로 문제없다.
- `issuer_code=03` 이 `system_codes.card_issuer` 의 하나(구외환)와 맞아떨어져 카드사 표기가
  자동으로 붙었다(사람이 입력하지 않는다는 설계가 실물로 검증됨).

### 남은 것

1. **실 결제 1건** — 승인 응답 필드(`trx_id`·`appr_num`)를 실물로 확인. 돈이 나가므로 갑 판단.
2. **에러 코드표** — 한도초과 등 폴백 판정을 아직 문구로 한다.