# 참고자료 — 기존(타) 솔루션의 「회사 정보 수정」 화면 분석

> **성격:** 확정 사양 아님. 다른 솔루션의 총판/대리점 설정 화면 스크린샷 6장(6개 탭)을 우리 스키마와 대조한 **분석 메모**다.
> **용도:** 향후 조직 설정 화면을 설계할 때의 체크리스트 · 갑 확인 질문 목록.
> **주의:** 여기 적힌 내용은 **갑이 확정해준 규칙이 아니다.** LOGIC.md·DB_SCHEMA.md에 반영하지 말 것. 도입 결정이 난 항목만 그때 옮긴다.
> **작성:** 2026-08-11

---

## 0. 출처에 대한 중요한 관찰

스크린샷은 단순한 "경쟁사 화면"이 아닐 가능성이 높다.

| 화면 속 값 | 우리 시스템의 같은 이름 |
|---|---|
| 회사명 `오엑스플러스(총판)` | 시드 대리점 `오엑스플러스 (OXP-0001)` |
| 대표자명 `서원빈` | 실 정산 데이터의 라이더 `서원빈` (일일정산 대상) |
| 상위회사 `한율(지사)` | 우리에 없는 계층 |
| 하위 조직명 `민세훈…` 계열 | 시드 총판 `민세훈커넥트` |

즉 **갑이 현재 쓰고 있거나 이전에 쓰던 시스템**으로 보인다. 그렇다면 이 화면들은 경쟁사 정보가 아니라 **갑의 실제 운영 요구사항이 이미 구현된 형태**이며, 여기 있는 필드는 "있으면 좋은 것"이 아니라 **갑이 실무에서 실제로 쓰던 값**일 확률이 높다. 분석 우선순위를 그 전제로 매겼다.

⚠️ 다만 **추정이다.** 갑에게 "이 화면이 현재 쓰시는 시스템 맞는지" 먼저 확인할 것. 맞다면 이 문서의 격차 목록이 곧 마이그레이션 요구사항이 된다.

> 스크린샷에 실제 사업자번호·계좌번호가 찍혀 있었으나 이 문서에는 옮기지 않았다(마스킹).

---

## 1. 탭 구성

기존 시스템은 조직 1개를 **6탭 모달** 하나로 관리한다.

```
[일반정보] [플랫폼설정] [수수료설정] [결제설정] [기사기본값] [프로모션 설정]
```

우리는 같은 정보가 **여러 화면에 흩어져 있다**:
`system/orgs`(조직 기본) · `system/pg-fee`(수수료 분배) · `withdrawal/settings`(정산수수료·보증금) · `withdrawal/payment-setup`(카드) · `deduction/agency-fee`(대행수수료) · `system/settlement-excel`(엑셀 암호) · 라이더 상세(원천세 토글).

→ 기능 격차와 별개로 **"조직 1개 = 설정 1곳"** 이라는 정보구조 자체가 참고할 만하다. 지금은 대리점 하나를 세팅하려면 화면 6곳을 돌아야 한다.

---

## 2. 탭별 필드 → 우리 대응 매핑

범례: ✅ 동등 기능 있음 · 🟡 구조는 있으나 형태/위치 다름 · ❌ 없음 · ⛔ 우리 정책과 충돌

### 2-1. 일반정보

| 기존 필드 | 우리 대응 | 판정 |
|---|---|---|
| 회사명 / 별칭 | `organizations.name` (별칭 없음) | 🟡 |
| **송금자** (출금 시 송금자명) | 없음 | ❌ **중요** |
| 회사구분 / 상위회사 | `organizations.level` / `parent_id` | ✅ |
| 대표자명 / 대표자HP / 생년월일 | `contact_name`/`contact_phone`(=담당자, 대표자와 다름) | 🟡 |
| 연락처 / EMAIL | `contact_phone` / 없음 | 🟡 |
| **사업자 정보** (사업자명·사업자번호·업태·종목·주소) | 없음 | ❌ **중요** |
| 계좌정보 (은행·계좌번호·예금주) | `agency_bank_accounts`(별도 테이블, 핀테크번호 위주) | 🟡 |
| **계좌 「미확인」 배지 + 「계좌확인」 버튼** | 없음 (예금주 실명조회 개념 자체가 없음) | ❌ **중요** |
| **정산회사** (본사/지사/총판/대리점 라디오) | 없음 — 우리는 대리점이 항상 정산 주체 | ❌ 개념차 |
| 비활성화 버튼 | `Organization::setActive` | ✅ |

**주목할 점 — 「정산회사」가 「회사구분」과 별개다.** 스크린샷의 조직은 회사구분=총판인데 정산회사=대리점으로 찍혀 있다. 계층상 위치와 "정산·지급을 실제로 수행하는 주체"를 분리해 지정하는 구조다. 우리는 이 둘이 항상 일치한다고 가정하고 전체를 짰다(`settlement_uploads.agency_id`, `agency_wallets`, PG 결제 주체 전부). 갑이 이 분리를 실제로 쓰고 있었다면 파급이 크다.

### 2-2. 플랫폼설정 (배민 / 쿠팡 각각)

| 기존 필드 | 우리 대응 | 판정 |
|---|---|---|
| 배민/쿠팡 **아이디 다건 등록·삭제** | 없음 (조직의 플랫폼 계정 개념 없음. `rider_platforms`는 라이더용) | ❌ |
| 대리점 수수료: **비율% + 건당원 동시** | `AgencyFeeConfig`는 적립일수 구간별 **건당 정액만** | 🟡 구조차 |
| 수수료 **선차감** 체크 | `is_daily_settlement`에서 파생(별도 토글 없음) | 🟡 |
| 쿠팡 전용 **선차감 수수료** (비율/건당, 원천세공제, 기사설정무관 항상차감) | 없음 | ❌ |
| 고용/산재: **사업주부담금 / 기사부담금 / 주간정산 추가지급 납부** 선택 | 우리는 **기사부담 고정**(전원 무조건 공제) | ⛔ |
| **고용산재적립처 지정** (사업주·기사 부담분 각각 어느 조직에 적립) | 없음 — 갑이 "고용·산재는 예수금 아님" 확정(2026-07-15) | ⛔ |
| **「주정산 계산값사용(해제 시 엑셀값)」** | 우리는 항상 계산(0.8%/0.88%). 엑셀에도 값이 있으나 미사용 | ❌ 옵션차 |
| 주정산 선정산기사: 시간제보험료 제외 / 추가지급 제외 | 없음 (주정산 회차 × 선정산 기사 교차 예외 처리 없음) | ❌ |
| 주정산 **협력사자체미션 시트 사용** | 해당 탭 **미파싱**(DB_SCHEMA §4에 명시) | ❌ |
| 주정산 **관리비 정산** (정산회사→대리점 지급, 부가세 +10%) | 없음 | ❌ 신규개념 |

### 2-3. 수수료설정

| 기존 필드 | 우리 대응 | 판정 |
|---|---|---|
| 지사 수수료 | 지사 계층 없음 | ⛔ (갑이 "지사 없음" 확정) |
| 총판/대리점 수수료: **비율% + 건당원 + 별칭** | `org_fee_config.hq_pct/distributor_pct/agency_pct` — **비율만**, 건당 정액·별칭 없음 | 🟡 |
| 원천세(3.3% 기사) 차감 체크 | `riders.withholding_tax_enabled` — **라이더별만**, 조직 기본값 없음 | 🟡 |
| 원천세 **적립처 지정** | `agency_wallets.withholding_reserve` **고정** | 🟡 |
| **선정산 수수료 슬롯 1~4** (각 비율%+건당원+적립처) | 대행수수료 **1종**(대리점 몫 고정) | ❌ **구조차 최대** |
| **주정산 수수료 슬롯 1~3** (동일) | 없음 — 주정산 라이더는 대행수수료 미부과(2026-08-08 확정) | ⛔ |
| 적립처 드롭다운이 **「권한 없음」으로 잠김** | 화면 단위 권한(`role_permissions`)만 있고 **필드 단위 잠금 없음** | ❌ 패턴차 |

**주목할 점 — 다중 수수료 슬롯 + 슬롯별 적립처.** 기존 시스템은 수수료를 "한 건에서 여러 조각을 떼어 각각 다른 조직에 적립"하는 범용 엔진으로 만들었다. 우리는 용도별로 개별 구현(대행수수료=대리점, 정산수수료=본사, 플랫폼수수료=3분할)해서 슬롯을 늘릴 수 없다. 갑이 계약별로 수수료 구성을 바꿔 쓰고 있었다면 우리 구조로는 대응이 안 된다.

또 **「권한 없음」 잠금**이 시사하는 바 — 총판 계정으로 열었을 때 상위(본사)가 정한 적립처는 회색으로 보이기만 한다. 우리 권한 모델은 라우트×area 단위라 이런 표현이 불가능하다.

### 2-4. 결제설정

| 기존 필드 | 우리 대응 | 판정 |
|---|---|---|
| 가맹점 정보 **[BC] / [WG] 2개 PG** + 가맹점 프로필(`페이플러스2_10분`) | 갑이 "플랫폼 전체 **단일 PG사**" 확정(2026-07-03) | ⛔ |
| 등록된 카드 목록 + 카드 등록 | `agency_cards` | ✅ |
| — | **우리에만 있음**: `priority` 순 자동 대체결제, `mock_limit` 한도 시뮬레이션 | ➕ |

### 2-5. 기사기본값

「신규 기사 등록 시 자동 적용될 값」 세트다.

| 기존 필드 | 우리 대응 | 판정 |
|---|---|---|
| **배민 정산방식 / 쿠팡 정산방식 각각 선정산·주정산** | `riders.is_daily_settlement` — **라이더당 단일 플래그, 플랫폼 구분 없음** | ❌ **스키마 영향** |
| 원천세 차감 | `riders.withholding_tax_enabled` (조직 기본값 없음) | 🟡 |
| 보증금 | `withdrawal_config.reserve_amount` (대리점별 설정 가능) | ✅ |
| **"기본값 세트"라는 개념 자체** | 없음 — 라이더 등록 기본값이 코드 상수 | ❌ |

**주목할 점 — 정산방식이 플랫폼별이다.** 한 라이더가 배민은 선정산, 쿠팡은 주정산일 수 있다는 뜻이다. 우리는 `is_daily_settlement` 하나로 라이더 전체를 가르고, 정산 반영·일일정산 지급·대행수수료 부과가 전부 이 플래그에 매달려 있다. 사실이라면 컬럼 하나 추가로 끝나지 않고 `SettlementLedger`·`DailyPayout` 분기 전반이 영향받는다.

### 2-6. 프로모션 설정

| 기존 | 우리 |
|---|---|
| 배민/쿠팡 각각 **4가지 지급 모드**: 전체건 지급 / 초과건 지급 / 고정금 지급 / 최저금액 보장 | 없음 |
| **5단계 × (일일건수·일일지급금액·주간건수·주간지급금액)** 룰 테이블 | 없음 |
| 「주정산시 선정산기사포함」 토글 | 없음 |
| 건수 도달을 보고 **자동 계산·지급** | `Promotion` = **엑셀 업로드로 라이더별 금액 수동 입력** |

판정: ❌ **기능 격차 최대 지점.**

기존 시스템은 **건수 기반 룰 엔진**이다. 대리점이 단계표를 세팅해두면 정산 시 자동으로 프로모션이 계산된다. 우리 `promotion_batches`/`promotion_entries`는 사람이 엑셀에 금액을 적어 올리는 방식이라 룰이 없다.

모드별 의미(화면 하단 설명 기준):
- **전체건 지급** — 모든 건에 지급금액 추가 (자동)
- **초과건 지급** — 기준 건수를 넘은 건만 추가 (자동)
- **고정금 지급** — 도달한 가장 높은 단계 금액을 1회만 (자동)
- **최저금액 보장** — 기준 건수 초과 건의 배달료를 보장금액까지 끌어올림 (**수동**)

⚠️ 혼동 주의: `settlement_daily_riders.fee_promo1~4`는 **쿠팡이 정산서에 이미 계산해 넣어준** 플랫폼 프로모션이다. 이 탭은 **대리점이 자체적으로 얹는** 프로모션이라 별개 축이다. 우리 `Promotion` 모듈이 그 자리를 차지하고 있다.

---

## 3. 격차 정리

### A. 빠져 있고, 곧 실제로 막힐 것 (우선순위 높음)

1. **송금자명(입금자 표시명)** — `FirmBankingGateway::transfer()` 시그니처에 이 인자가 없다(`inc/FirmBankingGateway.php:29`). 라이더 통장에 찍힐 이름을 지정할 방법이 현재 없다. 쿠콘/하이픈 실 스펙이 오면 거의 확실히 요구되는 필드다.
2. **사업자 정보(사업자번호·상호·업태·종목·주소)** — 우리는 「원천세 명세」(`settlement/withholding`) 화면까지 만들어 놓고 정작 **신고 주체의 사업자번호가 없다.** 세금계산서·지급명세서로 넘어가는 순간 막힌다.
3. **계좌 실명조회(예금주 확인) 상태** — 기존 시스템은 계좌에 「미확인」 배지와 「계좌확인」 버튼을 둔다. 우리 `agency_bank_accounts`엔 검증 상태 개념이 없어, 잘못 입력된 계좌가 이체 실패로만 드러난다.
4. **조직별 「기사 등록 기본값」** — 지금은 코드 상수라 대리점마다 다른 운영 관행을 못 담는다.

### B. 구조가 달라서, 도입하려면 재설계인 것

5. **다중 수수료 슬롯 + 슬롯별 적립처 지정** (선정산 4 / 주정산 3)
6. **비율% + 건당원 병행** 수수료 (우리는 정액 전용)
7. **프로모션 룰 엔진** (건수 단계별 자동 지급 4모드)
8. **플랫폼별 정산방식** (`is_daily_settlement` 단일 플래그와 충돌)
9. **「정산회사」와 「회사구분」 분리**
10. **관리비 정산** (정산회사 → 대리점 지급, 부가세 별도)
11. **필드 단위 권한 잠금** (「권한 없음」 회색 처리)

### C. 갑이 이미 "안 쓴다"고 확정한 것 — 도입 금지 또는 재확인

| 항목 | 우리 확정 내용 | 출처 |
|---|---|---|
| 지사 계층 | "지사 없음, 본사>총판>대리점 3단" | 2026-07-03 |
| 고용/산재 적립 | "예수금 아님, 공제하고 끝" | 2026-07-15 |
| 멀티 PG 가맹점 | "플랫폼 전체 단일 PG사" | 2026-07-03 |
| 주정산 대행수수료 | "선정산 라이더 반영 시점에만 부과" | 2026-08-08 |

→ **화면에 있다는 이유로 되살리지 말 것.** 되살리려면 갑에게 "예전 시스템엔 있었는데 이번엔 빼는 게 맞느냐"를 다시 물어야 한다.

### D. 우리가 더 나은 부분 (유지)

- 카드 **다건 + priority 자동 대체결제** — 기존 화면은 카드 목록만 보인다
- **age-bucket 정산수수료** (주문별 경과일 80/40원 합산)
- **부채 원장** (대여금/리스/선지급, 리스 3자 수수료 배분) — 기존 화면에 대응 항목 없음
- **감사로그 전면 적용**, 본사 수동조정 경로
- **보증금 경계 정책** (건 단위 출금, 5만원 상시 확보)

---

## 4. 갑에게 확인할 것 — ✅ 2026-08-15 전부 답변 완료

| # | 질문 | 답변 | 의미 |
|---|---|---|---|
| 1 | 이 화면이 현재 운영 중인 시스템인가? | **아니요** | **가장 중요한 답.** 이 스크린샷은 갑의 실제 운영 요구사항이 아니라 무관한 남의(예전) 시스템이었다. §3의 격차 목록(A·B) 전체가 **실제 요구사항이 아니라 단순 참고 관찰로 격하**된다. |
| 2 | 「정산회사」≠「회사구분」으로 쓴 적 있나? | **아니요** | 우리 전제("대리점=항상 정산 주체")가 맞다. §3-B #9 재설계 불필요 — **폐기**. |
| 3 | 정산방식(선/주)을 배민·쿠팡 다르게 가져간 라이더 있나? | **아니요** | `riders.is_daily_settlement` 단일 플래그로 충분. §3-B #8 재설계 불필요 — **폐기**. |
| 4 | 수수료 슬롯을 몇 개나 채워 썼나? | **아니요**(안 씀) | 대행수수료 1종 고정 구조로 충분. §3-B #5·#6 재설계 불필요 — **폐기**. |
| 5 | 프로모션 4모드 중 실제로 쓴 건? | **"프로모션은 내가 따로 지시해줄꺼야"** | 4모드 룰 엔진을 지금 추정으로 만들지 않는다. **별도 지시 대기 — 착수 보류.** §3-B #7 보류. |
| 6 | 관리비 정산을 쓰나? | **없어** | 신규 개념 불필요. §3-B #10 — **폐기**. |
| 7 | 고용/산재 「사업주부담금」을 선택해 쓴 적 있나? | **없어** | 2026-07-15 확정("전원 기사부담")과 **충돌 없음**, 화면의 사업주부담 옵션은 참고할 필요 없음. |
| 8 | 협력사 자체 미션 시트를 쓰나? | **안써** | 파싱 추가 불필요. |

**결론 — §3-B(구조 재설계 항목) 전부 폐기, §3-A(우선순위 높음 4건)는 아래 §5로 재평가.**

---

## 5. 다음 단계 — ✅ 확인 완료, §3-A 재평가

- Q1이 "아니요"이므로 이 문서의 격차 목록은 더 이상 마이그레이션 요구사항이 아니다. **LOGIC.md·DB_SCHEMA.md에 반영할 항목 없음.**
- §3-B(5~11) 전부 폐기. §3-C(지사 계층·고용산재 적립·멀티PG·주정산 대행수수료)는 기존 확정과 그대로 일치하므로 변경 없음.
- §3-A(우선순위 높음 4건: 송금자명·사업자정보·계좌실명조회·조직별 기사등록 기본값)는 **이 화면과 무관하게 우리 시스템 자체의 실제 약점**이라 별도로 유효하다. 다만 "지금 당장 막힌다"는 근거였던 갑 화면이 사라졌으므로 **급한 순서가 아니라 필요해질 때(펌뱅킹 실 스펙 도착 등) 다시 판단**한다.
- **프로모션 자동화(§3-B #7)는 예외적으로 살아있다** — 폐기가 아니라 "갑이 별도로 요구사항을 줄 예정"으로 상태만 바뀜. 지시 오면 이 섹션이 아니라 새 문서/LOGIC.md에 반영.
