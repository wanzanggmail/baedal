# 2026-08-12 갑 회의 — 전달사항 및 영향 분석

> **성격:** 갑이 회의에서 확정해 전달한 규칙 + 현재 코드와의 대조 결과.
> **상태:** ✅ **2026-08-13 전 항목 구현 완료.** 아래 ❓ 4건은 2026-08-12 갑 추가 답변으로 전부 해소됐다(§확인 답변 참고).
> **작성:** 2026-08-12 · **갱신:** 2026-08-13

---

## ✅ 갑 확인 답변 (2026-08-12) — 미결 4건 전부 해소

| # | 질문 | 갑 답변 |
|---|---|---|
| A | 최저금액 적용 단위 | **출금 1건당** |
| B | 최저금액 vs 3분할 관계 | **본사 몫이 최저금액으로 정해지고**, 대리점은 그 이상을 받아야 하며, 출금 1건당 그 금액을 본사 지갑으로 보낸다 |
| B' | 총액이 최저금액보다 작을 때 | **"최저금액은 배달 건당금액이야"** — 최저금액 자체가 배달 건당 정액이라 총액 미달 상황이 구조적으로 생기지 않는다 |
| C | 설정 범위 | **대리점별** |
| D | 기존 `pg_fee_in` 적립분 | **"과거 데이터는 신경쓰지마"** |

추가 지시: *"정산수수료에 대해서는 본사 총판 대리점 몫을 나눠서 해당 지갑으로 배분해줘. 일일정산이 아니더라도 해야해."*

### 추가 확정 (2026-08-12 2차)
- **대리점 몫이 0이 되어도 된다** — 갑: *"대리점은 0이 되어도 되. 어차피 본사만 잘 받으면 되니까 대리점을 알아서 해야지."*
  → 처음에 넣었던 "본사 건당 몫 < 건당 수수료" 저장 검증을 **제거**했다. 본사 몫이 걷은 총액을 넘으면 총액까지만 가져가고 대리점 몫이 0이 된다(음수로는 내려가지 않는다 — 그러면 대리점 지갑에서 없던 돈이 빠진다).
- **일일정산에도 정산수수료를 부과한다** — 갑: *"일일정산도 정산수수료를 부과해야해."*
  → `DailyPayout::payRider()`(원클릭 지급)가 주정산과 같은 age-bucket 규칙으로 수수료를 떼고 배분까지 실행한다. 지급 리스트 화면에 「잔액 / 정산수수료 / 실지급액」 컬럼을 분리 표기.
  → 정산반영 시 자동출금(`DailyAutoWithdrawal`)은 내부적으로 `rider_manual` 경로를 쓰므로 **이미 적용돼 있었다**(별도 수정 불필요).

### 확정된 계산 모델
```
본사 몫   = 배달 건당 정액(hq_fee_per_order) × 출금에 포함된 배달 건수
총판 몫   = (총 정산수수료 − 본사 몫) × fee_share_distributor_pct
대리점 몫 = 나머지 전부 (끝수를 여기로 몰아 3몫 합 = 총액 보장)
```
- 본사 건당 몫이 건당 정산수수료보다 커도 **막지 않는다**(2차 확정). 그 경우 본사가 걷은 총액 전부를 가져가고 대리점 몫은 0이 된다.
- **대리점 몫은 지갑 이동이 없다** — 정산수수료는 라이더 지갑에서 빠져 이미 대리점 지갑에 남아 있는 돈이라, 본사·총판 몫만 대리점 지갑에서 빼서 옮긴다(리스 수수료 배분과 같은 패턴).
- 적용 경로 **3곳**: 주정산 수동 완료(`markCompleted`) · 주정산 펌뱅킹 이체(`executeTransfers`) · 일일정산 원클릭 지급(`DailyPayout::payRider`).

범례: ✅ 현재 코드가 이미 그렇게 동작 · 🔧 변경 필요 · 🆕 신규 구현 · ❓ 해석 확인 필요

---

## 1. 「라이더 채무」 → 「라이더 정산금」 (표기 변경) 🔧

대리점 지갑의 차감 항목 이름을 바꾼다. "채무"라는 표현이 대리점이 빚진 것처럼 읽혀 부적절.

**영향 범위 — 표시 문자열 5곳:**

| 파일 | 위치 |
|---|---|
| `admin/views/agency_payout.php` | 53 (− 라이더 채무) |
| `admin/views/system_manual_adjust.php` | 107 |
| `admin/views/system_orgs.php` | 560 (라이더 채무 / 원천세예수금) |
| `admin/views/wallet_ledger.php` | 268 |
| `inc/AgencyWallet.php` | 63 (주석) |

내부 키 `rider_debt`(배열 키)는 그대로 둬도 무방하다 — 화면 문구만 바꾸면 된다. 굳이 키까지 바꾸면 API 응답을 읽는 곳을 전부 손봐야 해서 이득 대비 위험이 크다.

**리스크 없음. 즉시 착수 가능.**

---

## 2. 고용·산재는 따로 남기지 않고 대리점이 출금 가능 ✅

**현재 이미 그렇게 동작한다. 코드 변경 없음.**

- `AgencyWallet::withdrawable()` = `balance − 라이더정산금 − withholding_reserve`
- `withholding_reserve`에 누적되는 건 **원천세뿐**이다 (`SettlementLedger.php:655`가 원천세만 `addWithholdingReserve()` 호출).
- 고용보험(0.8%)·산재보험(0.88%)은 라이더 몫에서 **공제만** 되고 별도 적립처가 없다 → 대리점 잔액에 그대로 남아 출금 가능.

2026-07-15 확정("고용·산재는 예수금이 아니다")과 동일한 내용이 재확인된 것이다.

> 💡 **부수 소득:** [REF_LEGACY_ORG_SETTINGS.md](REF_LEGACY_ORG_SETTINGS.md) §2-2에서 "기존 시스템엔 「고용산재적립」(부담금별 적립 주체 지정) 기능이 있는데 우리 정책과 충돌한다"고 올려둔 미확인 항목이 **이번 결정으로 해소**됐다 — 그 기능은 도입하지 않는다.

---

## 3. 정산수수료 최저금액 — 본사가 설정, 본사 귀속 🆕❓

> 갑 원문: *"본사에서 정산수수료(건당 40원씩 떼는것)의 최저 금액을 설정할 수 있고 그 최저금액은 본사로 들어간다"*

**현재:** 최저금액 개념이 없다. 정산수수료는 age-bucket 건당 합산만 한다
(`WithdrawalConfig::feeForCycles` — 기준일 7일 이내 주문 건당 80원 / 지난 주문 건당 40원).

**필요한 것:** `withdrawal_config`에 최저금액 컬럼 + 본사 설정 화면 + 계산 시 하한 적용.

### ❓ 확인 필요 A — 최저금액의 적용 단위
- (가) **출금 1건당**: 이번 출금의 정산수수료 합계가 최저금액보다 작으면 최저금액으로 올림
- (나) **기간(월) 단위**: 한 달 누적 정산수수료가 최저금액에 못 미치면 차액을 별도 청구

→ 계산 시점·저장 위치가 완전히 달라진다. (가)면 출금 미리보기에서 즉시 반영, (나)면 정산 배치가 따로 필요.

### ❓ 확인 필요 B — 「최저금액은 본사로」와 4번(3분할)의 관계
4번에서 정산수수료를 본사/총판/대리점이 나눠 갖기로 했는데, 3번은 최저금액이 본사로 간다고 한다. 두 가지 해석이 가능하다:

- (가) **최저금액에 걸린 회차는 전액 본사** — 계산값이 최저 미만이면 3분할 없이 통째로 본사
- (나) **부족분만 본사** — 계산값은 3분할하고, 최저금액을 채우려고 더 얹은 차액만 본사

→ 총판·대리점 수취액이 달라지므로 반드시 확정 후 착수.

### ❓ 확인 필요 C — 최저금액의 설정 범위
본사가 **전역 1개** 값으로 정하는지, **대리점마다** 다르게 정하는지.
(보증금·건당수수료는 2026-07-16에 "대리점별 설정"으로 확정된 이력이 있어 혼동 소지가 있다.)

---

## 4. 정산수수료 = 본사 + 총판 + 대리점 3분할 🆕 (대형)

> 갑 원문: *"정산수수료는 본사 수수료, 총판수수료, 대리점수수료로 나뉜다"*

**기존 확정 뒤집힘.** 2026-07-16에 "정산수수료 수취인은 본사"로 확정했던 것이 3분할로 바뀐다.

### ⚠️ 착수 전 반드시 정리해야 할 선행 문제

코드를 확인해보니 **정산수수료가 현재 어느 지갑으로도 들어가지 않는다.**

- 라이더 지갑: `RiderWallet::deductAfterWithdrawal(rider_id, amount + 정산수수료)` — 수수료만큼 더 빠짐 ✅
- 본사 지갑: **적립 코드 없음** — "본사 몫"이라는 건 문서에만 있고 원장에 안 남음
- 대리점 지갑: `rider_manual`(주정산 출금)은 `AgencyWallet::debit()` 호출이 **아예 없음**
  (일일정산 `DailyPayout`만 차감함 — `inc/DailyPayout.php:181`)

즉 주정산 출금은 라이더 지갑만 줄고 대리점 지갑은 그대로다. 3분할을 얹기 전에 **"이 돈이 실제로 어느 지갑에서 나와 어느 지갑으로 가는가"**를 먼저 확정해야 한다. 지금 상태에서 분배만 추가하면 원장이 안 맞는다.

### 구현 시 필요한 것
- 정산수수료 분배 요율 설정 (`org_fee_config`의 hq/distributor/agency_pct와 같은 형태, 다만 별도 테이블·컬럼)
- 출금 확정 시 3개 조직 지갑에 원장 기록 (`agency_wallet_ledger`에 새 reason 코드)
- 5번(플랫폼 수수료는 지갑에 안 넣음)과 **방향이 반대**라는 점 유의 — 정산수수료는 지갑에 넣고, 플랫폼 수수료는 안 넣는다. 의도된 차이인지 확인하면 좋다.

---

## 5. PG 결제 시 플랫폼 수수료 규칙

| # | 갑 전달 내용 | 현재 코드 | 판정 |
|---|---|---|---|
| 5-1 | 총 금액에서 수수료 %만큼 **더** 결제 | `total_charged = net + service_fee` | ✅ 일치 |
| 5-2 | 지갑에 들어가는 건 수수료를 **제한** 총금액 | `AgencyWallet::credit(net)` — 순액만 충전 | ✅ 일치 |
| 5-3 | 플랫폼 수수료는 **지갑에 더하지 않고**, 본사에서 대리점·총판별 발생 내역만 기간 조회 | `PgPayment::creditFeeSplit()`가 본사/총판/대리점 지갑에 `pg_fee_in`으로 **적립 중** | 🔧 **변경 필요** |

### 5-3 변경 내용
- `inc/PgPayment.php:296` `creditFeeSplit()` 의 지갑 적립을 **중단**한다.
- 분배 금액 자체는 `pg_payments`의 스냅샷 컬럼(`hq_amount`/`distributor_amount`/`agency_amount`)에 이미 저장되므로 **조회용 데이터는 그대로 남는다.**
- 조회 화면 「플랫폼 수수료 내역」(`settlement/platform-fee`)이 이미 있고 기간 필터도 있다 → 대리점별·총판별 집계 형태가 요건에 맞는지만 점검하면 된다.
- 실제 정산은 본사가 계산서를 발행해 오프라인으로 처리(시스템 밖).

### ❓ 확인 필요 D — 기존에 적립된 `pg_fee_in` 처리
이미 지갑에 들어간 과거 플랫폼 수수료 적립분을 **되돌릴지**(취소 원장), **그대로 둘지** 결정 필요.
개발 DB라 실데이터 영향은 없지만, 운영 전환 시점에 한 번은 정해야 한다.

---

## 6. 리스·렌탈 수수료 배분 = 본사 전용 🔧

> 갑 원문: *"리스 렌탈의 경우 수수료 배분 기능은 본사에서만 한다. 대리점이나 총판은 볼 필요 없음. 대리점에서는 수수료배분없이 가져간다."*

**두 부분으로 나뉜다:**

**(1) 화면 접근 제한 — 🔧 변경 필요**
`deduction/lease-fees`(「리스 수수료 배분」)가 현재 `deduction` 영역 권한만 있으면 열린다
(`inc/sidebar.php:161`이 `admin_can_access_route()`로 게이트) → 대리점·총판도 보임.
→ **본사 전용으로 제한**하고 사이드바에서도 숨긴다.

**(2) 대리점 제공 리스는 배분 없이 전액 대리점 — ✅ 이미 일치**
`rider_debts.lease_provider`가 `agency`면 배분 참여자가 대리점 단독이고,
상위 조직 몫은 서버에서 0으로 강제된다(DB_SCHEMA §5). 정책이 이미 그대로다.

---

## 구현 결과 (2026-08-13)

| 항목 | 상태 | 반영 위치 |
|---|---|---|
| 1. 라이더 채무 → 라이더 정산금 | ✅ 완료 | 화면 4곳 + 주석·문서 (내부 키 `rider_debt`는 유지) |
| 2. 고용·산재 미적립, 대리점 출금 | ✅ 변경 불필요 | 이미 그렇게 동작 |
| 3·4. 정산수수료 최저금액 + 3분할 | ✅ 완료 | `withdrawal_config`에 `hq_fee_per_order`·`fee_share_distributor_pct` 추가, `WithdrawalConfig::feeShare()`, 신규 `inc/WithdrawalFeeShare.php`, `Withdrawal` 완료 경로 2곳 연결, 설정 UI(본사만 편집) |
| 5-1·5-2. PG 결제·충전 금액 | ✅ 변경 불필요 | 이미 일치 |
| 5-3. 플랫폼 수수료 지갑 적립 중단 | ✅ 완료 | `PgPayment::creditFeeSplit()` 제거, `pg_payments` 스냅샷 + 「플랫폼 수수료 내역」 조회만 유지 |
| 6. 리스 수수료 배분 본사 전용 | ✅ 완료 | `admin_can_access_route()` 본사 제한 + 뷰 자체 방어 |

### 검증
- 분배 계산: 배달 10건/3건, 80·40원 혼합, 건수 0(구 출금), 끝수(401원) 전 케이스에서 **3몫 합 = 총액** 확인
- 설정 방어: 본사 건당 몫 ≥ 건당 수수료면 저장 거부
- 대리점이 저장할 때 배분 설정이 초기화되지 않고 유지되는지 확인
- 실제 지갑 이동 e2e: 총 400원·10건 → **본사 +100 · 총판 +90 · 대리점 −190**(대리점 몫 210은 이동 없이 잔류), 자금보존 합 0, 원장 3행(`wd_fee_up`/`wd_fee_in`) 기록. 테스트 데이터 전량 원복

### 남은 참고 사항 (갑 확인 대상 아님 — 구현 판단 필요)
**주정산(`rider_manual`) 출금 시 대리점 지갑이 차감되지 않는다.** 라이더 지갑만 줄고 대리점 지갑은 그대로다(일일정산 `DailyPayout`만 `AgencyWallet::debit()` 호출). 이번 정산수수료 배분은 "대리점 지갑에 남아 있는 수수료를 상위로 옮기는" 부분만 담당하므로 정상 동작하지만, **라이더에게 실제로 나간 실지급액이 대리점 지갑에서 빠지지 않는 것은 별개의 기존 문제**다. 펌뱅킹 실연동 전에 정리 필요.
