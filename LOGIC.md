# 도깨비 배달대행 — 비즈니스 로직 정리

> **목적:** 페이지별로 기능을 붙여 나가면서 **규칙·상태·권한·DB 연동 여부**가 어긋나지 않도록 기준 문서를 둡니다.  
> **유지:** 화면/API/도메인 클래스를 수정할 때 **해당 섹션을 함께 갱신**하세요. (`.cursor/rules/logic-md-sync.mdc`)  
> **DB 스키마 상세:** 테이블·컬럼·관계는 [`DB_SCHEMA.md`](./DB_SCHEMA.md) 참고 — 스키마 변경 시 그 문서도 함께 갱신(`.cursor/rules/db-schema-sync.mdc`).

**현재 국면:** 2026-07-02 갑 미팅으로 정산·출금 로직이 대대적으로 재설계됨. 멀티테넌시 기반(§2)은 이미 구현·검증 완료 — **재설계는 정산/출금/콘텐츠 권한 도메인에 국한**되며 기반 인프라는 변경 없음. 협의 원본 근거는 프로젝트 메모리 `client-business-rules.md`.

**상태 표기:** ✅ 확정·구현됨 · 🔧 기존 코드 수정 필요 · 🆕 신규 개발 · ❓ 사양 자체 미확정(갑 확인 필요) · ⚠️ 이번 검토에서 발견한 충돌·리스크

---

## 1. 전체 프로세스

```
① 조직 설정          본사가 총판·대리점 생성 + 로그인 계정 동시 발급
        │
② 라이더 등록         대리점이 직접 등록 (겸업 시 대리점마다 별도 계정)
        │
③ 정산 업로드         쿠팡이츠 정산 엑셀 → 미리보기(파싱+매칭) → 미매칭 즉시등록 → 확정
        │
④ 정산 반영           수수료 계산(선정산수수료·정산수수료·원천세(대상자만)·고용·산재
        │             ·시간제보험·선지급차감) → 라이더 지갑(rider_wallets) 적립
        │             정정 필요 시 본사 전용 수동조정 화면으로 대응(§5.4a)
        │
⑤ PG 카드결제         대리점 등록카드로 **라이더별 건건히**(총액+영업대행수수료) 결제 — 자금 조달(FUND)
        │             카드 여러 개 등록 가능, 한도초과 시 다음 카드로 자동 재시도
        │             성공(건별) → 대리점 잔액(agency_wallets) 충전, 영업대행수수료 3자 분배
        │             최종실패(전카드) → 알림 + 실패이력(사유) + 재시도(해당 라이더 건만)
        │
⑥ 지급                선정산 대상 → 관리자가 "일일정산 리스트"에서 원클릭 지급
        │             주정산 대상 → 라이더 전액출금 신청 → 대리점 수기 승인
        │
⑦ 오픈뱅킹 이체        대리점 계좌 → 라이더 계좌 — 실지급(DISBURSE)
                     성공 → 완료 / 실패 → 알림 + 실패이력(사유) + 재시도

(분기) 대리점 자체 인출  agency_wallets.balance − 라이더채무 − 원천세 예수금 만큼 인출 가능
                     ⚠️ withdrawal_requests 스키마와 충돌 — 개발 시 처리(§7 #1)

(별도 흐름) 공지·배너    본사만 작성 → 전 조직 자동 broadcast → 라이더 앱 노출
```

시각 자료: 이번 세션에서 그린 순서도 참고(대화 내 아티팩트).

---

## 2. 조직 계층 (멀티테넌시) — ✅ 기반 완료

```
본사(admin, 루트 1개)
 └─ 총판(distributor, N)
     └─ 대리점(agency, N)
         └─ 라이더(riders)
```

| 규칙 | 내용 |
|------|------|
| 조직 = `organizations`(id, parent_id, level, name, code, is_active) | 계정 ≠ 조직. 로그인 계정은 `admins.org_id`로 소속, 한 조직에 여러 계정 가능 |
| 라이더 소속 | `riders.agency_id` → `organizations.id`(level=agency) |
| 데이터 스코프 | 본사=전체 · 총판=하위 대리점 전체(조회만) · 대리점=자기 것만 |
| 권한 = 범위×기능 2축 | 범위(org_id 트리 위치) × 기능(`admins.role`: super/operation/settlement/admin) |
| 스코핑 구현 | `inc/Org.php`(`scopeAgencyIds`/`scopeOrgIds`/`agencyScopeClause`/`canAccessAgency`), 목록 쿼리에 `WHERE agency_id IN (:scope)` 주입 |

🔧 **조직 생성 권한 축소 필요** — 현재 총판도 대리점을 생성할 수 있게 구현돼 있으나, **본사만 조직 생성 가능**해야 함(`Organization::create`, `admin_can_manage_orgs()` 수정). 총판의 조회 권한은 변경 없음.

🆕 **대표·서브계정 구조** — 대리점·총판 하나에 대표계정 1개 + 서브계정 여러 개, 대표가 자기 조직 범위 내 서브계정을 만들고 권한을 부여할 수 있어야 함. 현재 `system/admins`는 super 전용이라 신규 화면·API 필요. "대표계정"을 DB에 표시하는 방법(플래그 vs 최초계정) 설계 필요.

---

## 3. 메뉴 구성

| 메뉴 | 본사 | 총판 | 대리점 | 상태 |
|------|:---:|:---:|:---:|------|
| 대시보드 | 전체 | 하위합산 | 자기것 | ✅ + 🆕 리스크알림·큰금액하이라이트 |
| 정산 업로드 / 반영 | — | 조회 | ✓ | ✅ (반영=PG결제 트리거) |
| 업로드 이력 / 수수료 내역 | ✓ | ✓ | ✓ | ✅ |
| 선공제(대행수수료) 설정 | — | — | ✓ | ✅ + 🔧 계산시점 토글 |
| 대여금·리스 원장 (`deduction/debts`) | — | — | ✓ | ✅ 신규(2026-07-24) — 대여금/리스/선지급 잔액 원장, §5.7. 구 "선지급(대여금) 입력"(`deduction/advance`)을 대체해 **메뉴에서 제거**(라우트·화면은 잔존, 직접 URL 접근만 가능) |
| 출금 신청 목록·승인 | 조회 | 조회 | 승인 | ✅ 승인주체 재검증 |
| 출금 정책 설정 | — | — | 보증금+정산수수료(기준일수·금액, 전부 개별) | ✅ |
| 대리점 계좌 등록(오픈뱅킹) | 계약주체 | — | 등록 | 🆕 |
| 대리점 카드 등록(PG) | — | — | 등록 | 🆕 |
| 결제수수료 분배 계약 | ✓ | — | — | 🆕 |
| 대리점 자체 정산금 인출 | — | — | 신청·바로 실행(승인절차 없음) | 🆕 ⚠️ 스키마 충돌 |
| 공지 / 배너 관리 | ✓(작성) | 조회 | 조회 | ✅ + 🔧 권한 축소 |
| 라이더 관리 | 조회 | 조회 | ✓(등록·관리) | ✅ |
| 조직 관리 | ✓(생성) | — | — | ✅ 고도화(2026-07-23): KPI·검색/필터·트리·대표역할·상세모달·코드자동생성·설정 시딩 |
| 대표/서브계정 관리 | ✓ | 자기조직 | 자기조직 | 🆕 |
| 관리자·권한 / 코드마스터 / 감사로그 | ✓ | — | — | ✅ + 🆕 리스크 플래그 |
| 정산 엑셀 열기 암호 | 전역 | — | 대리점별 | ✅ |
| 정산/잔액 수동 조정 | ✓(본사 전용) | — | — | 🆕 |
| 일일정산(선정산) 지급 리스트 | — | — | 원클릭 지급 | 🆕 |

**라이더 앱** (별도 세션, 본인 것만 조회 — 소속 대리점+상위 broadcast 자동 적용)

| 화면 | 상태 |
|------|------|
| 홈 — 공지·배너 미리보기, 출금가능액 | ✅ DB, 단 ⚠️ **"이번 달 정산 합계" 카드는 하드코딩 목업**(`₩3,842,500` 고정) |
| 정산 목록 보기 | ✅ DB |
| 정산 수수료 내역 | ✅ DB(`SettlementLedger::listForRider`) |
| ⚠️ **정산 달력 보기** | **완전 목업** — 파일 내 하드코딩 배열, DB 미연동. 🆕 출금 신청 UI로 컨셉 확장한 프로토타입 제작됨(`mockups/rider_withdrawal_calendar_mockup.html`, 건당수수료 구간 시각화) — 관리자 작업 우선이라 목업 단계 유지, 실개발은 라이더 착수 시 |
| ⚠️ **정산 상세("샘플")** | **완전 목업** — 정적 HTML, DB 쿼리 없음. 메뉴명에도 "(샘플)" 표기됨 |
| 출금 신청 / 신청 내역 | ✅ DB (단, §5.4 정정에 따라 선정산 대상은 향후 "일일정산 리스트" 방식으로 대체 예정) |
| 공지 목록·상세 | ✅ DB |
| 프로필 / 계좌정보 / 비밀번호 변경 | ✅ DB |

---

## 4. 교차 규칙

### 4.1 인증·RBAC

- 로그인 `admins` 테이블, 세션에 `admin_org_id` 포함(구세션은 DB에서 1회 보충). 브루트포스 10분 5회 잠금.
- 역할(role): `super`(전체, 항상 전권) · `manager`(총괄 — 시스템관리 제외 소속 조직 전체 화면 조회·쓰기, 항상 전권. 2026-08-05 추가, 대리점/총판 담당자 1명이 그 조직 업무를 혼자 처리하는 경우용) · `operation` · `settlement` · `admin`(레이블은 "조회전용"이나 실제 권한은 아래 DB 테이블에서 결정).
- ✅ **역할별 화면 권한은 `role_permissions` 테이블(role×area→can_view/can_write)로 DB화**(2026-08-05). 화면은 「권한 관리」(`system/permissions`, super 전용). area 종류: `dashboard`/`settlement`/`deduction`/`promotion`/`withdrawal`/`content`/`riders`. `admin_can_access_route()`가 라우트 접두사를 area로 매핑해 조회권을 확인, `admin_can_write()`가 쓰기권을 확인 — `inc/RolePermission.php` 참고.
- **`system/*`(시스템관리)는 이 테이블과 무관하게 코드에서 super 전용으로 고정**(요청에 따른 설계) — `system/orgs`(본사 조직관리)·`system/team`(대표계정 자기관리)만 role이 아닌 조직레벨 기반 특례 유지.
- "공지는 본사만"은 role 체크만으로는 강제 안 돼 `admin_can_write('content')`에 조직레벨(admin_org_level()===HQ) 체크를 별도로 추가해 둠(권한표의 content 쓰기값과 AND 조건).
- 시드 계정: `admin`/super@본사, `operation01`/operation@총판, `settlement01`/settlement@대리점 (`Admin1234!`).

### 4.2 시스템 코드 (`system_codes`)

`bank`·`vehicle`·`rider_status`·`settlement_status`·`withdrawal_status`·`platform`·`deduction_kind`. code는 카테고리 내 유일, 생성 후 불변. 참조 중이면 삭제 대신 비활성화.

⚠️ **상태값 이중 정의 위험** — `withdrawal_status`에 `failed` 추가 시 PHP `Withdrawal::STATUS_LABELS`와 `system_codes` **양쪽 다** 갱신 필요(이 문서 §9 체크리스트 참고).

### 4.3 감사 로그 (`audit_logs`)

`AuditLog::record('도메인.동작', ...)`, 테이블 없으면 조용히 skip. 🆕 리스크성 action(역할변경·대량상태변경·고액출금승인 등) 태깅 → 대시보드 알림 연동 예정.

---

## 5. 도메인별 로직

### 5.1 공지·배너 (`Notice`/`Banner`)

🔧 **작성은 본사만.** 대리점·총판은 조회만(broadcast 수신). 라이더는 소속 대리점+상위 조직 broadcast만 조회. 슬롯(`home_top`/`home_middle`/`rider_app`), 라이더 홈 캐러셀은 `rider_app`만.

> ⚠️ `content_notices/banners.org_id`가 본사만 작성이면 항상 본사 id로 고정되어 사실상 무의미해짐 — 유지 여부 재검토 여지.

### 5.2 라이더 (`riders`)

상태 `active`/`suspended`/`leave_request`/`offboarded`. 등록·수정은 대리점, 조회는 상위 조직도 가능. 겸업 시 대리점마다 별도 로그인(스키마 변경 불필요, 1라이더-1대리점 FK 유지). 상태변경 통보 불필요.

✅ **대리점 이관은 별도 절차가 아님** — 새 대리점에서 완전 신규 라이더로 등록. 이름·전화번호·플랫폼 라이선스ID(`rider_platforms.external_id`) 중복 허용, `riders`는 `rider_code`·`login_id`만 UNIQUE라 스키마상 이미 지원됨(개발 불필요).

✅ **[2026-07-23] 플랫폼 매칭키 매핑** — 정산서 매칭키를 `rider_platforms.external_id`에 등록. **쿠팡=성함**(정산서 표기 그대로, 예: `박성준1682`), **배민=UserID**(예: `adammins`). ① 라이더 상세 "플랫폼 아이디 연동" 카드(`set_platform`), ② 라이더 등록 폼(`riders.php` `coupang_id`/`baemin_id`), ③ 정산 미리보기 미매칭 시 **"기존 라이더에 연결"** 모드(`settlement_register_rider.php` `action=link`).
⚠️ **매칭키는 전역 유일이 아님** — 라이더는 대리점 이동 시 별도 등록(앱 로그인ID 새 발급, `riders.login_id` 전역 UNIQUE라 자동 강제)되므로 같은 성함/UserID가 **여러 대리점에 중복 존재 가능**. 따라서 **정산 매칭·중복검사는 반드시 `agency_id`와 함께**(모든 매칭·dup 쿼리에 `AND r.agency_id = ?`). 매칭 순서: 매칭키(성함/UserID) → 이름 폴백, 모두 대리점 스코프. 실 파일 검증: 쿠팡 `박성준1682`·배민 `adammins` 정상 매칭.

✅ **[2026-08-09] 라이더 엑셀 일괄등록** — 신규 대리점 입점 시 기존에 관리하던 라이더를 하나씩 입력하지 않고 엑셀 한 번에 옮겨 적기 위한 기능. 단건 등록(`riders.php`)의 검증·생성 규칙을 `inc/RiderRegistration.php`로 공용화(`validate()`는 DB 쓰기 없이 검증만, `create()`는 `validate()` 후 실제 INSERT) — 단건·대량 두 경로가 항상 같은 규칙을 쓰도록. 화면: `admin/views/riders_list.php` "엑셀 일괄등록" 모달 → ① `riders_bulk_template.php`로 빈 양식(이름·휴대전화만 필수, 나머지 컬럼 안내 시트 포함) 다운로드 → ② `riders_bulk_upload.php`에 업로드, `mode=preview`로 행별 검증 결과만 확인(저장 안 함, `RiderRegistration::validate()`만 호출) → ③ `mode=confirm`으로 확정, 오류 없는 행만 실제 등록(행별 독립 실행이라 일부 실패해도 나머지는 등록됨). 정산 엑셀 업로드의 2단계(미리보기→확정) UX와 프로모션 업로드의 헤더-이름 매핑 파싱 패턴을 그대로 재사용. 검증 규칙은 단건 등록과 동일: 이름/휴대전화 필수, 로그인ID·라이더코드 비우면 자동생성, 초기 비밀번호 0000, 쿠팡ID/배민ID는 같은 대리점 내에서만 유일. ⚠️ 주의: `RiderRegistration::create()`가 내부에서 자체 `db_transaction()`을 여므로, 미리보기에서 "실제로 저장했다가 롤백"하는 방식을 쓰면 PDO가 트랜잭션 중첩을 지원하지 않아 곧바로 에러가 난다 — 그래서 검증(`validate()`)과 쓰기(`create()`)를 완전히 분리했다.

✅ **[2026-07-25] 앱 로그인 ID 자동생성** — 기존엔 관리자가 등록마다 손으로 입력해야 해서 의미 없는 임의 숫자가 잦았음(라이더 이름·쿠팡/배민 아이디는 대리점 간 중복 허용이라 그대로 못 씀). `inc/RiderLoginId.php` 신규: **휴대전화번호(숫자만)를 기본값으로 쓰고, 충돌하면 소문자 a, b, c…를 순차로 접미사에 붙여 유일화**(`01012345678` → 충돌 시 `01012345678a` → 또 충돌하면 `b`, `c`…, z를 다 채우면 `aa`로 자연 확장). 전화번호가 없으면(정산서 미매칭 빠른등록 등) 랜덤 폴백. 정식 등록(`riders.php`)·빠른등록(`settlement_register_rider.php`) 양쪽 다 로그인 ID 필드를 **선택 입력**으로 바꿔 비우면 자동생성(직접 입력도 계속 가능). 빠른등록의 기존 `suggestLogin()`(라이선스ID를 그대로 로그인ID에 넣던 로직 — 매칭키 중복 허용 원칙과 충돌해 위험했음)은 제거. 실 API 검증: 정식등록 `010-7777-1111`→`01077771111`, 전화번호 없는 빠른등록→랜덤 폴백 형식, 겸업 시나리오(같은 번호·다른 대리점)→두 번째 등록이 `a` 접미사, 순수 로직 8/8(충돌·재충돌·연쇄충돌·z→aa 확장·폴백).

### 5.3 정산 업로드·반영

```
엑셀 업로드(mode=preview: 파싱+매칭만) → 미리보기 모달
  → 미매칭 라이더 즉시등록(license 연동) → 확정 업로드(저장)
  → settlement_uploads / settlement_daily_riders(종합탭, 라이더·일자 요약)
                        / settlement_order_details(오더별 상세내역, 주문 단위 원본)
                        / settlement_hourly_insurance(시간제보험, 라이더·일자별 내역)
                        / settlement_support_amounts(🆕 지원금/추가지원금, 지급액에 가산)
                        / settlement_weekly_deductions(차감내역, 라이더 매칭)
  → 확정 직후 **업로드 상세 화면으로 자동 이동**(매칭 확인·정산 반영을 이어서)
정산 반영 (SettlementLedger::applyUpload)
  → settlement_rider_cycles + settlement_fee_items → RiderWallet::credit()
  → 🆕 일일정산 라이더는 이어서 자동출금(DailyAutoWithdrawal::runForRiders)
```

✅ **[2026-08-09] 일일정산 라이더 자동출금** — 일일정산(선정산, `riders.is_daily_settlement=1`) 라이더는 그날 정산분을 그날 받는 게 원칙인데, 기존엔 관리자가 「정산 반영」 후 출금 화면에 또 들어가 승인해야 했다. 이제 `settlement_apply.php`가 반영 성공 직후 `DailyAutoWithdrawal::runForRiders($result['applied_rider_ids'])`를 호출해 **신청(`Withdrawal::applyForRider`) → 즉시 이체(`Withdrawal::executeTransfers`)** 까지 이어서 처리한다. 주정산 라이더는 대상이 아니며 기존대로 라이더가 직접 신청한다.
- **보증금은 별도 처리 불필요** — `RiderWallet::previewWithdrawal()`이 이미 `잔액 − 보증금(reserve_amount, 기본 5만원)`만 출금 대상으로 잡으므로, 보증금이 안 찬 라이더는 자동으로 "출금 가능액 0원"이 되어 건너뛰어진다.
- **건너뜀은 정상 동작** — 계좌 미등록·출금보류·보증금 미달·진행 중 신청 존재는 예외를 위로 던지지 않고 사유만 모아 화면에 표시한다(정산 반영 자체는 이미 성공했으므로 되돌리면 안 된다).
- **이체 실패해도 정산 반영은 유지** — 실패 건은 `status='failed'`로 남아 출금 화면에서 재시도할 수 있고, 그 정산 사이클은 계속 점유되어 이중 출금이 막힌다.
- 🐛 **[2026-08-09 실사용 버그 수정] 대상 선정 기준 = "미출금 정산분이 남았는지"** (❌ "이번에 새로 반영됐는지" 아님). 처음엔 `applyUpload()`가 새로 만든 사이클의 라이더만 대상으로 삼았는데, **계좌 미등록으로 건너뛴 라이더의 계좌를 채우고 「정산 재반영」을 눌러도 그 라이더는 이미 사이클이 있어 반영 단계에서 skip → 자동출금 대상 목록에서 빠져 영영 재시도되지 않았다**(실제로 서원빈 건에서 발생: 재반영 0건 + 출금요청 0건, 화면엔 아무 안내도 없음). 그래서 `DailyAutoWithdrawal::runForUpload($uploadId)`가 **업로드에 매칭된 일일정산 라이더 중 `WithdrawalCycles::unwithdrawn()`이 비어있지 않은 사람**을 대상으로 잡도록 바꿨다. 중복 출금은 이미 받아간 사이클이 `unwithdrawn()`에서 빠지고 진행 중 신청은 `applyForRider()`가 막아 구조적으로 방지된다 — 즉 **대상을 넓혀도 금액 로직은 그대로 안전**하다.
- **반영 0건이어도 자동출금 결과를 항상 표시** — 재반영은 원래 0건이 정상이라, 예전엔 "0건 재반영"만 뜨고 자동출금이 시도됐는지조차 알 수 없었다. 이제 대상·지급·미지급 인원과 라이더별 사유를 함께 보여준다.
- **건너뜀 사유에 숫자를 붙인다** — "출금 가능 금액이 없습니다"만으로는 조치할 수 없어, 보증금 미달이면 `잔액/보증금/부족액`을, 건 단위 경계에 막힌 거면 `얼마가 더 쌓여야 하는지`를 계산해 보여준다(`explainSkip()`).
- ⚠️ 호출은 반드시 `applyUpload()`의 **트랜잭션 커밋 후**, 그리고 **바깥 트랜잭션 없이**(실제 이체라 건별 독립 커밋 필요 + PDO는 중첩 트랜잭션 미지원).
- 실검증: 하루 3만원×5일 시뮬레이션에서 1~2일차는 보증금 적립으로 건너뛰고 3일차부터 매일 29,416원(지급 29,216 + 수수료 200) 출금, 잔액은 계속 58,832원(≥보증금 5만원) 유지 — 보증금 침범 없음, 사이클 중복 출금 없음. 계좌 미등록·주정산 라이더는 출금 신청 0건. 이체 실패 시 지갑 미차감·failed 상태 유지·사이클 점유 유지 확인.

| 항목 | 내용 |
|------|------|
| 지원 플랫폼 | ✅ 쿠팡이츠 일간 + **배달의민족(2026-07-23 완료)** |
| ✅ **[2026-07-23 완료]** 배민 파서 | 실 파일(`배달처리비_표준서울강서C오엑스플러스_...`) 검증. 배민은 **시트 1개(배달 내역 상세)·주문 단위 원본만** 제공(종합/차감/보험 탭 없음) + **파일 하나가 여러 운행일 포함**. `XlsxParser::parseBaeminOrders()`로 주문 파싱 → `settlement_baemin_normalize()`가 **라이더·운행일별 집계**해 쿠팡과 동일한 `settlement_daily_riders`(운행일별 행)·`settlement_order_details`에 저장. `settlement_daily_riders` UNIQUE를 `(upload_id, license_id, settlement_date)`로 확장(한 업로드 다중 날짜 허용). **라이더 매칭키=UserID(K)**(대리점 스코프, 2026-07-23 확정 — 라이더ID(J) 아님). 배달처리비(AH)=payout(=기본단가+각종할증 합, 검증됨). 정산 반영은 기존 선정산/주정산 로직 그대로(선정산 대상=정산내역 생성, 나머지=지갑 적립). 플랫폼 자동감지에 배민 마커 추가. 검증: 3주문→2라이더·운행일 요약, 합계 12,460 원본 일치 |
| 중복 검사 | 귀속일+플랫폼 / 파일명 / SHA256, 대리점 범위 내 판정 |
| 플랫폼 자동감지·불일치 차단 | 신뢰도 medium 이상 불일치 시 업로드 거부(강제 확인 가능) |
| ✅ **[2026-07-23 완료]** 오더별 상세내역 row 저장 | 실제 쿠팡 정산서(`오엑스플러스_서울_강서남부_20260628.xlsx`)로 시트 구조 확인 후 신규 `settlement_order_details` 테이블에 **주문 1건=1행**으로 저장(성함/주문번호/스토어/픽업·배달지역/배정·수락·배달 실시각/거리/할증세부/정산금액). 검증: 328건 합계가 종합탭 총정산금액(1,390,241원)과 **정확히 일치**. 배달시각이 실제 타임스탬프로 남아 향후 §7 #18(정산수수료 age-bucket) 계산의 데이터 소스가 됨 |
| ✅ **[2026-07-23 완료]** 시간제보험 row 저장 + TODO 해소 | 시간제보험 탭(발생일자·성함·금액)을 `settlement_hourly_insurance`에 라이더·일자별 저장, 동시에 `settlement_daily_riders.hourly_insurance`(§7 #6 TODO였던 컬럼)를 실제 파싱값으로 채움. 검증: 파일의 종합탭 AH합계(-5687/-1907)와 정확히 일치(부호만 양수로 정규화) |
| 🐛→✅ **[2026-07-23 버그 발견·수정]** 차감내역(`settlement_weekly_deductions`) | 실 파일로 헤더를 대조한 결과, 기존 파서가 **D열부터 한 칸씩 밀려 읽고 있어 실제 차감액(J열, 금액)이 아니라 배달비(I열)를 저장하던 버그**를 발견·수정(헤더 키워드 기반 매핑으로 재작성: 주문일자/축약형ID/성함/구분/스토어명/배정시간/메뉴가/배달비/금액). 라이더 매칭(성함 기반, 종합탭과 동일 파일 내 매칭 우선)도 이번에 추가 — 이전엔 rider_id가 항상 NULL이었음. **차감정보 등록** 기능 신규(`admin/api/deduction_register.php`, `settlement/upload-detail` 화면에 목록+등록/등록취소 버튼) — 매칭된 행만 `deduction_entries`로 등록 가능, 등록 시 정산 반영에서 자동 차감됨 |
| 🔧 고용·산재보험(0.8%·0.88%) | ✅ **확정(2026-07-15)** — **정산 반영 시점에 라이더 몫에서 공제하고 라이더 지갑에 적립하면 끝.** 대리점이 국세청/공단에 별도 납부할 필요 없음 — **쿠팡이 이미 이 부분을 처리해서 대리점에 비용 지급**(대리점 입장에선 예수금 아니라 그냥 라이더 차감액). 컬럼만 `employment_ins_pct`(0.8%)/`industrial_accident_ins_pct`(0.88%) 분리하면 됨, 납부상태 추적 불필요 |
| 🆕 원천세 라이더별 설정 | ✅ **확정(2026-07-15~16)** — 원천세는 고용·산재와 다름, **실제 예수금**(대리점이 신고·납부 책임). **적용 여부를 대리점이 라이더 상세화면에서 체크박스로 설정**(`riders`에 플래그 신규 필요) — 대상자만 원천세 공제. **세율(3.3%)은 고정**(대리점별 조정 불가). **대리점이 신고할 때 볼 수 있는 원천세 대상자 명세 조회 화면** 신규 필요 |
| 🆕 선지급(advance) 입력 | 대여금 성격 차감, `deduction_entries`(kind=advance) 스키마는 있으나 입력화면 없음. **"선정산수수료"와 다른 항목** |
| ✅ 선정산수수료(=대행수수료) 계산시점 | **확정(2026-07-15) — 대리점이 임의로 고르는 게 아니라 `riders.is_daily_settlement`에 자동 연동.** 일일지급 라이더 = "선정산"(정산반영 시 즉시 계산) / 그 외(주정산) 라이더 = "후정산"(출금신청 시점에 계산). 즉 `fee_calc_timing`은 별도 대리점 설정값이 아니라 **라이더 플래그에서 파생되는 값** — 신규 컬럼 자체가 불필요해질 수 있음(§7 재검토). 후정산 계산 시 **정산수수료(본사·건당)+선정산수수료(대리점)가 동시 부과**(기존 확정 유지) |
| ✅ 정산 반영 후 정정 | **확정(2026-07-14 미팅) — 자동 롤백/PG 환불 API 없음.** 관리자 화면에 **본사 전용 수동 조정 기능**(잔액·정산금 직접 수정)을 신규로 만들어 대응. 상세는 §5.4a |

✅ **용어 혼동 해소(2026-07-15)** — `riders.is_daily_settlement`와 `fee_calc_timing`은 **연동됨**(위 표 참고). §8-A 미결 #1·#2 모두 해소.

⚠️ **남은 용어 혼동 위험** — "선정산수수료(=대행수수료, `AgencyFeeConfig`, 적립일수 기준, 대리점 몫)"와 §5.4의 "**영업대행수수료**"(PG 결제 시 발생, 대리점·총판·본사 분배)는 이름이 비슷하지만 **완전히 다른 별개 수수료**. 코드 작성 시 변수/컬럼명을 명확히 구분할 것(예: `agency_fee` vs `pg_service_fee`).

### 5.4 출금 — PG 카드결제 → 오픈뱅킹 이체 🔧 전면 재설계

> 신한 이체파일 다운로드 방식 폐기. 2단계: **PG 카드결제(조달)** → **오픈뱅킹 이체(지급)**.

**1단계 — PG 카드결제 (신규)**

- 트리거: **정산 반영(수수료 계산 완료) 시점.**
- 🔧 **[2026-07-15 정정] 결제 단위 — 총액 일괄이 아니라 "라이더별 건건히".** 그 날 정산 반영된 **라이더 한 명당 하나씩** PG 결제를 실행(N명이면 N번 결제) — 총액을 한 번에 결제하는 게 아님. 대상은 선정산·주정산 구분 없이 전부(금액 산정 기준은 유지).
  - 장점: 카드 한도·오류로 결제가 막혀도 **그 라이더 건만 실패**, 나머지는 정상 진행 — 지난 미팅에서 우려했던 "카드 한도 초과 시 전체 지급 마비" 리스크가 구조적으로 완화됨.
- 대리점 등록카드로 라이더별 `해당 라이더 net 금액 + 영업대행수수료` 결제.
- ✅ **명칭 확정 — "영업대행수수료"**: PG 결제로 대리점 잔액을 발생시킬 때 붙는 수수료(구 "결제수수료"). 대리점·총판·본사가 **계약별로 분배**(고정비율 아님). 🔧 **초기 분배값(2026-07-16)**: 대리점·총판·본사 **각 1%씩 임시 세팅** — 확정값 아님, 추후 재조정 예정(§7 반영).
- PG사는 **본사가 결정, 플랫폼 전체 단일사**.
- 🆕 **[2026-07-15] 대리점 카드 다건 등록 + 자동 대체결제.** 대리점은 카드를 **여러 개 등록** 가능. 결제 시 우선순위 1번 카드로 시도 → **한도초과 등 실패 응답 시 다음 카드로 자동 순차 재시도**, 성공할 때까지 진행. (전 카드 실패 시에만 최종 실패 처리)
- 성공(라이더 건별) → `agency_wallets.balance`(신규, 대리점 잔액)에 해당 금액 충전 — 실제 은행계좌 입금 여부와 무관한 시스템 내부 잔액.
- 최종 실패(전 카드 실패) → **알림 + 실패이력(사유) + 수동 재시도** (확정) — 해당 라이더 건만 영향, 다른 라이더는 정상 진행.

**2단계 — 지급 (오픈뱅킹, 신규) — 정산 주기별로 두 갈래**

> ✅ **[2026-07-16 확정] 1단계(PG결제)와 2단계(지급) 시점이 완전히 분리됨.** PG결제는 "대리점 잔액 충전"까지만 담당하고, 그걸로 라이더에게 자동으로 돈이 나가지 않음. **"일일정산" 지급은 PG결제와 동시에 일어나는 게 아니라, PG결제가 다 끝난 뒤 완전히 별도로(별도 시점·별도 화면) 진행**되며, 이 지급도 자동이 아니라 **수동**(관리자가 리스트 확인 후 실행).

- 대리점 명의 계좌를 오픈뱅킹에 등록(핀테크이용번호). 본사가 오픈뱅킹 이용기관 계약 주체.
- ✅ **지급 방식 확정(2026-07-14~16):**
  - **선정산(`riders.is_daily_settlement=1`) 대상:** 라이더가 개별 신청하지 않음. **관리자(대리점)가 "일일정산 지급 리스트" 화면에서 그날 확정된 대상자를 확인하고 원클릭으로 지급**(완전자동 아님 — PG결제와 별도 타이밍에, 지급 전 사람이 한 번 확인하는 절차를 의도적으로 유지). 🆕 신규 화면.
  - **주정산(그 외) 대상:** 기존과 동일 — **라이더 전액출금 신청 → 대리점 수기 승인** → API 호출.
- 실패 → **알림 + 실패이력(사유) + 수동 재시도** (확정) — **일부 라이더만 실패해도 나머지는 정상 지급**, 실패건만 리스트에 남아 재처리(2026-07-16 확인).
- 🔧 **[정정 2026-07-24] 탈퇴/정지 라이더 잔여 처리 — "0원 상각" → "실제 지급 후 종결".** 기존 `zeroClose`는 잔여 잔액(보증금 포함)을 0원 이체로 **상각해 소멸**시켰으나, 보증금은 "만약을 위해 남겨두는 예치금"이므로 정상 종결 시 라이더에게 **실제로 지급**해야 함(사용자 확정). `DailyPayout::closeOut()`이 `payRider`와 동일한 자금 흐름(대리점 잔액 확인 → 이체 → 대리점 잔액 차감 + 라이더 지갑 0)을 따른다. **종결 지급에는 정산수수료를 부과하지 않음**(예치금 반환 성격). 잔액 0이면 이체 없이 종결. 잔액이 있는데 **계좌가 없거나 대리점 잔액이 부족하면 거부**(돈을 말없이 없애지 않음). `zeroClose()`는 별칭으로 유지.

🔧 **[정정] 건당수수료 계산방식 — 기존 `fee_per_tx_short/long` 단일값 모델에서 변경, 명칭도 확정.** 2026-07-13 라이더 앱 "출금 신청 정산 달력" 목업 논의 + 2026-07-14 미팅으로 확정:
- ✅ **명칭 확정 — "정산수수료"**(구 "이체수수료"). 라이더가 정산(출금)할 때 건별로 붙는 수수료 — **본사 몫**.
- **기준은 "마지막 출금 이후 경과일"이 아니라 "오늘로부터 그 주문이 며칠 전인지"** — 플랫폼(쿠팡·배민)이 대리점에 **일주일 단위로 정산**하기 때문에, 대리점이 아직 못 받은 최근 7일 이내 주문을 라이더가 미리 받아가면 수수료가 더 붙고(80원), 7일이 지나 대리점이 이미 정산받았을 주문은 수수료가 낮음(40원).
- **단위는 출금 1건이 아니라 "배달 주문 1건당"** — 하나의 출금 안에 최근분(80원)과 오래된분(40원)이 **섞여서 각각 합산**됨. 기존처럼 `accrued_days` 하나로 전체 출금에 단일 수수료를 매기는 방식이 아님.
- 계산: `총 정산수수료 = (7일 이내 주문 건수 × 80원) + (7일 지난 주문 건수 × 40원)`, `실지급 = balance − reserve_amount − 총 정산수수료`.
- 화면 표기: "40원 구간 N건=₩x / 80원 구간 M건=₩y / 총 수수료 ₩z / 예상 출금액 ₩w".
- **현재는 라이더 앱 목업(프로토타입, `mockups/rider_withdrawal_calendar_mockup.html`)에만 반영, `Withdrawal`/`WithdrawalConfig` 실제 도메인 로직은 아직 구 모델(단일 accrued_days 기준) 그대로** — 라이더 개발 착수 시 `Withdrawal::applyForRider()`·`RiderWallet::previewWithdrawal()` 재작성 필요(§7).
- ✅ **[2026-07-16 재정정] 기준일수·금액 전부 대리점별 설정 가능.** 지난 결정("본사 전역값만")을 뒤집음 — **기준일수(7일)·미만금액(80원)·이상금액(40원) 셋 다 대리점이 관리**. 원래 `withdrawal_config.org_id` 구조가 이미 대리점별 override를 지원하므로 **오히려 개발 작업이 줄어듦**(전역강제 로직만 없으면 됨). 금액의 **방향은 변경 없음**(최근=비쌈/80원, 오래됨=쌈/40원) — 대리점이 조정할 수 있는 건 그 "며칠"과 "얼마"의 **값**이지, 방향 자체는 아님.
- ℹ️ **수취인은 그대로 본사.** 대리점이 기준·금액을 설정하지만, 이 수수료 자체의 귀속(누가 가져가는 돈인지)은 여전히 본사 몫으로 이해했습니다 — 다르면 알려주세요.
- ✅ **신규 대리점 기본값(2026-07-16 확정, 2026-07-23 구현)**: 대리점 생성 시 **기준일수 7일 · 미만금액 80원 · 이상금액 40원**으로 자동 세팅. `Organization::create()`가 대리점 생성 시 `withdrawal_config`(7/80/40)·`agency_wallets`(0)·`org_fee_config`(1%) 행을 함께 시딩(총판은 org_fee_config만).

#### 5.4a 정산/잔액 수동 조정 (신규, 본사 전용) — 정산 정정 프로세스의 실체

> ✅ **2026-07-14 미팅으로 확정.** PG 자동결제·오픈뱅킹 자동이체가 도입되면서 §8-A #1(정산 반영 후 정정)이 미결이었으나, **자동 롤백·PG 환불 API 연동 없이 "관리자가 수동으로 고친다"** 방향으로 확정.

- 🆕 **신규 화면(본사=super 전용)**: 라이더 지갑(`rider_wallets.balance`)·대리점 잔액(`agency_wallets.balance`)·정산 사이클(`settlement_rider_cycles`) 등을 **직접 조정**할 수 있는 관리 화면. 조정 시 사유 입력 필수 + `AuditLog` 기록(`before_value`/`after_value` 모두 채움 — 기존 §4.3 "before_value 미사용" 관행에서 이 화면만 예외).
- **PG 카드결제·오픈뱅킹 이체 자체를 취소·환불하는 자동 연동은 만들지 않음** — 실제 돈은 그대로 두고, 시스템상 잔액/기록만 관리자가 바로잡는 방식. (필요 시 실제 환불은 PG/은행 콘솔에서 수동 처리, 시스템은 그 결과만 반영)
- 🆕 **일반 원칙(2026-07-14 확정)**: "대부분의 수정·변경은 본사가 관리자 화면에서 직접 처리할 수 있어야 한다" — 이번 재설계로 늘어나는 자동화(PG결제·오픈뱅킹·일일정산 원클릭 등) 때문에 **문제 발생 시 DB를 직접 만지지 않고도 본사가 화면에서 되돌리거나 고칠 수 있는 경로**를 각 신규 기능에 함께 설계할 것. §7 신규 항목으로 반영.

### 5.5 대리점 자체 정산금 인출 🆕 신규 도메인

```
인출가능액 = agency_wallets.balance − 라이더채무(rider_wallets 합계) − 원천세 예수금(원천세 대상 라이더분 누적)
```

🔧 **[2026-07-15 정정] 세금예수금 범위 축소 — 원천세만 해당.** 고용보험(0.8%)·산재보험(0.88%)은 **예수금이 아님** — 쿠팡이 이미 처리해서 대리점에 비용 지급하므로 대리점이 별도 납부할 게 없음(§5.3). **원천세만 대리점이 실제로 신고·납부하는 진짜 예수금**이라 인출가능액 계산에서 제외해야 함. "납부상태 추적"은 불필요(대리점이 알아서 신고) — 대신 **원천세 대상 라이더 명세를 조회할 수 있는 리포트**만 있으면 됨(§5.3, §7).

라이더 이체와 동일한 대리점 계좌 사용.

✅ **승인 절차 없음(2026-07-16 확정)** — 인출가능액이 이미 예수금(원천세)·라이더채무를 제외한 "순수 대리점 몫"이므로, **대리점이 신청 즉시 실행**되면 됨(본사 승인 불필요). `withdrawal_requests`(또는 신규 테이블)의 `agency_payout` 상태 흐름도 승인 대기 단계 없이 신청=완료로 단순화 가능.

> ⚠️ **스키마 충돌 (가장 시급)** — `withdrawal_requests.rider_id`가 `NOT NULL`(FK)인데 대리점 인출(`kind=agency_payout`)은 라이더가 없음. **`rider_id`를 nullable로 바꾸고 `agency_id` 추가**하거나 **별도 테이블**(`agency_payout_requests`)이 필요.

### 5.6 관리자 계정 / 대시보드

- super만 전체 계정 CRUD, 🆕 조직 대표계정은 자기 org 범위 내 서브계정 CRUD 가능해야 함(§2).
- **대시보드는 조직 레벨에 따라 두 종류**(2026-08-05):
  - **대리점** (`AdminDashboard` + `admin/views/dashboard.php`) — *라이더 운영* 관점. **차트 3종**(정산 추이 area · 플랫폼 비중 donut · 라이더별 정산액 순위 horizontal bar), KPI 4종(활성라이더·출금대기·당월차감·게시공지), 최근 처리 현황 타임라인, 업로드 이력.
  - **본사·총판** (`OrgDashboard` + `admin/views/dashboard_org.php`) — *대리점 관리* 관점. **차트 3종**(정산 추이 area · 플랫폼 비중 donut · 대리점별 정산액 순위 horizontal bar, ApexCharts), KPI 4종, 대리점별 실적 비교표(라이더·건수·정산액·지갑잔액·출금대기·미수금·최근 정산일), 총판별 롤업(본사만), 손이 필요한 대리점 감지, 플랫폼 수수료 내 몫, 리스크 알림(본사만) + 큰 금액 출금.
  - ⚠️ **대시보드 금액 지표는 전부 `settlement_daily_riders.payout_amount`(플랫폼 지급액) 기준으로 통일**한다. `settlement_rider_cycles.net_amount`(수수료·공제 후 지갑 적립액)와 섞으면 같은 화면에서 숫자가 어긋난다(2026-08-08 실제 발생, 39만원 차이).
  - 분기는 `dashboard.php` 최상단에서 `admin_org_level()`로 수행하며 라우트는 `dashboard` 하나를 공유한다.

### 5.7 라이더 부채 원장 — 대여금·리스·선지급 🆕(2026-07-24)

> 근거: 실제 라이더에게 나가는 **정산명세서 PDF**(`도꺠비정산서/기사별_PDF명세서`) 분석. 명세서엔 대여금/리스/선지급의 **잔액 이월·일납 상환** 명세가 있는데 우리 시스템엔 일회성 `deduction_entries`만 있어 잔액 추적이 불가능했음.

- **테이블**: `rider_debts`(원장 헤더) + `rider_debt_entries`(차감 이력). `inc/RiderDebt.php`, `admin/api/debt_action.php`.
- **화면 2곳**: ① **차감 › 대여금·리스 원장**(`deduction/debts`, `debt_list.php`) — 전체 라이더 부채를 한 화면에서 등록·차감·수정, KPI(진행건수·총 미상환잔액·리스 일납합계·완납건수)+필터(종류/상태/검색), 라이더 검색 picker로 등록. ② **라이더 상세 "부채" 카드** — 그 라이더의 부채·차감 이력 조회 및 이력 취소.
- **종류**: `loan`(대여금)·`advance`(선지급)=**상각형**(원금→잔액이 줄어 0이면 자동 완납) / `lease`(리스·렌탈)=**반복 부과**(일납×일수를 매 정산마다 부과, 잔액 개념 없음).
- **차감 규칙**(실 PDF로 검증): 차감액 = 입력값 또는 **일납금액 × 차감일수**. 상각형은 잔액을 초과할 수 없음(자동 캡). 예: 리스 일납 24,000 × 7일 = 168,000, 대여금 잔액 1,250,000 → 50,000 차감 → 1,200,000.
- **정산 연동(중요)**: 차감 실행 시 **`deduction_entries`(kind=loan/lease/advance) 행을 생성**만 한다 → 기존 `SettlementLedger::buildFeeItems`가 `applied_date` 기준으로 그대로 차감(중복 로직 없음, 갑의 "본사가 화면에서 처리" 원칙과도 일치). 이력 취소 시 연결된 `deduction_entries`도 삭제되고 상각형 잔액 복구.
- ✅ **[2026-07-30] 리스 자동 일수계산 + 재실행 멱등성** — 실제 운영 파서(`parser.py`) 확인 결과, 리스는 관리자가 일수를 수동 입력하는 게 아니라 **계약기간(`opened_on`~`planned_end_on`)과 정산기간이 겹치는 일수를 자동 계산**해서 차감한다. `RiderDebt::applyLeaseForPeriod($debtId, $periodStart, $periodEnd)` 신규 — 겹치는 구간을 계산해 `applyRepayment()`를 호출(중도 출고/종료 비례배분 자동 처리). `SettlementLedger::applyUpload()`가 업로드의 정산기간(min~max settlement_date)을 구해 매칭된 라이더의 활성 리스마다 **1회씩** 자동 호출(`applyActiveLeasesForUpload`, 일자별 사이클 순회와는 별개). **재실행 멱등성**: `rider_debt_entries` UNIQUE(debt_id, applied_date)로 같은 정산을 재반영해도 이중 차감되지 않음(DB 제약 위반 시 조용히 skip). 계약기간(`planned_end_on`) 미설정 리스는 자동계산 불가 → 기존 수동 차감(`applyRepayment`)으로 처리. 검증 11/11 + `SettlementLedger` 배선 검증(7일 겹침 → 105,000원 정확).
- ✅ **[2026-07-30] 지원금/추가지원금 파싱** — §5.3 참고. 신규 `settlement_support_amounts` + `XlsxParser::parseSupportSheet()`/`parseAddSupportSheet()`.
- **미해결(정산명세서 대비 남은 격차)**: [[settlement-statement-gaps]] — 차명(借名)계좌·소득신고 명의자·주민번호, 타임버킷/참여인정구간 집계(프로모션 자동계산의 자격판정 기준), 프로모션 자동계산, 보험료소급, 고용·산재보험을 쿠팡 파일값 그대로 쓰지 않고 재계산 중인 것, **정산수수료 구조 자체가 실 운영(정산유형+주간물량 기준)과 다름**(현재 age-bucket, 갑 확인 대기), **라이더용 통합 명세서 발급** 기능은 아직 미구현.

---

## 6. 마이그레이션·시드

`php migrate.php`(`inc/MigrateRunner.php`, 멱등) / `php seed.php`(조직트리+시드계정). 재설계분 신규 마이그레이션 필요: 대리점 은행계좌·카드·`agency_wallets`·`pg_payments`·`withdrawal_requests` 실패상태·이체수수료원가 컬럼. 운영 배포 후 `migrate.php`/`seed.php` 웹 접근 차단 권장.

---

## 7. 구현 작업 목록 (재설계분 스코프)

기존 인프라(라우팅·인증·멀티테넌시 스코핑·엑셀파싱)는 변경 없음. 아래가 이번 재설계로 실제 코드에 반영해야 할 전체 목록.

**🔴 시급 — 스키마 충돌**
1. ✅ **[2026-07-22 완료]** `withdrawal_requests.rider_id` nullable화 + `agency_id`·`fail_reason` 컬럼 추가, `kind` enum에 `agency_payout`, `status` enum에 `failed` 추가. FK(`fk_wr_rider`) 유지. (§5.5, MigrateRunner::migrateWithdrawalRedesign)

**🔧 기존 코드 수정**
2. ✅ **[2026-07-22 완료]** 조직 생성 권한: 총판 → 본사 전용 (`Organization::create`, `admin_can_manage_orgs` 본사 레벨만) (§2)
3. ✅ **[2026-07-22 완료]** 공지·배너 쓰기 권한: 본사 전용 + org레벨 가드 (`admin_can_write('content')`에 `admin_org_level()===admin` 추가) (§5.1, §4.1). ⚠️ **부수 발견→✅복구(2026-07-22)**: `admin/api/banners.php`가 빈 파일이었음(과거 "디비 복구" 커밋 때 유실) → `Banner` 도메인(save/delete/listAdmin) 기반으로 재구현. 검증: 저장→목록→삭제
4. ✅ **[2026-07-22 완료]** `deduction_global_config` 고용/산재 분리 + **계산 반영 완료**. `SettlementLedger::buildFeeItems`가 `employment_ins`(고용 0.8%)·`accident_ins`(산재 0.88%) **2개 항목으로 분리 공제**(기존 합산 9.12% 버그 교정 — 100만원 기준 91,200→16,800). (§5.3)
5. ✅ **확인 완료** ~~`withdrawal_config`: 이체수수료 전역 고정~~ 대리점별 설정 — `withdrawal_config.org_id` override 구조가 이미 존재, **추가 작업 불필요**(전역강제 제약도 없음). (§5.4)
6. ✅ **[2026-07-23 완료]** 시간제보험 — 실 파일로 파서 매핑 확정, `settlement_daily_riders.hourly_insurance` 자동 채움 + `settlement_hourly_insurance` 별도 내역 저장까지 완료(§5.3). §7 #6 종료
7. ✅ **[2026-08-08 완결]** `SettlementLedger::buildFeeItems`가 선정산수수료(대행)를 `riders.is_daily_settlement`로 분기 — **선정산(daily) 라이더만 반영 시점에 부과**, 주정산은 skip(별도 `fee_calc_timing` 컬럼 불필요 확인). **"주정산의 출금시점 대행수수료 부과(후정산)"는 계획 자체가 취소됨**(2026-08-08 갑: "출금 신청 시 건당 수수료만 적용하면 된다") — 주정산 라이더가 부담하는 건 출금 시 **건당 정산수수료뿐**이고, 대행수수료는 반영시점·출금시점 어디서도 부과하지 않는다. 현재 코드가 이미 이 동작이라 변경 없음(주석만 정정). (§5.3)

**🆕 신규 개발**
8. 🧩 **[2026-07-22 뼈대 완료]** PG 카드결제 — `PgGateway` 인터페이스 + `MockPgGateway`(팩토리 교체식), `AgencyCard`(다건 카드·우선순위·모의한도), `PgPayment::chargeForRider`(라이더별 건건히, **한도초과 시 다음 카드 자동 재시도**, 성공 시 지갑충전+영업대행수수료). `agency_cards`·`pg_payments` 테이블. 검증: 한도초과 대체결제·전카드실패. **실 PG사 스펙 도착 시 RealPgGateway만 추가.** (§5.4)
9. ✅ **[2026-07-22 지갑 인프라 완료]** `agency_wallets`(대리점 잔액) — `AgencyWallet` 클래스(get/withdrawable/credit/debit/setBalance/ledger + `agency_wallet_ledger` 원장). **인출가능액 = balance − 라이더채무(rider_wallets 합계) − 원천세예수금** 계산 검증 완료. ⏳ PG 충전(credit 호출)은 Phase F (§5.4, §5.5)
10. 🧩 **[2026-07-22 뼈대 완료]** 오픈뱅킹 — `OpenBankingGateway` 인터페이스 + `MockOpenBankingGateway`, `BankAccount`(대리점 계좌·핀테크번호), `Disbursement::transfer`. **DailyPayout·AgencyPayout에 연결**(이체 성공→completed, 실패→failed+잔액보존). `agency_bank_accounts` 테이블. 검증: 이체 성공/실패. **실 금융결제원 연동 시 RealOpenBankingGateway만 추가.** (§5.4)
    - 🧩 **[2026-08-08 추가] 라이더 출금은 펌뱅킹(쿠콘·하이픈)으로 분리** — 신규 `FirmBankingGateway`(+Mock+팩토리), `Withdrawal::executeTransfers()`. 대리점이 「출금 확정」을 누르면 **건별 즉시 이체**, 실패해도 나머지 계속(성공 건만 완료·지갑 차감). 기존 파일 다운로드 경로는 백업으로 유지. 오픈뱅킹과 별도 클래스인 이유는 사업자·인증 체계가 달라서이며, 일일정산·대리점 인출은 계속 오픈뱅킹 경로를 쓴다. **실 API 도착 시 `FirmBankingGatewayFactory::make()`만 교체.**
11. ✅ **[2026-07-22 완료]** 대리점 자체 인출 화면 — `AgencyPayout`(승인 없이 즉시 차감, kind=agency_payout, 인출가능액 초과 거부), `withdrawal/agency-payout` 화면(지갑요약+신청폼+내역), API. 대리점 운영/정산만 신청, 본사·총판 조회. ⏳ 실제 이체(pending→completed)는 오픈뱅킹 Phase F (§5.5)
12. ✅ **[2026-07-22 완료]** 영업대행수수료 분배 — `org_fee_config`(조직별 요율, 기본 1%), `PgFeeConfig`(pctForOrg/breakdownForAgency/feeAmount), `system/pg-fee` 본사 관리 화면. 대리점 결제 총 요율 = 대리점+상위총판+본사 몫. 검증: 100만원→3%→30,000원. (§5.4)
13. ✅ **[2026-07-22 완료]** 선지급(advance) 입력 화면 — `deduction/advance`(라이더 검색+금액+정산일+메모), `advance_entry.php` API(create/delete, deduction 권한). deduction_entries(kind=advance) 입력→정산반영 시 차감 반영 검증. (§5.3)
14. ✅ **[2026-07-22 완료]** 대표·서브계정 권한관리 — `OrgAccount`(대표=조직 최초계정, 서브계정 CRUD, super 부여 금지, 타조직 방어), `system/team` 화면(대표만), `admin_can_manage_team()`. 검증: 대표 CRUD·타조직 차단·비대표 차단. (§2)
15. 🆕 **원천세 라이더별 적용여부 설정**(`riders`에 플래그) + **대리점용 원천세 대상자 명세 조회 화면**(신고용) (§5.3, §5.5). ✅ **[2026-07-22 계산+예수금 완료]** — `withholding_tax_enabled=1` 라이더만 원천세 공제, 공제분을 `agency_wallets.withholding_reserve`에 누적(`AgencyWallet::addWithholdingReserve`). ✅ **[2026-07-22 UI 완료]** 라이더 상세화면 원천세 토글(`withholding_tax` 액션) + 대리점용 대상자 명세 조회 화면(`settlement/withholding`, 대상자별 공제누계+예수금+기간필터)
16. ✅ **[2026-07-22 완료]** 리스크 알림·큰 금액 하이라이트 — 대시보드 위젯(`AdminDashboard::riskAlerts` 본사전용 수동조정·삭제 감사로그 / `largeWithdrawals` 100만원↑ 출금, 스코프 COALESCE(rider.agency, wr.agency)). 검증: 본사만 리스크, 대리점은 자기 대형출금. (§4.3, §5.6)
17. ✅ **[2026-07-22 완료 → 2026-07-24 정책 변경]** 탈퇴 라이더 잔여 처리 — ~~0원 이체 상각~~ → **잔여(보증금 포함) 실제 지급 후 종결**(`DailyPayout::closeOut`, `zeroClose`는 별칭). `rider_action.php` `close_out`(구 `zero_close` 호환) + 라이더 상세 버튼 "잔여 지급 후 종결". 자금 흐름은 `payRider`와 동일(대리점 잔액 확인→이체→대리점 차감+지갑0), 종결 지급엔 수수료 미부과. 검증 20/20(정상지급·잔액0·계좌없음거부·활동중거부·대리점잔액부족거부·원장 direction=debit·잔액원복). (§5.4)
18. 🔧 **정산수수료(구 이체수수료) 계산모델 교체** — 단일 `accrued_days` 기준 → 주문별 "오늘로부터 경과일" age-bucket 합산 모델 (§5.4).
    - ✅ **[2026-07-24 완료] 수수료 계산 함수** — `WithdrawalConfig::feeForCycles(cycles, orgId, asOf)`. 같은 정산일 주문은 경과일이 동일하므로 **사이클(라이더·일자)의 `order_count` 단위로 계산해도 주문 1건씩 계산한 것과 결과가 같다**(원래 이연 사유였던 "주문별 날짜 데이터 부재"는 실제 문제가 아니었음 — `settlement_rider_cycles`에 `order_count`·`settlement_date`가 이미 있음). 경계(임계일 당일=long)·미래일자·잘못된 날짜(보수적으로 short) 포함 13/13 테스트 통과. 구 모델 대비 예시에서 80원 → 3,640원(45배 과소부과 교정).
    - ✅ **[2026-07-24 완료] 사이클 추적 + 선택 로직** — `settlement_rider_cycles.withdrawn_amount`(부분출금까지 표현: 0=미출금, net_amount=완전출금) + 신규 `withdrawal_request_cycles`(출금↔사이클 연결, 금액·부과건수). 신규 `WithdrawalCycles` 클래스가 **오래된 사이클부터 선택**(대리점이 이미 플랫폼에서 정산받은 분이라 40원 구간 = 저렴)·수수료 산출·점유·해제를 담당. `RiderWallet::previewWithdrawal()`은 사이클 기반으로 재작성(구 키 전부 호환 유지 + 구간 내역 키 추가), 사이클이 없으면 **구 모델로 자동 폴백**. `Withdrawal::applyForRider()`는 신청+점유를 한 트랜잭션으로 처리, `markRejected()`는 **점유 해제**(반려된 건만). 라이더 출금 화면에 "최근 7일 이내 N건×80원 / 7일 지난 M건×40원" **구간 분리 표기**. 검증 20/21(오탐 1건은 직전 실패 실행의 고아 데이터, 정리 후 해소).
    - ✅ **[2026-08-09 정정 확정] 보증금 경계 정책 = `POLICY_ORDER`(배달 건 단위 출금).** 갑 확답의 "한 건"은 **배달 1건**이지 정산 1건(=하루치 사이클)이 아니다. 배달 1건은 몇 천 원이라 보증금(5만원)을 넘을 일이 없고, 수수료도 이미 주문 건별(80/40원)이라 건 단위 절단과 정확히 맞물린다 — 가져간 건수만큼만 부과하면 된다.
      - 🐛 **[2026-08-09] 이전 `POLICY_WHOLE`은 오독이었다.** "한 건"을 정산 1건으로 읽어 사이클 통째로만 출금하게 했는데, 실데이터는 하루치 정산이 **평균 12.6만원·최대 24.6만원, 사이클의 80.6%가 보증금 초과**라 **"잔액 24만원인데 출금 0원"** 교착이 상시 발생했다(실제 발생: 서원빈 건). 전제가 정반대였던 셈.
      - **절단 방식** — 사이클의 미출금 배달 건수 Mr·금액 R에서 1건당 평균 R/Mr(올림)로 보고, 출금가능액에 들어가는 K건만 가져간다. 금액은 `R×K/Mr`(내림)에 가용액 클램프. 1건 값도 안 되면 그때만 차단.
      - **검증** — 무작위 2만 조합에서 초과인출·건수오류·불필요차단 0건, 반복 출금 시 사이클 완전 소진되며 **누적 부과 건수가 실제 배달 건수와 정확히 일치**(과다부과 없음), 실데이터 22명 중 21명 출금 가능으로 해소(보증금 침범 0건). E2E(하루 20만원×4일 일일정산)에서 매일 정상 지급 + 지갑 회계 완전 정합.
      - **순서 보장** — 막힌 사이클에서 `break`(건너뛰고 뒤의 작은 사이클을 먼저 먹지 않음). 화면 문구 "오래된 정산분부터 순서대로"와 달력의 "그 날짜까지" 의미를 지키기 위함(건너뛰면 중간에 구멍이 뚫린다).
    - ✅ **[2026-08-08] #7 후정산(주정산 대행수수료 출금시점 부과)은 취소** — 출금 시점엔 **건당 정산수수료만** 부과한다(갑 확정). §7 #7 참고.
19. ✅ **[2026-07-22 완료]** 일일정산(선정산) 지급 리스트 + 원클릭 지급 — `DailyPayout`(지급대상=선정산·잔액>0 라이더, payRider/payBatch: 대리점잔액에서 차감·라이더잔액0·auto_daily completed, 대리점잔액 부족 시 거부), `withdrawal/daily-payout` 화면(대상목록·개별/일괄 원클릭·부분성공). end-to-end 검증(라이더10만→0, 대리점30만→20만). ⏳ 실제 오픈뱅킹 이체는 Phase F(현재 원클릭=completed 기록) (§5.4)
20. ✅ **[2026-07-22 완료]** 정산/잔액 수동 조정 화면(본사 super 전용) — `ManualAdjust`(adjustRiderWallet/adjustAgencyBalance/adjustAgencyReserve, 사유 필수 + before/after 감사로그 `MANUAL_ADJUST`), `system/manual-adjust` 화면(조회→조정), API. PG/오픈뱅킹 자동 롤백 없음(실제 환불은 콘솔에서 별도). (§5.4a)

**🆕 라이더 앱 — 재설계와 무관하게 원래 남아있던 미구현분 (관리자 작업 후 착수)**
21. 정산 달력 보기 — 완전 목업, DB 연동 필요. 출금 신청과 결합한 신규 컨셉 프로토타입 완료(`mockups/rider_withdrawal_calendar_mockup.html`), 정식 개발 시 반영
22. 정산 상세("샘플") — 완전 목업, DB 연동 필요
23. 홈 "이번 달 정산 합계" 카드 — 하드코딩 제거, 실제 집계로 교체

---

## 8. 미결·리스크

### A. 갑 확인이 필요한 비즈니스 사항

> ✅ **2026-08-08 기준 미결 0건** — 아래 3건 모두 해소됐다(이력 보존용으로 남겨둠). 새 항목이 생기면 아래에 추가할 것.
>
> 🔎 **코드에서 찾는 법** — 미확정 지점은 전부 `TODO(갑확인)` 태그로 표기돼 있다:
> ```
> grep -rn "TODO(갑확인)" --include=*.php .
> ```
> **현재 0곳** — 2026-08-08에 남아 있던 3곳(`WithdrawalCycles::BOUNDARY_POLICY`, `PgFeeConfig::DEFAULT_PCT`, `Organization::create()` 시드)이 모두 확정돼 태그를 제거했다. 새로 미확정 지점이 생기면 이 태그를 붙여 관리할 것.

1. ✅ **[2026-08-08 해소 → 2026-08-09 해석 정정] 정산수수료 age-bucket — 보증금 경계 처리 방식.** 갑 확답: **"한 건이 5만원을 넘는 경우는 없다. 출금은 건으로 나눠서 항상 보증금 5만원 이상이 남도록."** ⚠️ 이 **"한 건"은 배달 1건**이다(2026-08-09 갑 재확인). 처음엔 정산 1건(하루치 사이클)으로 읽어 `POLICY_WHOLE`로 구현했다가 실데이터에서 출금 교착이 발생해 **`POLICY_ORDER`(배달 건 단위)로 정정**했다(§7 #18에 상세). 용어가 겹칠 때는 단위를 반드시 되물을 것 — "건"이 주문·정산·출금 중 무엇인지에 따라 결과가 정반대가 된다.
2. ✅ **[2026-08-08 해소] 영업대행수수료 분배비율.** 갑 확답: **"계약할 때마다 바뀌는 거라 지금처럼 1,1,1로 해놓고 어차피 등록할 때마다 등록할 거야. 초기 비율이 중요하지 않아."** → 1%×3은 "확정 대기 중인 임시값"이 아니라 **빈 값 방지용 자리값**. 실제 비율은 조직 등록 시 매번 입력한다. `PgFeeConfig::DEFAULT_PCT`·`Organization::create()` 시드의 `TODO(갑확인)` 태그 제거 완료.
3. ✅ **[2026-08-08 해소] 정산수수료 계산 구조 — `parser.py`는 구 프로그램, 대조 대상 아님.** 갑 확답: **"파이선 프로그램은 구 프로그램이고, 우리는 출금 신청 시 건당 수수료만 적용하면 된다."**
   - 2026-07-30에 "최대 쟁점"으로 올렸던 근거(운영 파서 `parser.py`의 주정산 50원 / 선정산 200건미만 200원·이상 100원 + 라이더별 수동단가)는 **레거시 시스템의 규칙**이라 우리가 맞출 필요가 없다.
   - **우리 모델 유지** — 출금 신청 시점에 **건당 정산수수료만** 부과(age-bucket: 임계 7일 이내 80원 / 지난 건 40원, `WithdrawalConfig::feeForCycles`). 대행수수료는 선정산 라이더의 **정산 반영 시점**에만 부과되고, 주정산 라이더에겐 어디서도 부과하지 않는다(§7 #7 참고).
   - 따라서 §7 #7의 "후정산 대행수수료 출금시점 부과"도 함께 취소. **현재 코드가 이미 이 동작이라 계산 로직 변경 없음**(주석·문서만 정정).

### B. 구현 시 유의 (문서 내부 정합성)

1. `content_notices/banners.org_id` 실질적 무의미 — 제거 검토
2. RBAC role 체크만으론 "공지=본사전용" 강제 안 됨, org레벨 가드 별도 필요
3. `withdrawal_status.failed` 추가 시 PHP enum·system_codes 동시 갱신 (상태값 이중정의 패턴)
4. sidebar 권한 없는 메뉴는 403(숨김 optional)
5. ⚠️ **"대행수수료"(AgencyFeeConfig) vs "영업대행수수료"(PG) 이름이 비슷해 혼동 위험** — 완전히 다른 별개 수수료(§5.3·§5.4), 코드 변수/컬럼명을 명확히 구분할 것
6. "지사"라는 표현이 대화 중 재차 등장(2026-07-15, 영업대행수수료 맥락) — §2에서 **지사 없음(본사·총판·대리점 3단)**으로 이미 확정했으므로, 영업대행수수료 조직별 설정은 **총판·대리점 각각**에 적용되는 것으로 해석. 혼동 시 재확인.

### ✅ 확정으로 해소된 항목 (2026-07-02 · 07-13 · 07-14 · 07-15)

지사 없음(본사·총판·대리점 3단) · 라이더 대리점 이관 절차 불필요 · 이체/PG결제 실패 처리(알림+이력+재시도) · 대리점 인출 잔액 정의(원천세만) · 후정산 시 정산수수료+선정산수수료 동시부과 · PG사(본사 단일결정) · PG 정산금 입금경로(agency_wallets) · **정산 반영 후 정정=본사 전용 수동조정 화면**(§5.4a) · **선정산 지급=일일정산 리스트+원클릭**(자동아님, 사람 확인 유지) · **용어 확정**(구 이체수수료→정산수수료, 구 결제수수료→영업대행수수료) · **PG 결제 단위=총액 일괄 아닌 라이더별 건건히**(2026-07-15 정정) · **대리점 카드 다건등록+자동 대체결제** · **고용·산재보험은 예수금 아님**(쿠팡 처리) · **원천세는 라이더별 설정+대리점 조회리포트** · **fee_calc_timing=is_daily_settlement로 자동 결정**(별도 설정 아님) · **"정산주기" 질문의 실체 확인(2026-07-16)** — 별도 주간개념이 아니라 **정산수수료(기준일수·금액)를 대리점이 직접 관리하는 것**이었음, 방향은 유지(최근=80원/오래됨=40원)

---

## 9. 새 기능 추가 체크리스트

- [ ] `inc/routes.php`(+`rider_routes.php`) 등록
- [ ] `inc/auth.php` — 라우트 접근·쓰기 권한 반영
- [ ] 도메인 클래스(`inc/*.php`) — SQL·검증·상태 전이
- [ ] `admin/api/*.php` — 로그인+쓰기권한+`AuditLog::record`
- [ ] `system_codes` 필요 여부, 상태값 추가 시 PHP enum도 동시 갱신
- [ ] migrate/SQL + seed
- [ ] 이 문서 §3·§5·§7·§8 갱신

---

## 10. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-08-09 (6) | **정산 원본 상세 — 정산예정·실지급 산식 + 공제 내역.** 정산예정 = 픽업·배달·할증·프로모션 합계 + 부가세 10%(엑셀 총액과 일치 검증). 실지급 = 정산예정 − (부가세 + 시간제보험 + 기타공제). 기타공제는 종합탭 고용·산재·원천세 등 항목별 미저장분. 차감내역 탭은 「정산 반영 시 추가 차감」으로 분리 표시(엑셀 실지급에 아직 미포함). |
| 2026-08-09 (5) | **정산 원본 상세 모달 — 받을 돈 / 수수료 / 실지급 구분.** 픽업·배달 등 엑셀 세부액은 받을 돈 구성(+), 시간제보험·정산예정−실지급 차액은 수수료(−). 상단에 `받을 돈 − 수수료 = 실지급` 식을 크게 표기. |
| 2026-08-09 (4) | **정산 업로드 상세 「원본 데이터」 모달 가독성 개선.** 라벨·값 나열을 헤더(엑셀이름·플랫폼·매칭 라이더) + KPI(오더/정산예정/지원금/실지급) + 수수료·프로모션 표(항목/건수/금액, 0원 항목은 각주로 생략)로 재구성. |
| 2026-08-09 (3) | **정산 업로드 상세 라이더명 링크 404.** query-URL 모드에서 `admin_url('riders/detail')` 뒤에 `?id=`를 또 붙여 `?route=riders%2Fdetail?id=N`이 되던 버그. 표의 라이더 이름 링크와 상세 모달 「라이더 상세 보기」 모두 `?`가 이미 있으면 `&id=`로 붙이도록 수정. |
| 2026-08-09 (4) | **🐛 플랫폼 수수료가 아예 발생하지 않던 문제 수정 + 원천세 명세 보강.** ① **플랫폼 수수료 미발생** — `PgPayment::fundAppliedUpload()`(정산 반영분 PG 자금조달)가 구현만 돼 있고 **아무데서도 호출되지 않는 죽은 코드**였다. 그래서 `pg_payments`가 0건이었고 「플랫폼 수수료 내역」 화면이 계속 비어 있었다. 갑 확인("플랫폼수수료는 정산을 하면서 바로 발생") 대로 `settlement_apply.php`에서 **정산 반영 직후 → 자동출금 이전**에 호출하도록 배선(돈이 대리점→라이더로 흐르므로 조달이 출금보다 먼저). 재실행해도 이미 `success`인 (upload_id, rider_id)는 제외돼 이중청구가 없다. **카드 미등록 가드 추가** — 없으면 라이더 수만큼 "카드 없음" 실패기록이 쌓이므로, 한 번에 막고 "카드를 등록하세요" 한 줄만 돌려준다. ② **원천세 명세 기본 기간 = 이번 달 1일~말일**(월 단위 신고에 맞춤) + 이번달/지난달/올해 빠른선택. ③ **원천세 라이더별 상세 내역** — 「내역 N건」 버튼으로 정산일별(플랫폼·팀지역·실지급·지원금·과세표준·원천세) 펼침. 과세표준 = 실지급+지원금임을 화면에 명시. 검증: 정산 반영 API 실호출로 조달 98,320원·플랫폼수수료 2,950원(3자 배분 합계 일치) 발생 확인 후 화면 노출 확인, 재반영 시 이중청구 0건, 원천세 상세에서 지원금 가산 케이스(246,766+3,200=249,966 × 3.3% = 8,249원) 정확 확인. 테스트 데이터·지갑·수수료율 전량 원복. |
| 2026-08-09 (3) | **🐛 보증금 경계 정책 정정 — `POLICY_WHOLE` → `POLICY_ORDER`(배달 건 단위).** 갑 확답의 "한 건이 5만원을 넘지 않는다"를 **정산 1건(하루치 사이클)** 으로 오독해 사이클 통째 출금(WHOLE)으로 구현했었으나, 갑 재확인 결과 **배달 1건**이었다. 실데이터는 하루치 정산이 평균 12.6만원·최대 24.6만원으로 **사이클의 80.6%가 보증금(5만원) 초과**라, WHOLE에서는 "잔액 24만원인데 출금 0원" 교착이 상시 발생했다(실제로 서원빈 건에서 발생). `WithdrawalCycles`에 `POLICY_ORDER` 추가 — 미출금 배달 건수 기준 1건당 평균값으로 보고 출금가능액에 들어가는 건수만큼만 절단(`orderUnitSplit`/`remainingOrderUnit`). 수수료가 이미 주문 건별이라 절단 단위와 정확히 맞물린다. 검증: 무작위 2만 조합 불변식(초과인출·건수오류·불필요차단 0), 반복 출금 시 완전 소진 + **누적 부과건수 = 실제 배달건수**(과다부과 0), 실데이터 22명 중 21명 출금 가능으로 해소, E2E(20만원×4일 일일정산) 매일 정상 지급 + 지갑 회계 정합(적립=지급+수수료+잔액). WHOLE/PARTIAL 구현은 상수 전환용으로 존치. |
| 2026-08-09 (2) | **일일정산 라이더 자동출금 신규.** 「정산 반영 · 수수료·지갑」을 누르면 일일정산(`is_daily_settlement=1`) 라이더는 반영 직후 곧바로 이체까지 실행(`inc/DailyAutoWithdrawal.php`). 보증금은 `RiderWallet::previewWithdrawal()`이 이미 `잔액−보증금`만 대상으로 잡으므로 자동 제외. 부수 변경: 엑셀 업로드 확정 후 업로드 상세로 자동 이동, 업로드 상세 라이더 목록에 「정산유형」(일일/주정산) 컬럼 + 자동출금 예고 배너. |
| 2026-08-09 | **라이더 엑셀 일괄등록 신규.** 신규 대리점 입점 시 기존 라이더를 한 번에 옮겨 적는 기능(§5.2). 단건 등록(`riders.php`) 검증·생성 로직을 `inc/RiderRegistration.php`로 공용화(`validate()`/`create()` 분리 — 미리보기는 검증만, 확정은 검증 후 실제 INSERT). `riders_bulk_template.php`(템플릿 다운로드) + `riders_bulk_upload.php`(미리보기→확정 2단계 업로드, 행별 독립 처리라 일부 실패해도 나머지는 등록) 신규. 겸사겸사 발견한 정산 업로드 크리티컬 버그(`settlement_upload.php` — `db_transaction` 클로저의 `use()` 누락으로 `team_name`/`region_name`이 undefined variable warning을 출력해 JSON 응답이 깨지던 문제)도 이 기간에 수정 완료. |
| 2026-08-08 (10) | **운영 데이터 초기화 스크립트.** 최고 관리자(`role=super`)와 그 소속 본사 조직, `system_codes`/`role_permissions`/전역 설정만 남기고 총판·대리점·다른 관리자·라이더·정산·출금·콘텐츠·감사 로그를 삭제하는 CLI `scripts/reset_operational_data.php` 추가. `--execute` 없이 미리보기, `--execute`로 실제 삭제. `docs/manual/OPERATIONS_MANUAL.md`에 절차 기록. |
| 2026-05-24 | 초안: RBAC·감사·코드마스터·콘텐츠/라이더/정산/출금/시스템 정리, 출금 일원화, 목업 제거 |
| 2026-06-30 | 멀티테넌시 설계 도입 — 3계층 조직트리, 2축 권한 |
| 2026-07-01 | 멀티테넌시 Phase 1~5 완료·검증 — 스코핑엔진, 조직관리, 전 도메인 스코핑, per-agency 설정값, 정산업로드 2단계화, 라이더앱 연동 |
| 2026-07-07 | **갑 미팅 — 대대적 재설계 확정.** 조직생성 본사전용, 대표+서브계정, 콘텐츠 본사전용, 라이더 이관 불필요 확인, 선정산 입력 신규, **출금 오픈뱅킹 전환**, 대리점 자체인출 신규 |
| 2026-07-12 | 세부 정정(고용·산재보험 분리, 시간제보험 파일파싱, 지사 없음 확정) + **PG 카드결제 신규**(자금조달) + 이체·PG실패/대리점인출잔액/수수료동시부과 등 세부 확정 |
| 2026-07-12 | 전체 프로세스 다이어그램 검토 — **`withdrawal_requests` 스키마 충돌 발견**(rider_id NOT NULL vs agency_payout), PG-정정 상호작용·선정산 용어중복 등 리스크 정리. **문서 재구성**(§1 전체프로세스, §3 메뉴표, §7 구현작업목록 신설) |
| 2026-07-13 | 관리자 작업 일시 보류, 라이더 앱 착수 준비 — 정산 달력·상세("샘플")·홈 정산합계 카드가 여전히 목업임을 재확인(§3). 출금 신청용 "정산 달력" 컨셉 목업(`mockups/rider_withdrawal_calendar_mockup.html`) 제작하며 **정산수수료(구 이체수수료) 계산모델 정정** — "마지막출금 후 경과일 단일값" → "오늘로부터 각 배달 주문의 경과일" 기준, **주문 건별로** 7일 이내 80원/지남 40원이 섞여 합산되는 방식으로 확정(§5.4) |
| 2026-07-14 | **관리자 미팅 완료 — §8-A 미결 3건 해소.** ① 정산 반영 후 정정=자동 PG환불 없이 **본사 전용 수동 잔액조정 화면** 신설(§5.4a) + "대부분 수정은 본사가 관리자화면에서" 일반원칙 확정. ② PG 결제금액=업로드 총액 전액(선정산/주정산 구분 없음, 단순함 우선). ③ 용어 확정 — 구 "결제수수료"→**영업대행수수료**(PG 발생, 3자분배), 구 "이체수수료"→**정산수수료**(라이더 출금 건당, 본사몫). ④ 선정산 지급방식 확정 — 라이더 개별신청 아님, **관리자가 "일일정산 리스트"에서 원클릭 지급**(사람 확인 유지, 완전자동 아님). 남은 미확정 2건(§8-A #1~2)만 남음. **"대행수수료" vs "영업대행수수료" 용어 혼동 위험 신규 발견**(§8-B #7) |
| 2026-07-15 | **미팅 후속 답변 — §8-A 마지막 2건 완전 해소, LOGIC.md §8-A 항목 0건.** ① `fee_calc_timing`은 대리점 임의선택이 아니라 **`riders.is_daily_settlement`로 자동 결정**(선정산=반영시 즉시계산, 주정산=출금신청시 계산) — 별도 컬럼 불필요할 수도. ② 정산 주기(주간)는 별도 개념 불필요, `is_daily_settlement`로 충분. **PG 결제 단위 정정(중요)** — 총액 일괄 결제 → **라이더별 건건히 결제**로 변경(카드한도 리스크 구조적 완화). **대리점 카드 다건 등록 + 한도초과 시 자동 대체카드 순차재시도** 신규. **세금예수금 대폭 정리** — 고용·산재보험(0.8%/0.88%)은 쿠팡이 처리해 예수금 아님(단순 차감만), **원천세만 진짜 예수금**이라 라이더별 적용여부 설정 + 대리점용 신고 리포트 신규 필요. 영업대행수수료는 총판·대리점 조직별 개별 입력, 본사 통합관리 확인. 수동조정 본사전용 재확인 |
| 2026-07-16 | **"정산 주기" 재해석 — 어제(07-15) "별도 개념 불필요"로 종결했던 게 부정확했음을 재확인.** 실제로는 라이더 앱 정산달력 목업에 이미 들어간 개념(정산수수료 기준일수·금액)을 **대리점이 직접 관리**하게 해야 한다는 의미였음. **정산수수료(구 이체수수료) 범위 재정정** — 어제 "본사 전역값"으로 확정했던 걸 다시 뒤집어 **대리점별 설정**으로 최종 확정(기준일수 7일·미만금액 80원·이상금액 40원 전부 대리점이 관리, 방향은 불변). 원래 `withdrawal_config.org_id` 구조가 이미 있어 오히려 개발 작업 감소. 금액 방향(최근=80원·오래됨=40원)은 재확인해 변경 없음 |
| 2026-07-16 (2차) | **후속 질문 답변 — 남은 세부사항 확정.** ① 대리점 자체 정산금 인출(§5.5)은 **승인 절차 없음 확정** — 인출가능액 산식에 이미 예수금·라이더채무가 빠져있어 신청 즉시 실행. ② 신규 대리점 생성 시 정산수수료 기본값(기준일수 7일·미만 80원·이상 40원) **자동 seed**, 이후 대리점이 자유 수정 — `Organization::create()`에 반영 필요. ③ 원천세 적용여부는 **대리점이 라이더 상세화면 체크박스로 설정**(신규 컬럼), **세율 3.3%는 고정**(대리점별 조정 불가). ④ 영업대행수수료 초기 분배값 = **대리점·총판·본사 각 1%씩 임시 세팅**(확정값 아님, 추후 재조정). ⑤ **PG결제(1단계)와 일일정산 지급(2단계) 시점 완전 분리 확정** — PG결제 완료=대리점 잔액 충전까지만, 라이더 개별 지급은 PG결제가 다 끝난 뒤 **별도 시점에 별도로**, 여전히 **수동**(자동 아님)으로 진행. 일일정산 리스트 중 일부 라이더만 이체 실패해도 나머지는 정상 지급, 실패건만 재처리(§5.4 재확인). ⑥~⑧ 마이그레이션 스크립트·데이터 시딩·배민 정산자료는 준비되는 대로 후속 전달 예정. 개발 착수는 이후 별도 세션(Opus)에서 진행 |
| 2026-07-22 | **관리자 개발 착수(Opus) — Phase A·B 완료·실DB검증.** ▷ **Phase A 스키마 기반**: `withdrawal_requests` 스키마충돌 해소(rider_id nullable+agency_id+fail_reason, kind에 agency_payout, status에 failed) · `agency_wallets`+`agency_wallet_ledger` 신규테이블 · `deduction_global_config` 고용(0.8)/산재(0.88) 분리 · `riders.withholding_tax_enabled` 플래그. 전부 멱등 마이그레이션, 재실행 SKIP 검증. ▷ **Phase B RBAC**: 조직생성 본사전용(총판 권한 회수) · 공지·배너 쓰기 본사전용(org레벨 가드). 3계층 계정 시뮬레이션 검증(총판 operation이 조직관리·공지쓰기 모두 차단됨 확인). ▷ **부수 발견**: `admin/api/banners.php` 빈 파일(배너 CRUD 유실) → Phase E 재구현 대상 |
| 2026-07-22 | **Phase C 정산 반영 도메인 완료·검증.** `SettlementLedger::buildFeeItems` 재작성: ① **고용/산재 분리 공제**(고용 0.8% `employment_ins` + 산재 0.88% `accident_ins`, 기존 합산 9.12% 버그 교정) ② **원천세 대상자만 공제**(`riders.withholding_tax_enabled=1`), 공제분을 `agency_wallets.withholding_reserve`에 누적(신규 `AgencyWallet` 클래스) ③ **선정산수수료(대행) 반영시점 분기**(`is_daily_settlement=1` 라이더만 반영 시 부과, 주정산은 이연). Reflection 검증: 일일+원천세ON=49,880공제 / 주간+원천세OFF=16,800공제. ⏳ **#18 age-bucket 정산수수료 + #7 후정산 부과는 라이더 출금 플로우 단계로 이연**(미출금 사이클 추적 필요, 라이더 재설계와 결합) |
| 2026-07-22 | **Phase D 대리점 지갑·인출·수동조정 완료·검증.** ① `AgencyWallet` 클래스 확장(withdrawable=잔액−라이더채무−원천세예수금, credit/debit/setBalance + `agency_wallet_ledger` 원장). ② `AgencyPayout` — 대리점 자체 인출(승인 없이 즉시 차감, 인출가능액 초과 거부), `withdrawal/agency-payout` 화면+API(대리점 신청, 본사·총판 조회). ③ `ManualAdjust` — 본사 super 전용 수동조정(라이더지갑·대리점잔액·원천세예수금, 사유필수+before/after 감사로그), `system/manual-adjust` 화면+API. 라우트·auth·사이드바 등록. 도메인 통합테스트(충전500만→인출100만→잔액400만, 초과거부, 수동조정 before/after)+실DB 뷰렌더+권한시뮬 검증. ⏳ 실제 이체(pending→completed)는 오픈뱅킹 Phase F |
| 2026-07-22 | **Phase E (일부) — #19·#15·#13 완료·검증.** ① **#19 일일정산 지급 리스트**(`DailyPayout`, `withdrawal/daily-payout`): 선정산 라이더 원클릭/일괄 지급, 대리점잔액 차감·라이더잔액0·auto_daily completed, 잔액부족 거부, 부분성공 허용. ② **#15 원천세 UI**: 라이더 상세 토글(`withholding_tax` 액션) + 대상자 명세 화면(`settlement/withholding`). ③ **#13 선지급 입력**(`deduction/advance`): advance 입력→정산 차감 반영. 라우트·auth·사이드바 등록. ⏳ Phase E 잔여: #12 영업대행수수료 분배 · #14 대표·서브계정 · #16 리스크 알림 · 배너 CRUD 복구(admin/api/banners.php 빈파일) |
| 2026-07-22 | **Phase E 완결 — 나머지 #12·#14·#16 + 배너CRUD 복구.** ④ **#12 영업대행수수료 분배**(`org_fee_config`+`PgFeeConfig`, `system/pg-fee`, 조직별 요율 1%×3=총3%). ⑤ **배너 CRUD 복구**(`admin/api/banners.php` 재구현, Banner 도메인 노출). ⑥ **#14 대표·서브계정**(`OrgAccount`, `system/team`, 대표=조직 최초계정, 타조직/비대표/super 방어). ⑦ **#16 리스크 알림·큰금액 하이라이트**(대시보드 위젯, 본사전용 리스크+스코프 대형출금). 전부 도메인+실DB+뷰렌더 검증. **관리자 §7 백로그 중 외부연동(Phase F: PG·오픈뱅킹·시간제보험) + 이연(#18 age-bucket·#7 후정산)만 남음.** |
| 2026-07-22 | **Phase F 뼈대 완료 — 외부연동 어댑터 구조.** **드라이버 인터페이스 + Mock 구현**으로 전 흐름 동작(실 스펙 도착 시 Real 드라이버만 교체). ① **#8 PG**: `PgGateway`/`MockPgGateway`/`PgGatewayFactory` + `AgencyCard`(다건·우선순위·모의한도) + `PgPayment`(라이더별 결제, 한도초과 시 대체결제, 성공 시 지갑충전+영업대행수수료). `agency_cards`·`pg_payments`. ② **#10 오픈뱅킹**: `OpenBankingGateway`/`Mock`/`Disbursement` + `BankAccount`(핀테크번호), DailyPayout·AgencyPayout에 이체 연결(성공→completed/실패→failed+잔액보존). `agency_bank_accounts`. ③ **#17 0원 종결**(`DailyPayout::zeroClose`+라이더상세 버튼). ④ **#6 시간제보험** 컬럼+공제로직(파서 매핑만 실자료 대기). **결제설정 화면**(`withdrawal/payment-setup`: 카드관리+계좌+PG충전). 종합검증: 대체결제·전카드실패·이체성공/실패·0원종결. **⇒ 관리자 §7 백로그 전부 완료(이연 #18·#7 후정산 제외). 남은 건 각 Mock→Real 드라이버 교체(스펙 대기)뿐.** |
| 2026-07-22 | **DB_SCHEMA.md 신설.** DB 테이블·컬럼·관계 전용 기준 문서 추가(SHOW CREATE TABLE 기준 authoritative 스냅샷, 25개 테이블 전수 기술). `.cursor/rules/db-schema-sync.mdc` 신설(스키마 변경 시 함께 갱신 강제), 기존 `logic-md-sync.mdc`에도 상호 참조 추가. LOGIC.md 헤더에 DB_SCHEMA.md 링크. |
| 2026-07-22 | **정산 원본 데이터 상세 모달 추가.** `settlement/upload-detail` 표에 "상세" 버튼 추가 → `settlement_daily_riders` 전체 컬럼(픽업/배달/지역/거리구간/픽업콜/도착콜/기상할증/프로모션1~4/시간제보험) 조회 모달. 신규 API `admin/api/settlement_daily_rider_detail.php`(Org 스코프 검사). 모달에 "라이더 상세 보기" 버튼 추가(매칭된 경우만) — 기존 표의 라이더 이름 클릭 링크와 함께 두 경로 모두 `riders/detail`로 직결. |
| 2026-07-23 | **정산 엑셀 row-level 저장 확장 + 실 버그 수정.** 실제 쿠팡 정산서(`오엑스플러스_서울_강서남부_20260628.xlsx`)로 시트 구조 검증 후: ① **오더별 상세내역** 신규 저장(`settlement_order_details`, 주문 단위, 실 배정·수락·배달시각 포함 — §18 age-bucket 계산의 미래 데이터 소스). ② **시간제보험** 신규 저장(`settlement_hourly_insurance`) + `settlement_daily_riders.hourly_insurance` TODO 해소(실값 채움). ③ **차감내역 파서 버그 수정** — 기존 코드가 컬럼을 한 칸씩 밀려 읽어 실제 차감액(금액열) 대신 배달비를 저장하던 버그 발견·수정, 라이더 매칭 추가(이전엔 항상 미매칭). ④ **차감정보 등록** 신규(`deduction_register.php`, 업로드상세 화면에 목록+등록/취소) — 매칭된 차감행을 `deduction_entries`로 등록해 정산반영에 자동 반영되게 함. XlsxParser에 헤더 키워드 기반 컬럼매핑 헬퍼(`findHeaderRow`/`mapHeaderColumns`) 신설, 엑셀 시리얼→일시 변환 유틸 추가. 검증: 오더상세 328건 합계가 종합탭 총액과 정확히 일치, 시간제보험 2건 종합탭 AH값과 일치, 등록/중복방지/취소 전체 플로우 실DB 검증. |
| 2026-07-23 | **조직 관리 화면 고도화.** `system/orgs`에 ① **KPI 카드**(총판·대리점·활성라이더·중지조직) ② **검색/필터**(이름·코드·대표계정 검색 + 유형/상태 필터, 클라이언트) ③ **트리 들여쓰기**(총판→하위 대리점 그룹핑) ④ **대표 계정 역할 뱃지** 목록 표시(신규 `primary_role`) ⑤ **코드/이름 클릭 → 상세 모달**(`Organization::detail()` API: 기본정보+소속계정목록+대리점 라이더상태/지갑/정산수수료/영업대행/카드·계좌/원천세대상, 총판 하위대리점 목록) ⑥ **코드 자동생성**(`Organization::suggestCode()`, 레벨별 DIST-/AG- 접두+순번, 폼에서 유형선택 시 자동채움·재생성 버튼, 서버도 빈 코드면 자동생성) ⑦ **신규 조직 설정 시딩**(대리점 생성 시 withdrawal_config/agency_wallets/org_fee_config 자동 생성 — §5.4 미완이던 항목 완료). 도메인+API+뷰 렌더+create시딩 실DB 검증. |
| 2026-07-23 (2) | **조직 관리 — 역할별 다중계정 관리 + 수정 강화.** ① 본사가 조직관리 화면의 **상세 모달에서 특정 총판·대리점에 역할별 계정을 여러 개 추가·수정·비번재설정·활성토글** (신규 `Organization::addAccount/updateAccount/setAccountActive`, API `account_add/account_update/account_toggle`). 방어: super계정 불가, 타조직 불가, **마지막 활성계정 비활성화 불가**(로그인 보장). 기존 `system/team`(조직 대표계정용 `OrgAccount`)과 병행 — 본사는 모든 조직, 대표는 자기 조직. ② **수정 모달에 `memo`(내부 메모, organizations.memo 컬럼) 추가**(기존엔 이름·연락처만). 도메인+실API엔드포인트(추가→역할변경→토글) 실DB 검증. |
| 2026-07-23 (3) | **배달의민족 정산서 등록 지원(실 파일 검증).** 배민은 쿠팡과 구조가 근본적으로 다름 — **시트 1개(배달내역상세)·주문 단위만**, 종합/차감/보험 탭 없음, **파일 하나=여러 운행일(월간)**. `XlsxParser::parseBaeminOrders()` 신규 + `settlement_baemin_normalize()`(라이더·운행일별 집계 → 쿠팡과 동일 테이블 저장). `settlement_daily_riders` UNIQUE (upload,라이더)→(upload,라이더,날짜) 확장으로 다중 날짜 허용. 라이더 매칭 라이더ID→UserID→이름(matcher에 altId 인자 추가). 배민 수수료 체계 다름 → net_amount(배달처리비)만 정확 저장, 쿠팡 전용 fee 컬럼은 근사 매핑/0. 플랫폼 자동감지에 배민 마커+파싱검사 추가. 사용자 확정: 파일명 날짜범위 무시, 실제 운행일 기준 등록, 선정산/주정산 로직이 지급 처리. 실 API 업로드→DB(daily 2행·order 3건·합계 12,460 일치)→cascade정리 검증. |
| 2026-07-23 (4) | **라이더 플랫폼 아이디 매핑 3종.** ① **라이더 정보에 쿠팡/배민 ID 등록** — 라이더 상세 "플랫폼 아이디 연동" 카드(읽기전용 테이블 → 편집형, `rider_action.php` `set_platform` 액션, 같은 대리점 중복 방지·해제 지원) + 등록 폼에 쿠팡/배민 ID 입력(`riders.php`가 `coupang_id`/`baemin_id`로 `rider_platforms` 연동, 이전엔 external_id 빈값이라 매칭 불가였던 버그성 공백 해소). ② **정산 미매칭 시 기존 라이더 연결** — 빠른등록 모달에 "신규 등록/기존 연결" 탭, 검색(GET) 후 `action=link`로 쿠팡만/배민만 있던 라이더에 다른 플랫폼 ID 매핑(`settlement_register_rider.php`). ③ **라이더 상세 완성도** — 계약·보험 카드가 존재하지 않는 컬럼 참조로 경고+빈값이던 것 정리(KYC 뱃지·`!empty()` 가드), 상세 렌더 경고 0. 전 기능 실 API 엔드포인트로 검증(설정·수정·중복거부·해제·검색·연결·등록시 동시연동). |
| 2026-07-23 (5) | **정산 매칭키 확정 — 쿠팡=성함, 배민=UserID, 대리점 스코프 필수.** 사용자 확정: 쿠팡 매칭키는 라이선스ID가 아니라 **성함(name_raw, 예 박성준1682)**, 배민은 라이더ID(J)가 아니라 **UserID(K, 예 adammins)**. 라이더는 대리점 이동 시 별도 등록(로그인ID 새 발급, riders.login_id 전역 UNIQUE로 강제)이라 성함/UserID가 **대리점 간 중복 가능** → 정산 매칭·모든 플랫폼ID 중복검사에 `agency_id` 필수(이미 스코프였음, 재확인). `settlement_match_rider_id()`를 (platform, 매칭키, 이름폴백, agencyId)로 정리 + `settlement_row_match_key()`(쿠팡=name_raw/배민=UserID) 신규. 라이더 상세·등록·미매칭연결 UI 라벨을 성함/UserID로 변경. **실 파일 매칭 검증**: 쿠팡 `박성준1682`·배민 `adammins` 등록 후 미리보기 업로드 → 정확히 매칭. |
| 2026-07-24 | **라이더 관리 화면 수정·페이징·상세편집.** ① **라이더 목록 검색 버그 수정** — GET 필터 폼이 query-URL 모드에서 `route`를 잃어 검색이 동작하지 않던 것(action의 `?route=`는 폼 제출 시 브라우저가 폐기)을 `<input type="hidden" name="route">` 추가로 해결. ② **서버사이드 페이징** 신규(`riders_list`, 20건/페이지, 기존 `LIMIT 500` 캡 제거, Metronic pagination + "n–m/총 N명" 요약, 현재 필터 유지·필터변경 시 1페이지 리셋). ③ **상세 링크 404 버그 수정** — `admin_url('riders/detail')`(이미 `?route=` 포함) 뒤에 `?id=`를 또 붙여 이중 `?`로 route가 깨지던 것을 `&id=`(이미 `?` 있으면)로 수정(PHP 링크·등록 후 JS 리다이렉트 공통). ④ **라이더 상세 프로필 편집** — 툴바 "정보 수정" 버튼→모달에서 이름·전화·이메일·생년월일·KYC상태·팀·차량·주소·은행·계좌·예금주 일괄 편집, `rider_action.php` `update_profile` 액션(서버 유효성검사: 이메일/생년월일/차량enum/KYCenum/은행코드 존재/계좌 형식) 저장. 없는 컬럼(insurance_*/contract_signed_at 등)은 편집 대상 제외. 실DB로 전 필드 저장·검증 및 유효성 거부 확인. ⑤ **"팀"→"소속 대리점" 교체** — `riders.team_code`(gangseo_a 등)는 단일 대리점 시절의 지역/조 구분 잔재로 현재 전 라이더가 `etc`·실사용 없음. 멀티테넌시의 실제 소속 단위인 `agency_id`(organizations)를 상세·목록·수정에 노출하도록 교체: 상세 "소속·배달" 카드와 편집 모달에 **소속 대리점**(읽기전용, 이동은 재등록으로) 표시, 목록의 팀 컬럼→대리점 컬럼(organizations JOIN), 팀 필터→**대리점 필터**(`Organization::agencyOptions()` 스코프 인지, 본사·총판만 노출), 등록폼 팀 필드 제거(서버는 `team_code` 기본값 `etc`). `team_code` 컬럼·정산 등 타 사용처는 그대로 보존(UI에서만 제거, 되돌리기 쉬움). 실DB 렌더 검증: 목록 팀 0회·대리점 표시, 상세 소속 대리점 표시, update_profile가 team_code 미변경 확인. |
| 2026-07-24 (2) | **라이더 부채 원장 신규 — 대여금·리스·선지급(§5.7).** 실제 라이더 정산명세서 PDF 분석으로 발견한 최대 격차(잔액 이월형 부채)를 구현. 신규 `rider_debts`+`rider_debt_entries` 테이블, `inc/RiderDebt.php` 도메인(create/update/applyRepayment/reverseEntry), `admin/api/debt_action.php`, **전용 관리 화면 `deduction/debts`(차감 › 대여금·리스 원장 — 전체 라이더 대상 등록·차감·수정, KPI·필터·라이더 검색 picker)** + 라이더 상세 "부채" 카드(그 라이더 부채·이력·취소). 상각형(대여금/선지급) 잔액 자동 감소·완납, 리스는 일납×일수 반복 부과. **차감 실행이 `deduction_entries`를 생성해 기존 정산 반영이 그대로 차감**(중복 로직 없음). 실 DB 검증: 대여금 1,250,000→50,000차감→1,200,000, 리스 24,000×7=168,000, 초과차감 캡·완납·취소복구·CASCADE·엔드포인트 GET/스코프 전부 확인. DB 27→29개 테이블. **구 "선지급(대여금) 입력"(`deduction/advance`) 메뉴 제거** — 새 원장이 선지급금을 잔액 추적형으로 완전 대체하고 기존 `deduction_entries` 데이터가 0건이라 보존할 이력도 없음(라우트·`advance_input.php`/`advance_entry.php`는 남겨둬 직접 URL 접근 가능, 되돌리기 쉬움). 남은 격차(차명계좌·지원금·타임버킷·통합명세서 발급)는 [[settlement-statement-gaps]] 기록. |
| 2026-07-24 (3) | **정산수수료 age-bucket 계산 함수 + 탈퇴 잔여 지급 정책 변경(§7 #18·#17).** ① **`WithdrawalConfig::feeForCycles()` 신규** — 주문 건별 요율(임계일 이내 80원 / 지남 40원)을 사이클 단위로 합산. **이연 사유였던 "주문별 날짜 데이터 부재"가 오해였음을 확인**(같은 정산일 주문은 경과일이 동일 → `settlement_rider_cycles.order_count`로 동일 결과, `settlement_order_details`까지 갈 필요 없음). 13/13 테스트 통과, 구 모델 대비 80원→3,640원(45배 과소부과 교정). ② **탈퇴 잔여 "0원 상각"→"실제 지급"**(`DailyPayout::closeOut`, 사용자 확정) — 보증금은 예치금 성격이라 정상 종결 시 라이더에게 지급. `payRider`와 동일 자금흐름(대리점 잔액 차감 포함), 계좌없음·대리점잔액부족 시 **거부**(돈 소멸 방지), 종결 지급엔 수수료 미부과. 20/20 검증. ③ 🐛→✅ **출금 완료 시 지갑 덮어쓰기 버그 수정** — `RiderWallet::finalizeAfterComplete()`가 `balance = 보증금`으로 **대입**해서, 출금 신청(pending)~완료 사이에 정산이 반영되면(`credit()`) 그 금액이 **소멸**했다(예: 신청 시 159,000 → 중간 정산 +40,000 → 완료 시 50,000이 되며 40,000 증발). **`deductAfterWithdrawal(riderId, 실지급액+수수료)`로 개명·차감 방식 전환**(대입→차감, `GREATEST(0,…)`로 음수 방지). 사이클 재작성이 갑 확인 대기로 무기한 지연될 수 있어 **독립적으로 선반영**. 7/7 검증(정상경로 결과 동일·중간입금 보존·잔액급감 클램프·auto_daily 미차감). ④ **§8-A 신규 1건** — 보증금 경계 사이클 처리(통째 vs 부분출금) 갑 협의 대기로 사이클 선택 로직은 미착수. |
| 2026-07-24 (4) | **정산수수료 age-bucket — 사이클 추적·선택 로직 완성(§7 #18).** ① **스키마**: `settlement_rider_cycles.withdrawn_amount`(부분출금 표현) + 신규 `withdrawal_request_cycles`(출금↔사이클 연결) → **30개 테이블**. ② **`WithdrawalCycles` 신규** — 오래된 사이클부터 선택(40원 구간 우선 소진)·수수료 산출·점유(attach)·해제(release). **마킹 시점은 신청(pending)**이라 이중 출금이 구조적으로 막히고, 반려 시 정확히 복구된다. ③ **`previewWithdrawal` 재작성** — 사이클 기반 계산, 기존 반환 키 전부 호환 유지(라이더 화면 무수정 동작) + 구간 내역 키 추가, 사이클 없으면 **구 모델 자동 폴백**. ④ **`applyForRider`는 신청+점유를 한 트랜잭션**으로, `markRejected`는 점유 해제. ⑤ **라이더 출금 화면에 구간 분리 표기**(최근 7일 이내 21건×80원 / 7일 지난 15건×40원 / 합계 2,280원 → 실지급 106,720원, 실렌더 확인). ⑥ **보증금 경계 정책은 두 방식 모두 구현**하고 `BOUNDARY_POLICY` 상수로 격리 — 갑 확정 시 **한 줄 변경**으로 전환, 스키마 재마이그레이션 불필요. 검증 20/21(오탐 1건은 직전 실패 실행의 고아 데이터). |
| 2026-07-25 | **라이더 관리·전체 데이터 초기화 + 로그인ID 자동생성(§5.2).** ① **전체 데이터 초기화**(테스트 재시작 목적) — 관리자 계정·조직 구조(`organizations`)·각종 설정값(`org_fee_config`/`withdrawal_config`/`deduction_global_config`/`settlement_excel_config`)·`system_codes`는 유지, 라이더·정산·지갑·출금·부채 관련 19개 테이블만 FK 순서대로 삭제 + AUTO_INCREMENT 리셋(`agency_wallets`는 잔액만 0으로). `mysqldump` 미설치 환경이라 PHP로 자체 INSERT 덤프 백업 후 실행, 실행 후 화면 렌더까지 검증. ② **앱 로그인 ID 자동생성** — 신규 `RiderLoginId::generate()`: 휴대전화번호(숫자만)를 기본값, 충돌 시 소문자(a→b→c…, z 소진 시 aa로 확장) 접미사로 유일화(대리점코드 접미사 방식은 너무 길어져 알파벳으로 정정, 대문자는 다시 소문자로 정정), 전화번호 없으면 랜덤 폴백. `riders.php`·`settlement_register_rider.php` 양쪽 로그인ID를 선택 입력으로 전환(비우면 자동생성). 빠른등록의 `suggestLogin()`(라이선스ID를 그대로 로그인ID로 쓰던 로직, 매칭키 중복허용 원칙과 충돌해 위험)은 제거. 순수로직 8/8 + 실API 3건(정식등록·빠른등록·겸업 충돌 시나리오) 검증. |
| 2026-07-30 (2) | **정산명세서 실 파서(`parser.py`) 분석 → 지원금 파싱 + 리스 자동계산(§5.3·§5.7).** 실제 라이더에게 나가는 정산명세서를 만드는 운영 파이썬 스크립트(`도꺠비정산서/parser.py`, 3,243줄)를 처음으로 직접 분석. **발견한 격차**: ① 지원금·추가지원금이 정산금액과 별개로 존재하며 지급액에 가산되는데 우리는 아예 파싱 안 함(금액 누락) ② 리스 차감을 관리자가 수동 일수 입력하는데 실제는 계약기간∩정산기간 자동계산 ③ 대여금/리스 정산수수료 구조 자체가 우리(age-bucket) 사이 실 운영(정산유형+주간물량 기준, 단일 수수료)과 다름(→ 갑 재확인 대기로 보류) ④ 고용·산재보험을 쿠팡 파일값 그대로 안 쓰고 재계산 중 ⑤ 타임버킷 5구간 기반 프로모션 자동지급(150~199건 5만원, 200건 10만원, 초과 시 건당 1천원 가산 상한 30만원, 참여인정구간 20점 미만은 자격 박탈) 등 프로모션 로직 전체 미구현. 이번엔 ①②만 구현(나머지는 [[settlement-statement-gaps]]에 기록). ① **지원금 파싱**: `settlement_support_amounts` 신규 + `XlsxParser::parseSupportSheet()`/`parseAddSupportSheet()`. **실 파일로 헤더판정 버그 발견·수정** — `'지원금'`을 헤더 키워드로 쓰면 시트 제목("지원금 **상세 내역서**")에도 포함돼 있어 제목행을 헤더로 오판(`'주문일자'`만으로 정정). `settlement_upload.php`에 저장 배선 + `SettlementLedger`가 net 계산 시 gross에 가산하도록 수정. 실 파일(오엑스플러스 정산서) + 실제 라이더 성함으로 5,000+2,000원 정확히 가산되는 것까지 end-to-end 검증. ② **리스 자동계산**: `rider_debts.planned_end_on` 추가, `RiderDebt::applyLeaseForPeriod()`가 계약기간∩정산기간 겹치는 일수를 자동 계산해 차감, `SettlementLedger::applyUpload()`가 업로드 정산기간 기준으로 매칭 라이더의 활성 리스마다 1회 호출. **재실행 멱등성**: `rider_debt_entries` UNIQUE(debt_id,applied_date)로 같은 정산 재반영 시 이중 차감 방지. 검증 11/11(1일/5일/경계겹침/계약전후/미설정 스킵) + SettlementLedger 배선 검증. DB 30→31개 테이블. |
| 2026-07-30 (3) | **라이더 앱 전수 분석 + 지원금 표시 누락 수정.** ① **라이더 앱 17개 화면 전체 분석** — 실연동(홈 배너/공지/출금가능액, 정산수수료 내역·상세, 출금 신청·내역, 프로필·계좌·비밀번호, 공지)과 목업(정산 달력·정산 목록·정산 상세="샘플", 홈 "이번 달 정산 합계")을 구분. 정산 목록 목업에 우리 시스템에 없는 "요기요" 플랫폼이 섞여 있음 발견. 재설계 착수 전 **정산수수료 구조 확정(§8-A #3)이 선행조건**임을 확인(안 그러면 만들고 다시 갈아엎을 위험). ② **지원금 표시 누락 수정** — 직전 커밋에서 지원금을 `net_amount` 계산에는 반영했지만 **어디에도 표시가 안 되던 버그** 발견. `SettlementLedger::mapCycleRow()`에 `support_amount` 키 추가(→ `find`/`listAdmin`/`listForRider` 전부에 자동 반영), 어드민 수수료 목록·상세·업로드상세(요약+라이더별 모달) 4곳 + 라이더 앱 수수료 상세 1곳에 "+지원금" 표기 추가. 실 데이터로 5곳 전부 렌더 검증. |
| 2026-07-30 (4) | **은행 목록 중복 제거 + 라이더 앱 대여금/리스 잔액 조회 추가.** ① `rider/views/profile_bank.php`가 은행명 5개를 하드코딩하고 있던 것을 어드민(`riders_detail.php`)과 동일하게 `system_codes`(category='bank') 조회로 교체 — 이제 시스템 코드 관리에서 은행을 추가/변경하면 라이더 앱에도 자동 반영. ② 라이더가 본인 대여금·리스·선지급 잔액을 볼 방법이 없던 것을 확인, `rider/views/profile_debt.php` 신규(읽기전용 — 원금/잔액/일납/최근차감일 + 차감 이력 아코디언), 라우트 `profile/debt` 추가(`inc/rider_routes.php`), 드로어 메뉴·프로필 화면에 진입점 추가. 어드민 화면(`riders_detail.php`)의 부채 카드 스타일을 그대로 재사용(관리 버튼만 제외). 실제 라이더에 테스트 대여금 생성→차감→렌더링까지 확인 후 정리. |
| 2026-07-30 (5) | **라이더 앱 정산조회 상세 + 정산 달력 실연동.** 참고 화면(경쟁사 FRSOFT 스크린샷)의 "실수령금액/정산/공제(원천세·고용보험·산재보험)/일별상세" 구조를 실 데이터로 구현. ① 신규 `rider/views/withdrawal_detail.php`(라우트 `withdrawal/detail`) — 출금 신청 1건이 가져간 `settlement_rider_cycles`들을 `withdrawal_request_cycles`로 조회해 실수령금액(`amount`) / 정산 총액 / 공제 2단계(**정산 반영 시점** 공제=원천세·고용보험·산재보험 등 `settlement_fee_items`을 이번 출금이 가져간 비율만큼 안분 집계 + **출금 신청 시점** 공제=보증금·정산수수료)로 분리 표시, 일별 상세 리스트(날짜·플랫폼·건수·금액) 포함. `withdrawal_history.php` 각 행에 상세 링크 연결. **설계 확인**: 원천세/보험은 우리 시스템에서 정산 반영 시점에 이미 차감되어 있어(출금 시점 차감 아님) 화면상 "공제"를 두 단계로 나눠 보여주기로 사용자 확인 후 결정. ② `rider/views/settlement_calendar.php`의 목업 데이터(하드코딩 배열)를 실데이터로 교체 — `settlement_rider_cycles`(정산 반영 완료된 날짜만, `settlement_fees.php`와 동일 기준)로 달력 마커, `settlement_order_details`(오더별 상세, 2026-07-23 추가됨)로 날짜 클릭 시 그날 운행 내역(가맹점명·금액) 리스트 표시 — 기존 JS(`rider-settlement-calendar.js`)가 이미 이 데이터 구조(`{날짜: {items:[{store,amount}], delivery}}`)로 클릭 상세 시트를 지원하고 있어 PHP 쪽 데이터만 교체(JS 변경 없음). 정산 목록·정산 상세(샘플) 두 목업 화면은 이번 범위에서 제외(§8-A #3 수수료 구조 확정 대기, 사용자 지시로 보류). 실 라이더(rider_id=116, 실 주문상세 334건 보유)에 테스트 사이클·출금·fee_items 삽입 후 렌더링 검증(달력 아이템 14건/합계 45,815원, 출금상세 원천세 1,500원/고용보험 500원/보증금/수수료/일별 2건 전부 정상 출력) 후 정리. |
| 2026-08-03 | **확정 업로드의 미매칭 건 사후 연결/등록 기능 추가.** 기존엔 미매칭 라이더를 업로드 **미리보기 단계**에서만 연결/등록할 수 있었고, 확정 저장 후에는 조회만 가능하고 고칠 방법이 없었음(§8-A 갭으로 발견). 더 심각한 문제: `SettlementLedger::applyUpload()`가 `rider_id IS NULL` 행을 조회 자체에서 제외해 미매칭 건은 "정산 반영" 시 경고도 없이 조용히 스킵됨. ① `admin/api/settlement_register_rider.php`에 `upload_id`/`row_id` 파라미터 추가 시 동작하는 `propagateMatch` 로직 신규 — 연결/등록 즉시 해당 `settlement_daily_riders` 행뿐 아니라 같은 업로드·같은 엑셀이름(`rider_name_raw`)의 다른 원본 테이블(`settlement_order_details`/`settlement_hourly_insurance`/`settlement_support_amounts`/`settlement_weekly_deductions`)까지 `rider_id`를 채우고, `settlement_uploads.ok_rows`/`error_rows`를 재계산. ② `admin/views/settlement_upload_detail.php`에 미매칭 행마다 "연결/등록" 버튼(미리보기 모달과 동일한 UX) 추가. ③ "정산 반영" 버튼을 `status='applied'`(이미 반영됨) 이후에도 "정산 재반영(신규 매칭분)"으로 계속 노출 — `applyUpload()`가 이미 사이클 존재 여부로 스킵하는 멱등 구조라 재클릭해도 안전, 새로 매칭된 행만 추가로 반영됨. 실 라이더(id=116)로 가짜 미매칭 업로드(daily_riders 1행 + order_details 1행) 생성 → 연결 로직 실행 → 형제 테이블 rider_id 채워짐 확인 → `applyUpload()` 재실행 시 실제 정산 사이클 생성까지 end-to-end 검증 후 정리. |
| 2026-08-03 (2) | **이름 표기 오류로 인한 매칭 문제 추가 보강(오타·개명·오매칭).** 직전 항목(미매칭 사후 연결)에 이어, "라이더는 이미 등록돼 있는데 정산서 원본 이름이 달라 매칭이 안/잘못 되는 경우"를 다룸. ① `settlement_register_rider.php` GET 검색을 정확 LIKE 매칭에서 **유사도 랭킹**으로 개선 — 대리점 스코프 내 후보를 모두 가져와 이름 끝 숫자접미사를 뗀 뒤 레벤슈타인 거리로 정렬(바이트 단위라 한글 완전 정확은 아니나 후보를 가까운 순으로 줄 세우는 용도로는 충분, 관리자가 최종 확인). 검증: "고재창836"(오타/절삭) 검색 시 실제 라이더 "고재창8362"가 최상위로 랭킹됨을 확인. ② 이미 다른 라이더로 (잘못) 매칭된 행도 "재연결" 버튼으로 교정 가능하게 확장 — `propagateMatch`에 `force` 플래그 추가, 미적용(정산 반영 전) 상태면 즉시 교정 허용, **이미 `settlement_rider_cycles`가 생성돼 지갑에 적립까지 끝난 건은 자동 되돌리기 없이 명시적으로 차단**(수동조정 도구로 유도 — 돈이 걸린 되돌리기를 자동화하지 않는 것이 안전하다고 판단). UI: 매칭된 행에도 "재연결" 버튼 노출, 클릭 시 현재 매칭 라이더명 경고 + confirm 확인 후 진행. 검증: force 없이 시도 시 차단, 미적용 상태에서 force 재연결 성공, 사이클 생성 후 동일 재연결 시도 시 차단 — 3개 케이스 모두 확인 후 정리. |
| 2026-08-03 (3) | **라이더 앱 4건 수정: 달력 미표시 버그 · 기간 지정 출금 · 홈 실데이터 · 버튼 크기.** ① 🐛 **정산 달력이 아예 안 나오던 버그** — `assets/js/rider-settlement-calendar.js`가 **어디에서도 로드되지 않고 있었다**(CSS만 링크돼 있었음, 최초 커밋부터 계속). 달력 그리드는 전부 JS가 그리므로 화면이 빈 채였다. `settlement_calendar.php`에 script 태그 추가로 해결(브라우저에서 42셀·데이터 3일·합계 검증). ② **전액 출금 → 기간 지정 출금**(§7 #18-b). `WithdrawalCycles::select()`에 `$toDate` 파라미터 추가 — 사이클 소비는 age-bucket 요율·잔액 정합성 때문에 **반드시 가장 오래된 미출금분부터**여야 하므로, 임의 날짜 다중선택이 아니라 **"선택일까지 누적"**으로 자른다. `RiderWallet::previewWithdrawal()`·`Withdrawal::applyForRider()`에 동일 파라미터 전파(구 폴백 모델은 전액 전제라 기간 지정 시 consume=0으로 막음). 신규 `rider/p/withdrawal_preview.php`(날짜 선택 시 실시간 재계산 API) + `assets/js/rider-withdrawal-calendar.js` + 출금 신청 화면 전면 개편(미출금 정산일을 달력에 금액과 함께 표시, 선택 범위 하이라이트). 메모에 `[시작일~종료일]` 기록. ③ 홈 "이번 달 정산 합계 (샘플) ₩3,842,500" 하드코딩 → `settlement_rider_cycles` 당월 실합계 + 반영 일수. ④ 홈 달력/출금 버튼 세로 확대(90px→130px, 아이콘 40px) — **`assets/css/rider-mobile.css`가 빈 파일이었음**(로드는 되고 있었으나 내용 0줄)을 발견, 여기에 스타일 추가. ⚠️ Metronic 루트 font-size가 12px이라 rem이 의도보다 25% 작게 나오는 것을 브라우저 측정으로 확인 → px 고정. **검증 중 발견·수정한 표기 오류**: 출금 화면·정산조회 상세에서 보증금을 "−50,000" 차감 항목으로 표기했으나 실제로는 이번 출금에서 빠지는 돈이 아니라 **지갑에 남겨두는 최소 잔액**(선택 상한 = 잔액−보증금에 이미 반영)이라 금액 계산과 안 맞았음 → 안내 문구로 변경. 브라우저에서 로그인~달력 클릭~출금 신청~상세까지 end-to-end 검증 후 테스트 데이터 정리. |
| 2026-08-03 (4) | **달력 시작월을 "데이터 있는 최근 달"로 변경 + 검증 중 삭제한 실데이터 복구.** ① 정산 달력이 항상 **현재 달**로 열려, 정산 반영분이 과거 달에 있으면(예: 2~3월 데이터를 8월에 조회) 빈 달력만 보여 "데이터가 없다"고 오인하게 되던 문제. `rider-settlement-calendar.js`에 `initialMonth()` 추가 — 데이터가 있는 가장 최근 달로 열고, 데이터가 없을 때만 현재 달. (출금 달력은 이미 동일 방식) ② ⚠️ **운영 주의 — 검증 데이터 정리 시 실데이터 삭제 사고**: 직전 항목 검증 후 정리 과정에서 `DELETE FROM settlement_rider_cycles WHERE rider_id = 116`처럼 **라이더 단위 통삭제**를 써서, 그 라이더가 이전에 실제로 정산 반영받은 사이클 7건(2026-02-25~03-03, 1,059,604원)까지 함께 지워졌다(지갑도 0원). 원본 `settlement_daily_riders`와 업로드(status=applied)는 남아 있어 `applyUpload()` 재실행으로 전량 복구 — **멱등 설계 덕분에 업로드 7건 재실행 시 각 1건씩(= 사라진 116번분)만 생성되고 나머지 라이더는 전부 skip**되어 안전하게 복원됨(126→133건, 지갑=사이클합계 일치, 전체 27명 정합성 확인). **교훈: 테스트 정리는 반드시 생성한 PK를 특정해 삭제할 것.** |
| 2026-08-04 (5) | 🐛 **직전 항목의 회귀 버그 — 라이더 정산 수수료 화면 "조회" 클릭 시 홈으로 이동.** `rider/views/settlement_fees.php`의 새 기간 필터 폼에 query-URL 모드 필수인 `<input type="hidden" name="route">`를 빠뜨렸음. GET 폼은 `action` 속성의 querystring을 무시하고 자기 필드만으로 URL을 새로 구성하므로, `route` 파라미터가 통째로 사라져 라우팅이 빈 라우트(→ 홈)로 폴백됨 — 관리자 쪽 동일 화면(`settlement_fees.php`)엔 이미 있던 히든필드를 라이더 쪽엔 빠뜨린 것. `RIDER_USE_QUERY_URL` 분기로 추가해 수정. 브라우저에서 "조회" 제출 후 같은 화면에 필터 반영된 채 유지되는 것 확인. ⚠️ 같은 결함(hidden route 필드 없음)이 손대지 않은 기존 목업 화면 `rider/views/settlement_list.php`(§8-A 대기 중, 이번 범위 아님)에도 있음 — 나중에 그 화면 작업할 때 같이 고칠 것. |
| 2026-08-04 (4) | **라이더 앱 정산 수수료 내역에도 관리자 화면과 동일한 기간 조회·합계 적용.** 기존엔 from/to 필터조차 없이 최근 50건 고정 조회였음. `SettlementLedger`에 `sumForRider()`·`feeBreakdownForRider()` 신규(관리자용 `sumAdmin()`/`feeBreakdownAdmin()`과 동일 구조, `listForRider()`가 쓰는 `buildListWhere(riderScoped=true)`를 재사용 — riders 테이블 join 불필요, 대리점 스코프도 이미 rider_id로 한정돼 있어 무관). `rider/views/settlement_fees.php`에 기간 입력 + 기간 바로가기 칩(오늘/최근 7일/이번 달/지난 달/최근 90일) + 기간 합계 카드(플랫폼 지급·수수료·지갑 반영) + 항목별 합계 카드(원천세/보험 등, 대여금·리스는 배지로 분리) 추가, 기존 일별 카드 목록은 그대로 하단에 유지. 검증: 실 라이더(id=116) 데이터로 `sumForRider()`가 원시 SQL과 완전 일치(7건/1,077,709/18,105/1,059,604), 항목별 합계(9,483+8,622=18,105)도 사이클 fee 합계와 일치. 브라우저: 전체 기간 조회 시 카드·항목별 표시 정상, "최근 90일"(데이터가 없는 구간) 클릭 시 0원 처리 및 빈 목록 안내 정상. |
| 2026-08-05 (2) | **🆕 프로모션 지급 기능 신규(§5.8).** 엑셀 템플릿 다운로드 → 금액 입력 → 업로드 → 미리보기 → 확정 지급 흐름. ① 신규 테이블 `promotion_batches`(대리점·지급일자·상태·합계) + `promotion_entries`(라이더별 프로모션1/2·지급결과·pg_payment 연결) → **33개 테이블**. 신규 `inc/Promotion.php`(템플릿 목록·preview·createBatch·pay·재집계). ② **템플릿 다운로드**(`admin/api/promotion_template.php`) — 컬럼 `라이더코드 / 라이더이름 / 프로모션1 / 프로모션2`, 해당 대리점 활동 라이더가 **미리 채워진 채로** 생성(관리자는 금액만 입력). 라이더 식별키는 **라이더코드**(사용자 확정 — 중복·오타 위험 없음). ③ **업로드 파싱**(`promotion_upload.php`) — 헤더 자동 탐색, 금액은 `"20,000"`·빈칸·`원` 표기 모두 정규화. 미리보기에서 매칭/미매칭/0원제외 구분 표시. 같은 지급일자에 이미 받은 라이더가 있으면 **경고 후 추가 지급 허용**(사용자 확정). ④ **지급 실행**: 확정 시 **라이더 1명씩** `PgPayment::chargeForRider(프로모션액, 대리점)` 호출 → 카드 청구액 = 프로모션액 + **플랫폼 수수료**(대리점별 요율), 우선순위 카드 실패 시 다음 카드 자동 폴백. **결제 성공한 건만** `RiderWallet::credit()` — 실패 건은 사유를 남기고 지급되지 않으며 상세 화면에서 재시도 가능(`promotion_pay.php`, 이미 paid 인 건은 절대 재결제 안 됨). ⚠️ 프로모션은 지갑 적립 시 `accrued_days`를 올리지 않는다 — 정산 1일치가 아니라 별도 지급이라 출금 수수료의 경과일 산정에 영향을 주면 안 됨. ⑤ 화면: 관리자 「프로모션 지급」(업로드+이력)·「지급 상세」(라이더별 결과·재시도), 사이드바 메뉴, 라이더 앱 「프로모션」 내역 화면(프로모션1/2 합계 + 지급 이력). **⚠️ 발견**: 사용자가 "정산할 때와 마찬가지로 카드 계산"이라 했으나 실제로는 **정산 반영에 카드결제가 연동돼 있지 않았다** — `PgPayment::fundAppliedUpload()`가 존재하지만 **어디서도 호출되지 않는 죽은 코드**(정산은 지갑 적립만 함). 이 사실을 사용자에게 보고했고, 프로모션에는 요청대로 카드결제를 넣되 정산 쪽 연동 여부는 별도 결정 대기. 검증: 템플릿 다운로드→금액 입력→업로드 파싱 왕복 확인(콤마·빈칸 처리 포함), 카드 한도초과 시 다음 카드 폴백(attempts=2), 전 카드 실패 시 미지급 후 재시도 성공(중복 지급 없음), 수수료 4% 정확 계산, 대리점 지갑 충전액 = 지급 합계 일치. 검증 데이터는 PK 특정 삭제 + 지갑 원복(정합성 0건). |
| 2026-08-09 | 🐛🐛 **[2026-08-05 도입, 4일간 방치] 정산 확정 업로드가 항상 실패하던 치명적 회귀 — `use()` 캡처 누락.** 사용자가 "정산 데이터 미리보기" 확정 시 `Unexpected token '<', "<br /> <b>"... is not valid JSON` 오류를 보고, 마이너스 금액(한다빈 -16,241원) 행 때문이 아닐지 추정. 실제 원인은 전혀 달랐다 — **미리보기는 정상, "확정 업로드" 클릭 시에만 100% 재현**되는 버그. `admin/api/settlement_upload.php`의 `db_transaction(static function () use (...) {...})` 클로저 안에서 `$teamName`/`$regionName`을 쓰는데(정산일 팀지역별 업로드 분리, `INSERT INTO settlement_uploads`), **`use()` 목록에 이 두 변수가 빠져 있었다**. `git blame`으로 확인 결과 **2026-08-05 커밋(45ae290b, 팀지역별 업로드 분리 기능 추가)에서 `team_name`/`region_name` 컬럼을 INSERT에 추가하면서 `use()` 갱신을 빠뜨린 것** — 그 이후 이 화면으로 "확정 업로드"를 누른 모든 시도가 예외 없이 실패했을 것으로 추정된다. 증상 메커니즘: PHP가 클로저 안에서 정의되지 않은 변수를 참조하면 `null`로 취급하며 **Warning**을 출력하는데, `display_errors`가 켜져 있으면 이 Warning이 `<br /><b>Warning</b>: Undefined variable $teamName ...`처럼 **HTML로 응답 맨 앞에 섞여 나가 JSON을 깨뜨린다** — 그 뒤 `team_name`이 `NULL`이 되어 `NOT NULL` 제약 위반으로 실제 INSERT도 실패(트랜잭션 롤백, 데이터 유실은 없음). 마이너스 금액은 완전히 무관했음 — 재현 테스트에서 같은 파일에 정상 금액 라이더 1명 + 마이너스 금액(0건, -16,241원) 라이더 1명을 넣고 confirm 했더니 **수정 전엔 두 경우 다 100% 동일하게 깨졌고, 수정 후엔 마이너스 금액 라이더도 포함해 정상 저장**됨을 확인 — 데이터 내용과 무관한 코드 버그였다. 수정: `use()` 목록에 `$teamName, $regionName` 추가(한 줄). 검증: PhpSpreadsheet로 재현용 xlsx(정상 1명 + 마이너스 1명)를 만들어 실제 `/admin/api/settlement_upload.php`에 직접 POST — 수정 전 raw 응답이 정확히 사용자가 겪은 것과 같은 `<br /><b>Warning</b>...` HTML로 시작하는 것을 확인 후, 수정 후 재요청 시 완전한 유효 JSON(`{"ok":true,...}`)으로 바뀌고 마이너스 금액 행(`payout_amount: -16241`)이 DB에 정확히 저장됨을 원시 쿼리로 재확인. 테스트 업로드·행 삭제(실제 데이터는 건드리지 않음 — 화면에서 이미 확인된 라이더 "한다빈7174"는 사용자의 기존 데이터라 보존). |
| 2026-08-08 (17) | **미수금 원장 필터 — 대리점 선택을 총판 선택에 종속시킴.** 직전 항목은 대리점 select2가 처음부터 전체 목록을 보여주고 드롭다운을 열어야만 걸러지는 방식이었는데, 사용자가 "총판을 고르면 대리점이 나와야지"로 피드백 — 총판을 고르기 **전에는 대리점 선택 자체를 비활성화**(플레이스홀더 "총판을 먼저 선택하세요")하고, 총판을 고르면 그제서야 활성화 + 그 소속만 뜨는 방식으로 변경. `applyMatcher()`에서 `agencyEl.disabled = (distVal === '')`를 추가하고 플레이스홀더 텍스트도 상태에 따라 전환. URL에 `distributor` 파라미터가 이미 있는 상태로 들어오면(필터 적용 후 새로고침 등) 시작부터 활성화. 검증: 총판 미선택 시 대리점 select2가 회색 비활성 상태로 렌더, 총판 선택 시 즉시 활성화+해당 소속만 남음, 총판을 다시 "전체"로 지우면 대리점도 비활성화+선택값 초기화, `?distributor=N`으로 직접 접속 시 처음부터 활성화된 상태로 렌더, 콘솔 에러 0건. |
| 2026-08-08 (16) | **미수금 원장 — 총판·대리점 선택형 필터(select2).** 라이더명/코드로만 찾던 검색에 조직 단위 필터 추가. 총판 목록(스코프 내)·대리점 목록(스코프 내, `data-parent`로 소속 총판 표시)을 각각 select2로 렌더, **총판을 고르면 그 아래 대리점만 남도록** 대리점 select2를 커스텀 `matcher`로 필터링. 서버 쿼리는 대리점이 선택되면 그것 우선, 없으면 총판의 `Org::subtreeAgencyIds()`로 하위 전체를 조회. 🐛 **개발 중 발견한 select2 함정** — 총판 select2의 'change'를 `addEventListener`로 받았더니 **아무 반응이 없었다**: select2가 옵션을 선택할 때 발생시키는 'change'는 jQuery가 내부적으로 트리거하는 이벤트라, 이 jQuery 버전에서는 **네이티브 `addEventListener`로 못 받는다**(실측 확인 — `jQuery(el).val(x).trigger('change')`로도 네이티브 리스너 카운터가 0). `jQuery(el).on('change', ...)`으로 바꿔서 해결. 이 프로젝트에서 select2 change를 다룰 땐 항상 jQuery 바인딩을 쓸 것(다른 화면들의 select2 코드도 이 함정을 피해가고 있는지 확인해둘 만함). 대리점 select에서는 `data-control="select2"` 자동 스캐너를 의도적으로 빼고(총판 선택에 따라 옵션을 걸러야 해서 커스텀 matcher가 필요) 별도 초기화 — 그대로 뒀으면 자동 스캐너가 먼저 잡아 이중 초기화될 뻔했다. `canWrite`가 false면 전체 스크립트가 조기 `return`하는 기존 IIFE 밖에 별도 블록으로 둬서 **조회 권한만 있어도 필터가 동작**하게 함. 검증: 실제 UI 클릭(스크립트 트리거가 아닌 select2 결과 항목에 진짜 마우스 이벤트)으로 총판 선택 시 대리점 드롭다운이 정확히 걸러짐, 총판 전환 시 소속 안 맞는 기존 대리점 선택 자동 초기화, 서버 쿼리 4가지 조합(총판만/대리점만/총판+대리점/필터없음) 전부 실 데이터로 정확한 결과 확인, 콘솔 에러 0건, 테스트 조직·라이더·미수금 전량 삭제. |
| 2026-08-08 (15) | **영업대행수수료도 지갑으로 실제 이동(§5.4 · §7 #12).** 직전 항목에서 "리스만 이동하고 영업대행수수료는 리포트 전용"이라 남아 있던 불일치를 해소. ⚠️ **리스와 자금 출처가 정반대라 이동 방향도 다르다** — 리스료는 라이더 정산에서 걷어 **이미 대리점 지갑에 남아 있는 돈**이라 "대리점 → 상위" 이동이었지만, 영업대행수수료는 **대리점이 카드로 결제한 금액 중 net을 넘는 부분**(카드 청구액 = net + service_fee)이라 **그때까지 어느 지갑에도 없던 돈**이다. 따라서 어디서도 빼지 않고 **본사·총판·대리점 세 곳에 각자 몫만큼 credit**한다(기존엔 net만 대리점에 충전하고 수수료는 `pg_payments`에 금액만 기록돼 어느 지갑에도 없었다). ① `PgPayment::feeSplit()` 신규 — `record()`에 인라인돼 있던 3분할 금액 계산을 꺼내 지갑 적립과 **같은 계산을 공유**(스냅샷과 실제 이동이 어긋날 수 없다). 반올림 잔차는 대리점 몫이 흡수해 **세 금액 합 = service_fee** 보장. ② `PgPayment::creditFeeSplit()` — 결제 **성공 트랜잭션 안에서만** 실행(실패 건은 `record()`가 스냅샷을 남기지만 적립은 없음). ③ **총판 없는 본사 직속 대리점**이면 총판 몫이 갈 곳이 없어 증발하므로 **본사로 합산**하고 그 사실을 지갑 원장 메모에 남긴다(리스는 등록 단계에서 거부하는 방식이었는데, PG 요율은 이미 저장된 설정으로 결제가 들어오므로 런타임 흡수 방식을 택함). ④ `Org::chainForAgency()` 신규 — 리스에서 쓰던 `RiderDebt::orgChainForRider()`의 조직 체인 로직을 `Org`로 올려 **두 수수료가 같은 헬퍼를 공유**(RiderDebt는 위임만). ⑤ 리포트는 기존 「플랫폼 수수료 내역」(`platform_fee_history.php`)이 이미 3분할 금액을 보여주고 있어 화면 신규 없이 **"이제 실제로 지갑에 적립된다"는 설명만 추가**. 검증: 요율 본사2%·총판1%·대리점0.5%로 100만원 결제 시 수수료 35,000이 **본사 20,000 / 총판 10,000 / 대리점 5,000**으로 정확히 적립되고 **지갑 총 유입 1,035,000 = 카드 청구액**과 일치(자금 보존), `pg_payments` 스냅샷 3값이 실제 이동액과 완전 일치, 카드 한도초과로 결제 실패 시 세 지갑 모두 변동 없음 확인. 프로모션 지급·결제설정 충전·정산 자금조달 **3개 경로 모두 `chargeForRider()`를 거치므로 자동 반영**됨을 확인. 테스트 데이터 전량 삭제. |
| 2026-08-08 (14) | **리스 수수료 배분 — 실제 지갑 이동 + 리포트 화면(§5.5).** 직전 항목에서 "설정·기록까지만" 남겨뒀던 부분을 완결. ① **지갑 실이동** — 리스료를 라이더 정산에서 차감하면 그만큼 라이더에게 덜 나가므로 **그 돈은 자동으로 대리점 지갑에 남는다**(대리점 인출가능액 = 잔액 − 라이더채무 − 예수금). 따라서 **대리점 몫은 이동이 필요 없고, 상위(본사·총판) 몫만 대리점 지갑에서 빼서 각 조직 지갑으로 옮긴다** — `RiderDebt::moveLeaseFees()`, 차감과 **같은 트랜잭션**. 차감 취소(`reverseEntry`)는 **그때 옮긴 스냅샷 금액 그대로** 되돌린다(설정이 그 사이 바뀌어도 지갑이 어긋나지 않음). ⚠️ **본사·총판이 지갑을 갖는 건 이번이 처음** — `agency_wallets`는 org_id 기반이라 행 생성은 되지만 지금까지 대리점만 썼다. 참고로 **영업대행수수료(PG) 3자 분배는 여전히 `pg_payments`에 스냅샷만 남기는 리포트 전용**이라 실제 이동이 없다(리스만 이동). ② **배분 대상 조직 존재 검증** — 본사 직속 대리점(총판 없음)에 총판 몫을 넣으면 그 돈은 갈 곳이 없어 증발하므로 `orgChainForRider()`로 체인을 확인해 **등록·수정 단계에서 거부**. ③ **리포트 신규** — `deduction/lease-fees`(`lease_fee_report.php`), 기간·대리점 필터 + KPI 4종(걷은 합계/본사/총판/대리점) + **미배분액 표시**(일납 − 배분합 = 리스사 등 외부 지급분) + 차감 건별 상세(라이더·대리점·계약·차대번호·일수·배분액). 집계는 `rider_debt_entries` 스냅샷 기준이라 설정을 바꿔도 과거 실적 불변. 멀티테넌시 스코프 적용. 사이드바 「선공제(대행)」 하위에 메뉴 추가, 「수수료·차감 통합 조회」의 "미수금엔 수수료 없음" 문구가 낡아서 이 화면 링크로 교체. 검증: 본사제공(300/200/500) 10일 차감 시 **본사 +3,000·총판 +2,000·대리점 −5,000**(대리점 몫 500×10은 이동 없음) 정확히 이동하고 원장 3행 기록, 취소 시 세 지갑 모두 원복·리포트도 0, 대리점 단독 제공은 이동 0건, 제공주체 3종 혼합 시드로 **대리점 유출 6,300 = 상위 유입 6,300 자금보존** 확인, 리포트 화면 렌더·미배분 2,800원 계산 정확, 스코프별 조회(본사·총판·해당 대리점=전체 / 다른 대리점=0건) 확인, 콘솔 에러 0건·테스트 데이터 전량 삭제. |
| 2026-08-08 (13) | 🐛 **리스료가 원장에만 기록되고 실제로는 안 걷히던 버그 수정 + 리스 수수료 배분 구조 신규(§5.5).** 메인 프로세스 e2e 검증 중 발견. ① 🐛 **원인은 `SettlementLedger::applyUpload()`의 실행 순서** — 사이클을 만드는 트랜잭션이 끝난 **뒤에야** `applyActiveLeasesForUpload()`가 리스 `deduction_entries`(applied_date = 정산기간 종료일)를 생성했다. 그런데 그 행을 소비하는 쪽은 트랜잭션 **안**의 `buildFeeItems()`(applied_date = settlement_date로 조회)라, 리스 차감분이 항상 한 박자 늦게 만들어져 **어떤 사이클에도 반영되지 않았다**. 결과: `rider_debts` 원장엔 "받았다"고 기록되는데 라이더 정산에선 한 푼도 안 빠짐(= 대리점 손실 + 회계 불일치). 재반영해도 사이클 중복 체크로 스킵돼 영구 미회수, 우연히 **같은 날짜의 다른 팀지역** 정산이 들어올 때만 엉뚱한 사이클에서 뒤늦게 걷히는 상태였다(실측 확인). → `applyActiveLeasesForUpload()` 호출을 트랜잭션 **앞**으로 이동(트랜잭션 밖인 것은 유지 — 개별 리스 이상이 정산 반영 전체를 막지 않게. 이중 차감은 기존 UNIQUE(debt_id, applied_date)가 방어). ② **리스 수수료 배분 신규(갑 확정)** — 리스를 **누가 제공하느냐**에 따라 걷은 리스료를 나눠 갖는다: 본사 제공 → 본사·총판·대리점 3자 / 총판 제공 → 총판·대리점 / 대리점 제공 → 대리점 단독. 배분액은 **요율이 아니라 일 단위 정액(원)**을 계약 건마다 직접 입력하고, **합계가 일납을 넘으면 저장 거부**(걷는 돈보다 많이 나눠 가질 수 없음). 총판·대리점 몫 0으로 두고 본사가 전부 가져가는 것도 가능. 제공 주체보다 상위 조직 몫은 화면에서 숨기고 서버에서도 0으로 강제(화면 우회해도 안전 — curl로 확인). ③ **차대번호(VIN)** — 오토바이 리스라 `rider_debts.vin` 추가, 목록·등록·수정 화면에 노출. ④ **차감 시 배분 스냅샷** — `rider_debt_entries.fee_hq/fee_distributor/fee_agency`에 그 차감 건의 실제 배분액(일 단가 × 차감일수)을 박아둔다. 설정이 나중에 바뀌어도 과거 정산 근거가 보존되고, 부분 차감(금액 직접 입력)으로 덜 걷혔으면 그 비율만큼 줄여 **배분합 ≤ 차감액**을 보장(반올림 초과분은 최대 몫에서 차감). ⑤ 마이그레이션 `migrateLeaseProviderAndVin()`(멱등, rider_debts 5 + rider_debt_entries 3 컬럼). ⚠️ **아직 안 한 것: 배분액의 실제 자금 이동**(대리점/총판/본사 지갑 적립)은 범위 밖 — 현재는 "얼마씩 나눠 갖는지" 설정·기록까지만. 검증: 메인 프로세스 e2e(10일×5만원)에서 리스 10,000원이 정상 차감되고 배분 3,000/2,000/5,000 합계가 차감액과 일치, **자금 보존식(gross 500,000 = 차감 34,900 + 출금수수료 4,800 + 실수령 375,280 + 잔여 85,020) 정확히 성립**, 제공주체별 필드 제한·합계 초과 거부·부분차감 비례 축소 전부 확인, 브라우저에서 주체 전환 시 상위 몫 숨김+0 리셋·초과 시 빨간 경고·수정 모달 기존값 로딩 확인, 콘솔 에러 0건·테스트 데이터 전량 삭제. |
| 2026-08-08 (12) | **출금 실행을 펌뱅킹(쿠콘·하이픈) 즉시 이체로 전환 — 모의 게이트웨이(§5.4 · §7 #10).** 기존엔 "은행 이체파일 다운로드 → 은행에서 수동 이체 → 관리자가 입금완료 클릭"이라 실제 송금과 시스템 상태가 따로 놀았다. 이제 **대리점이 「출금 확정」을 누르면 그 자리에서 건별 이체가 실행**된다. 실 API 미도착이라 `MockFirmBankingGateway`가 결과를 시뮬레이션하고, 화면 상단에 **"펌뱅킹 모의 모드 — 실제 송금은 일어나지 않습니다" 경고 배너**를 띄운다(스펙 도착 시 `FirmBankingGatewayFactory::make()`만 교체, 호출부 수정 불필요). ① 신규 `inc/FirmBankingGateway.php` — 인터페이스 + Mock + 팩토리(`isMock()` 포함). 결과 타입은 기존 `TransferResult` 재사용, 오픈뱅킹(금융결제원)과 **별도 클래스로 분리**(사업자·인증 체계가 다르고 일일정산·대리점 인출은 기존 경로 유지). ② `Withdrawal::executeTransfers()` 신규 — **건 단위 독립 처리**(갑 확정): 한 건씩 이체하고 **실패해도 멈추지 않고 다음 건을 계속**, 성공한 건만 `completed`+지갑 차감, 실패 건은 `failed`+`fail_reason`. ⚠️ **전체를 한 트랜잭션으로 묶지 않고 건별 커밋** — 송금은 되돌릴 수 없어 뒷건 실패로 앞건을 롤백하면 "돈은 나갔는데 시스템은 미완료"라는 최악의 불일치가 생긴다. ③ **기존 파일 다운로드 경로는 백업으로 유지**(갑 선택) — 펌뱅킹 장애 시 수동 처리 가능. ④ **정합성 구멍 3개 함께 수정**: `failed`가 `STATUS_LABELS`에 없어 라벨이 깨지던 것, `hasOpenRiderRequest()`가 `failed`를 진행중으로 안 봐서 **점유된 사이클이 그대로인데 라이더가 재신청 가능**하던 것(재신청해도 고를 사이클이 없어 헛수고), `markRejected()`가 `failed`를 못 받아 **영구 실패 건의 사이클 점유를 풀 방법이 없던** 것. ⑤ `fail_reason`이 DB에만 쌓이고 화면에 없어 관리자가 원인을 모르던 것 → 상태 배지 아래 노출(반려 사유도 함께). ⑥ **실패 건의 사이클 점유 = 유지(2026-08-08 갑 확정).** 이체가 실패해도 그 정산 건들은 해당 요청 몫으로 계속 잡아두고, 관리자가 **재시도**하거나 **반려**해야만 풀린다. 그 사이 라이더는 재신청 불가. 대안(실패 즉시 점유 해제 → 라이더가 바로 재신청)은 원인 파악 전에 **같은 정산 건을 두고 실패한 옛 요청과 새 요청이 공존**할 수 있어 채택하지 않음. 검증: 3건 일괄 이체(정상·실패·정상)에서 **중간 실패가 뒷건을 막지 않음** 확인, 실패 라이더만 지갑 미차감(130,000 유지)·점유 사이클 2건 유지·재신청 차단 확인, 계좌 수정 후 재시도 성공(지갑 정상 차감), **완료 건 재이체 시도 시 이중 지급 안 됨**(skipped) 확인, 브라우저에서 일괄 확정→성공/실패 분리 표시→재시도까지 e2e 확인, 콘솔 에러 0건·테스트 데이터 전량 삭제. |
| 2026-08-08 (11) | **§8-A #2·#3 해소 — 갑 확인 미결 0건 달성, 코드 변경 없음.** ① **#2 영업대행수수료 분배비율**: "계약할 때마다 바뀌니 지금처럼 1,1,1로 두고 등록할 때마다 입력한다, 초기 비율은 중요하지 않다" → 1%×3이 **확정 대기 중인 임시값이 아니라 빈 값 방지용 자리값**임이 확인됨. `PgFeeConfig::DEFAULT_PCT`·`Organization::create()` 시드의 `TODO(갑확인)` 태그 제거하고 주석을 그 성격에 맞게 재작성. ② **#3 정산수수료 계산 구조(2026-07-30 "최대 쟁점")**: "파이선 프로그램은 구 프로그램이고, 우리는 출금 신청 시 건당 수수료만 적용하면 된다" → 운영 파서 `parser.py`의 요율 체계(주정산 50원 / 선정산 200건미만 200원·이상 100원 + 라이더별 수동단가)는 **레거시라 대조 대상이 아님**. 우리 모델(출금 신청 시점 건당 age-bucket 80/40원) 그대로 유지. ③ **연쇄 해소 — §7 #7 "후정산(주정산 대행수수료 출금시점 부과)" 계획 취소**: 출금 시점엔 건당 정산수수료만 부과하므로, 주정산 라이더는 대행수수료를 **반영시점·출금시점 어디서도 내지 않는다**. `SettlementLedger::buildFeeItems()`가 이미 그렇게 동작하고 있어(선정산 라이더만 `agency_fee` 부과) **계산 로직은 손대지 않았고**, "주정산은 출금 시점에 부과하도록 이연"이라 적혀 있던 **오래된 주석만 정정**했다. ⇒ `grep -rn "TODO(갑확인)"` **0곳**, §8-A 미결 0건. |
| 2026-08-08 (10) | **§8-A #1 해소 — 보증금 경계 정책 `POLICY_WHOLE` 확정(§7 #18).** 갑 확답: "한 건이 5만원을 넘는 경우는 없다. 출금은 건으로 나눠서 항상 보증금 5만원 이상이 남도록." → 2026-07-24부터 잠정 `POLICY_PARTIAL`로 두고 대기하던 상수를 `POLICY_WHOLE`로 전환. 예고대로 **상수 한 줄 + 스키마 변경 없음**으로 끝났지만, 그동안 실행되지 않던 WHOLE 분기가 처음 활성화되면서 **잠복 결함 2건이 드러나 함께 수정**: ① 🐛 **정책 차단 시 구 모델로 새어나가 무단 지급** — `RiderWallet::previewWithdrawal()`이 사이클 모델 여부를 `picked !== []`로만 판정해서, WHOLE 정책상 "출금가능액이 다음 사이클보다 작아 아무것도 못 고른" **정상 상황**이 "사이클 데이터 없음(구 데이터)" 폴백으로 빠졌다. 그 결과 사이클을 하나도 마킹하지 않은 채 **잔액−보증금 전액이 지급**됐다(실측: 잔액 75,000 → 25,000원 지급, 사이클 마킹 0건 → 같은 사이클 재출금 가능). `select()`에 `had_candidates`를 추가해 "정책상 차단"과 "데이터 없음"을 구분, 전자는 소진 0원으로 확정. ② **순서 보장** — 막힌 사이클에서 `continue`(건너뛰고 뒤의 더 작은 사이클을 먹음)하던 것을 `break`로 변경. 건너뛰면 화면 문구 "출금은 오래된 정산분부터 순서대로"와 달력의 "그 날짜까지" 의미가 깨지고 정산 이력에 구멍이 뚫린다(실측: 45k·10k 보유 시 최신 10k만 먼저 출금됨). age-bucket 요율상으로도 오래된 분이 40원으로 싸서 순서 유지가 라이더에게 유리. ③ **차단 안내 신규** — 차단은 정상 동작이지만 라이더에겐 이유 없이 버튼만 비활성화돼 보였다. `select()`가 `blocked_shortfall`(가장 작은 막힌 사이클을 가져가려면 얼마가 더 쌓여야 하는지)을 계산해 반환하고, 출금 화면에 "다음 정산 건을 통째로 출금하기에 ₩N이 부족합니다 — 정산이 그만큼 더 쌓이면 신청할 수 있습니다"로 표시. 검증: 7개 경계 시나리오에서 **보증금 불변식(출금 후 잔액 ≥ 50,000) 전부 성립**·부분출금 0건, 실 DB 픽스처로 previewWithdrawal→applyForRider→attach 전 경로 확인(차단 시 신청 자체가 거부되고 요청 0건 생성, 정상 시 두 사이클 모두 `withdrawn_amount = net_amount`로 통째 마킹 + `withdrawal_request_cycles` 2건 연결·합계 70,000 일치), 테스트 데이터 전량 삭제 확인. |
| 2026-08-08 (9) | **리스 차감 공백 경고(§5.5) — 스케줄러 없이 "밀렸는지"만 눈에 띄게.** 직전 항목에서 "매일 도는 배치가 없다"는 걸 확인한 뒤, 사용자가 스케줄러 도입 여부를 물어서 **인프라 부담 대비 이득을 비교해 스케줄러 신설 대신 공백 감지 경고**를 추천 — 이 앱 전체에 크론이 하나도 없어(전부 웹 요청 시점 실행) 새 크론을 걸면 서버 설정+모니터링이라는 새 운영 부담이 생기는 반면, 실제 리스크는 "며칠~몇 주 업로드 공백"뿐이라 감지·표시만으로 충분하다고 판단. ① `RiderDebt::leaseAccrualGap($debt, $today=null)` 신규 — active 리스에 한해 `min(오늘, 계약종료일)`까지 커버돼야 하는데 `due_updated_on`(최근 차감일, 없으면 개시일-1)이 얼마나 뒤처졌는지 계산, `GAP_WARNING_DAYS=7`(주 단위 정산 이월 관례에 맞춤) 이상이면 `overdue=true`. 종료일 미설정은 별도로 `missing_end_date=true`. 관리자·라이더 화면이 **같은 함수를 호출**해 판단 기준이 어긋나지 않게 함(SQL 쪽 KPI 집계도 같은 7일 상수를 참조). ② **관리자**: `debt_list.php`에 지연 건수 배너(빨강) 추가(기존 종료일-미설정 배너와 별개), 목록의 "계약기간(리스)" 열에 `N일 지연`(danger)/`N일 경과`(secondary, 7일 미만) 배지. ③ **놓칠 뻔한 것**: `riders_detail.php`(라이더 상세의 미수금 카드)가 `debt_list.php`와 **완전히 별도로 중복 구현된 등록/수정 모달**이라는 걸 이번에 발견 — 지난 항목에서 `planned_end_on` 입력 필드를 `debt_list.php`에만 추가하고 여긴 빠뜨렸다면 이 화면으로 리스를 등록하는 관리자는 여전히 자동계산이 막힌 리스를 만들게 됐을 것. `riders_detail.php`에도 계약기간 열·종료일 입력 필드·지연 배지를 동일하게 포팅. ④ **라이더 앱**(`profile_debt.php`): 계약기간 표시 + 지연 시 "최근 반영 이후 N일 경과 — 정산 업로드가 이루어지면 그동안의 리스료가 한 번에 반영됩니다"(라이더 잘못이 아니라는 중립적 문구, 배지 아닌 안내문). 검증: 종료일 없는 리스 1건 + 계약기간은 있지만 미차감 220일 경과 리스 1건을 테스트 라이더에 등록 → 관리자 목록(배너 2종 동시 표시, `220일 지연` 배지)·라이더 상세 카드(동일 배지)·라이더 앱 화면(PHP CLI로 세션 시뮬레이션해 직접 렌더 — 브라우저 로그인 세션이 다른 라이더로 고정돼 있어 이 경로로 검증)에서 모두 정확히 일치하는 문구·일수 확인 후 테스트 데이터 삭제, 관리자 4개 라우트 curl 스윕 PHP 에러 0건. |
| 2026-08-08 (8) | **미수금 원장 메뉴명 통일 + 리스 계약 종료일 필드 추가(§5.5).** ① 사이드바·라우트 제목은 "미수금 원장"인데 실제 화면(`debt_list.php`) 상단 `<h1>`은 "대여금 · 리스 원장"으로 달라 클릭한 사람이 다른 화면으로 왔나 헷갈리는 문제 — `<h1>`을 "미수금 원장"으로 통일(사이드바·라우트·페이지 제목·브라우저 탭 제목 전부 일치). ② **더 큰 문제 발견**: `rider_debts.planned_end_on`(2026-07-30 추가, 리스 계약 종료 예정일)이 DB 컬럼·`RiderDebt::applyLeaseForPeriod()`의 자동계산 로직에는 이미 쓰이고 있는데, **정작 등록/수정 화면·API 어디에도 이 값을 넣을 수 있는 곳이 없어서** 리스를 등록해도 자동 차감이 영영 동작하지 않는 상태였다(개시일만 있고 종료일을 넣을 방법이 없으니 `applyLeaseForPeriod()`가 항상 조용히 스킵). `RiderDebt::create()`/`update()`에 `planned_end_on` 처리 추가(종료일이 시작일보다 빠르면 `InvalidArgumentException`), `debt_action.php`의 create/update 필드 화이트리스트에 추가, 등록/수정 모달에 종류=리스일 때만 보이는 날짜 필드 추가, 목록 표에 "계약기간(리스)" 열 추가(미설정이면 "종료일 미설정 · 자동차감 안됨" 배지), 종료일 미설정 리스가 1건이라도 있으면 목록 상단에 경고 배너 표시. ③ **차감 스케줄러 관련 질문에 답하며 문서화**: 이 시스템엔 매일 도는 배치/크론이 없다 — 리스는 `SettlementLedger::applyUpload()`(정산 엑셀 반영 버튼을 누른 시점)가 업로드의 정산기간과 계약기간이 겹치는 일수를 계산해 자동 차감하고, 대여금·선지급금은 애초에 자동계산이 없어 관리자가 「차감」 버튼을 눌러야 한다 — 즉 **엑셀을 업로드·반영하지 않으면 리스도 차감되지 않는다**. 화면 하단 안내문에 이 사실을 명시. 검증: API로 종료일<시작일 등록 시도 시 정확히 거부, 종료일 정상 등록 후 DB 반영 확인, update로 종료일만 변경 시 반영 확인, 테스트 데이터 삭제 후 라이더 미수금 0건 재확인. |
| 2026-08-08 (7) | **대리점 대시보드도 같은 방식으로 차트화(§5.6).** 직전 항목(본사·총판)에 이어 대리점 화면에도 ApexCharts 3종 적용. ① **정산 추이**(area) — `AdminDashboard::dailyTrend()` 신규(OrgDashboard와 같은 규칙: 데이터 없는 날 0 채움 + 62일 초과 시 주 단위 묶음), 카드 헤더에 기간 정산 합계·전기 대비·배달 건수+건수 증감을 함께 배치. ② **플랫폼 비중**(donut) — 기존 `platform_rows`(진행바 형태로 표시하던 것)를 도넛으로 교체, 하단에 합계 유지. ③ **라이더별 정산액 순위**(horizontal bar, 상위 8명) — `topRiders()` 신규. 차트 아래에 상위 3명은 이름·건수·금액을 리스트로 한 번 더 보여주고 이름 클릭 시 라이더 상세로 이동. 레이아웃도 KPI 6개 나열 → **메인 차트 2개 + KPI 4개(활성라이더·출금대기·당월차감·게시공지) + 순위/타임라인 2단**으로 재편(정산합계·배달건수 KPI는 메인 차트 헤더로 흡수). 집계 소스는 처음부터 KPI와 같은 `settlement_daily_riders.payout_amount`로 통일(직전 항목에서 발견한 불일치 재발 방지). 검증: 대리점 계정(강남, 2/25~3/3)에서 KPI·추이차트·도넛 합계가 모두 14,623,819원 / 4,192건으로 일치, 상위 라이더 8명 금액이 CLI 원시 집계와 정확히 일치(정장원2538 1,185,263원/330건 등), 차트 3종 렌더·가로 스크롤 없음·콘솔 에러 0건, 데이터 없는 기간엔 차트 대신 안내문구로 대체됨 확인, 본사 계정은 기존 본사 대시보드 그대로(대리점 차트 요소 0개)이고 대리점용 집계 쿼리도 실행되지 않음(분기가 `AdminDashboard::load()` 앞) 확인. |
| 2026-08-08 (6) | **본사·총판 대시보드 차트화(§5.6).** Metronic 데모 대시보드(`demo1/dashboards/delivery.html`·`logistics.html` 등) 스타일을 참고해 숫자 나열이던 화면에 **ApexCharts 3종**을 넣었다. 우리 프로젝트와 데모가 같은 ApexCharts(v5.5.1, `plugins.bundle.js` 동봉)를 써서 설정을 그대로 이식 가능했고, 데모의 자동 초기화 번들(`widgets.bundle.js`)은 우리가 로드하지 않으므로 데모 위젯 id와 충돌하지 않는다(초기화 코드는 뷰 하단에 직접 작성). ① **정산 추이**(area, 메인) — `OrgDashboard::dailyTrend()` 신규. **데이터 없는 날도 0으로 채워** x축이 실제 날짜 간격을 반영하게 했다(데이터 있는 날만 이으면 2월과 6월이 붙어 보여 오해를 부름). 기간이 62일을 넘으면 **자동으로 주 단위로 묶어** 포인트 과다를 방지. 카드 헤더에 기간 정산 합계·전기 대비 배지·배달 건수를 함께 배치. ② **플랫폼 비중**(donut) — `platformMix()` 신규, 카드 하단에 플랫폼 수수료 내 몫 강조. ③ **대리점별 정산액 순위**(horizontal bar, 상위 8곳) — 기존 `agency_rows` 재사용. 색상은 `KTUtil.getCssVariableValue('--bs-primary')` 등 테마 변수를 읽고 `KTThemeMode.on('kt.thememode.change')`로 라이트/다크 전환 시 재생성. 금액 축·툴팁은 화면 표기와 같은 억/만 축약(PHP `formatWon`과 동일 규칙을 JS로 구현). 레이아웃도 재편 — KPI 6개 나열 → **메인 차트 2개 + KPI 4개 + 순위/주의 2단**. ⚠️ **검증 중 실제 불일치 발견·수정**: 새 차트를 처음에 `settlement_rider_cycles.net_amount`(수수료·공제 후 지갑 적립액)로 집계했는데, 바로 위 KPI "기간 정산 합계"와 순위·표는 `settlement_daily_riders.payout_amount`(플랫폼 지급액)를 쓰고 있어 **같은 화면에서 39만원 차이**가 나 버그처럼 보였다 — 차트를 KPI와 동일 소스로 통일(코드에 경고 주석 명시). 검증: 통일 후 KPI·추이차트·도넛·순위·총판롤업·표 합계가 **모두 14,623,819원 / 4,192건으로 정확히 일치**, 6개월 범위 선택 시 주간 버킷(26포인트) 자동 전환 확인, 본사/총판 각각 차트 3종 렌더 + 총판은 롤업·리스크알림 자동 제외 확인, 대리점 계정은 기존 화면 그대로(차트 요소 0개) 확인, 콘솔 에러 0건·가로 스크롤 없음·전 기간 조합 curl 스윕 PHP 에러 0건. |
| 2026-08-08 (5) | **관리자 매뉴얼 검색 — Enter 시 서버 렌더 결과로 항상 동작하는 폴백 추가.** 사용자가 "검색어를 넣어도 반응이 없다"고 리포트 — 이 세션의 브라우저 자동화 도구로는 실시간(AJAX) 검색이 정상 재현됐지만, 실제 사용 환경(확장 프로그램의 요청 차단, 세션/네트워크 문제 등)에서 `fetch()`가 막히면 기존 코드는 `.catch(function(){})`로 **오류를 완전히 삼켜서 아무 반응도 없는 것처럼 보이는 구조**였다. 근본 원인은 특정하지 못했지만, 원인과 무관하게 항상 동작하도록 이중화했다. ① 검색창을 `<form method="get">`으로 감싸 **Enter 키로 제출하면 JS 없이도** `manual_docs.php`가 `$_GET['q']`를 받아 서버에서 검색 결과를 렌더링(라이더 화면과 동일한 방식) — AJAX가 어떤 이유로 실패하든 이 경로는 항상 동작한다. ② 실시간 검색 실패 시 조용히 넘어가지 않고 **결과창에 "Enter 키를 눌러 검색해 주세요" 안내 + `console.error`**를 남기도록 수정. 검증: Enter 제출 시 서버 렌더 결과 30건 정상 표시(폼 직접 submit()으로 확인 — 이 세션 브라우저 도구의 합성 키 입력이 네이티브 폼 제출을 트리거하지 않는 한계가 있어 우회 검증), 기존 실시간 검색도 리팩터링 후 재확인(정상), 관리자 전 조합(q 단독/도큐먼트+검색어 동시/빈 검색어) curl 스윕 PHP 에러 0건. |
| 2026-08-08 (4) | **프로그램 내 매뉴얼 뷰어 + 검색 신규.** `docs/manual/*.md`(관리자가 직접 관리하는 문서 6개)를 앱을 벗어나지 않고 바로 볼 수 있게 요청. `composer require erusev/parsedown`(단일 파일, 의존성 없는 마크다운 파서) 추가. ① 신규 `inc/ManualDocs.php` — 문서 레지스트리(id→파일·제목·대상 audience: `admin`|`rider`), `renderHtml()`(Parsedown `setSafeMode(true)`로 원본 HTML 삽입 차단 + h2~h4에 등장 순서 기반 `id="sec-N"` 부여 — 한글 제목 슬러그화의 충돌 위험을 피하려고 순번 방식 선택), `sections()`(같은 순번 체계로 헤딩 단위 분할, 검색 대상), `search()`(제목 일치를 본문 일치보다 우선 정렬, 최대 30건, 스니펫 앞뒤 60자). **문서 간 상대링크**(README.md의 "문서 구성" 섹션이 `ADMIN_MANUAL.md` 같은 파일 링크를 씀) **재작성 지원** — `renderHtml(id, $linkBuilder)`에 콜백을 주입해 호출부(관리자/라이더가 URL 체계 다름)가 원하는 라우트로 바꿔치기. ② 관리자 화면 `docs/manual`(전체 6개 문서, 좌측 목차 + 검색창) — `admin_can_access_route()`에 `docs/manual` 전용 예외 추가(역할·조직 무관 로그인한 관리자 전원 허용, 정보 열람용이라 권한표 대상 아님). 실시간 검색은 신규 `admin/api/manual_search.php`(디바운스 250ms, AbortController로 이전 요청 취소) AJAX. 사이드바 최상단(대시보드 바로 아래)에 메뉴 추가 + Metronic 보일러플레이트로 남아있던 사이드바 하단 "Docs & Components"(케네테마 데모 사이트로 나가는 링크, 이 앱과 무관) 버튼을 매뉴얼 링크로 대체. ③ 라이더 화면은 `rider`(`RIDER_MANUAL.md`만, 다른 5개 문서는 라이더에게 노출 안 함) 라우트 — 라이더 앱엔 기존에 AJAX API 계층이 없어 검색은 서버 렌더 GET(`?q=`)으로 통일, 드로어 메뉴에 "이용 안내" 추가. ⚠️ **재발 방지 필요했던 버그** — 사이드바 문서 링크·검색 결과 링크를 처음에 `$listUrl . '?doc=' . $id`로 만들었다가 `admin_url()`이 쿼리-URL 모드에서 이미 `?route=...`를 포함해 `?route=a?doc=b`(물음표 2개)가 되는, 이 세션에서 여러 번 고쳤던 바로 그 패턴을 다시 만들었음 — `str_contains($url,'?') ? '&' : '?'` 안전 이어붙이기로 즉시 수정(PHP·JS 양쪽). 검증: 관리자 6개 문서 전부 렌더(헤딩·앵커 개수 일치 52/52), README.md의 문서간 링크가 올바른 `?doc=` 라우트로 재작성됨 확인, 실시간 검색("정산 반영", "미매칭" 등) 정확한 순위·스니펫·앵커 확인 후 클릭 시 실제 스크롤 위치까지 검증, 라이더 검색도 동일 확인, 관리자·라이더 전 라우트 curl 스윕으로 PHP 경고/치명적 에러 0건, 잘못된 `doc=` 파라미터는 `overview`로 안전 폴백 확인. |
| 2026-08-08 (3) | **라이더 앱 홈 화면 모바일 재설계 + 하단 탭바 신규(§5.2).** 기존 홈은 ① 공지 카드가 맨 위라 정작 중요한 돈 정보가 스크롤 아래로 밀리고 ② 이번달정산·출금가능액·보증금·지갑잔액이 한 카드에 fs-8/fs-9(작은 글씨)로 뭉쳐 있어 모바일에서 읽기 힘들고 ③ 바로가기가 달력·출금 2개뿐이라 수수료내역·프로모션·미수금은 햄버거 드로어를 열어야만 갈 수 있었다. **정보 우선순위를 "지금 얼마 뽑을 수 있나 → 뭐가 막고 있나 → 이번 달 얼마 벌었나 → 바로가기" 순으로 재배치**. ① **히어로 카드** — 출금 가능 금액을 34px 그라데이션 카드로 최상단 배치 + 48px 높이 CTA 버튼(상태에 따라 「출금 신청하기」/「계좌 등록하고 출금하기」/「신청 내역 보기」로 자동 전환), 보증금·지갑잔액은 카드 하단에 2분할로. ② **막힘 상태 알림 신규** — **계좌 미등록**(등록 전엔 출금 자체가 불가한데 홈에서 알 방법이 없었음 → 빨간 배너 + 계좌 등록 화면 링크), **진행 중인 출금 신청**(`Withdrawal::hasOpenRiderRequest()`, 상태 라벨·금액·신청일시). ③ **이번 달 요약 타일 3개**(정산액/배달건수/미수금 또는 프로모션 — 미수금이 있으면 그걸 우선 노출, 없으면 프로모션) ④ **바로가기 4칸**(정산달력·수수료내역·프로모션·출금내역) ⑤ **최근 정산 3건** 신규(날짜·플랫폼·건수·실지급액·수수료) ⑥ 공지·배너는 아래로 이동. 데이터는 기존 도메인 메서드 재사용(`SettlementLedger::sumForRider/listForRider`, `RiderDebt::forRider`, `Promotion::listForRider`, `Withdrawal::listForRider`)하고 전부 `tableReady()`/`tableExists()` 가드 + try/catch로 감싸 **한 지표가 실패해도 홈 전체가 죽지 않게** 처리. ⑦ 🆕 **하단 탭바**(`inc/rider_tabbar.php`, 홈/정산/출금/내정보) — 지금까지 라이더 앱의 유일한 이동 수단이 햄버거 드로어라 모바일에서 매번 두 번 탭해야 했음. `position:fixed` + `env(safe-area-inset-bottom)`(아이폰 홈바 대응), 라우트 접두사로 활성 탭 자동 판정, 본문에 `.rider-has-tabbar`로 하단 여백 확보. 로그인 등 `$riderMinimalShell` 화면에는 노출 안 함. ⚠️ CSS는 기존 주석대로 **px 고정**(Metronic 루트 font-size가 12px이라 rem이 의도보다 25% 작게 나옴). 검증: 375×812 모바일 뷰포트에서 실 데이터(출금가능 951,940원·지갑 1,014,500원·최근정산 3건) 렌더 확인, 가로 스크롤 없음(scrollWidth=clientWidth=375), 최하단까지 스크롤해도 콘텐츠가 탭바에 가리지 않음, 탭 활성표시 3개 라우트에서 정확, **라이더 12개 라우트 전부 HTTP 200 + PHP 경고 0건**, 로그인 화면엔 탭바 미노출 확인. 테스트용으로 라이더 1명 비밀번호를 임시 변경했다가 **원본 해시와 정확히 일치하도록 복구**(지갑 정합성 0건 재확인). |
| 2026-08-08 (2) | **오더별 상세 내역 화면 신규 + 건단위 파싱 실패 시 경고 추가.** 사용자 질문("엑셀 업로드를 하면 건단위로 데이터 업로드 되지 않아?")에 대한 조사 — 실제로 건(주문) 단위 데이터는 이미 `settlement_order_details`에 저장되고 있었지만(라이더 요약 `settlement_daily_riders`와는 별개 테이블), **이걸 필터로 조회할 화면이 없었고**, 쿠팡 업로드에서 "오더별" 탭을 못 찾으면 라이더 요약은 정상 저장되면서 건단위 데이터만 조용히 0건이 되는 구조적 위험(`inc/XlsxParser.php::parseOrderDetailSheet()`가 시트를 못 찾으면 에러 없이 `[]` 반환)도 함께 발견해 같이 고쳤다. ① `admin/api/settlement_upload.php` — 업로드 응답에 `warnings` 배열 추가, 쿠팡/기타 플랫폼에서 라이더는 매칭됐는데 오더상세가 0건이면 경고 문구 포함(배민은 요약·건단위가 같은 시트·같은 루프에서 나와 이 문제가 구조적으로 없음). `admin/views/settlement_upload.php` 업로드 성공 모달에 경고 표시 추가. `admin/views/settlement_upload_detail.php`엔 같은 조건에서 지속적으로 보이는 경고 배너 추가(나중에 상세 페이지만 다시 봐도 알 수 있게). ② 신규 화면 `settlement/order-details`(「오더별 상세 내역」) — 기간·대리점(본사/총판만)·플랫폼·라이더·매장명·주문번호 필터 + 요약 카드(건수·순수익 합계·평균 소요시간) + 화면 500건 표시 상한(`table-paginate.js`로 클라이언트 페이징) + **엑셀 다운로드**(필터 결과 전체, 최대 2만건, `admin/api/order_detail_export.php`). 업로드 상세 화면의 "오더상세 N건" 텍스트를 이 화면으로 가는 링크로 전환. ③ 🐛 **구현 중 발견한 버그**: upload_id로 특정 업로드를 필터링해도 날짜 조건(기본 최근 7일)이 별도로 함께 걸려 있어서, 오래된 업로드를 상세화면에서 클릭하면 날짜가 안 맞아 "조회 결과 없음"으로 보이는 문제 발견 — `upload_id`가 지정되면 그 자체로 스코프가 명확하므로 **날짜 조건을 아예 적용하지 않도록** 수정(화면·엑셀 다운로드 둘 다). 검증: 실제 업로드(884건)를 두 라우트(목록 화면·엑셀 다운로드)로 조회해 DB 원본 건수와 정확히 일치 확인(엑셀 재오픈 후 헤더+데이터 행수 대조), 업로드 상세 화면 링크 클릭 시 정확히 884건 표시(버그 수정 전엔 0건이었음), 대리점 계정으로 자기 업로드 조회=884건·타 대리점 업로드 조회=0건(스코프 차단) 확인. |
| 2026-08-08 | **업로드 상세 화면에 "엑셀 다운로드" 추가 — 저장된 원본 데이터를 그대로 재출력.** 업로드 상세(`settlement/upload-detail`)엔 이미 로우 단위 확인 기능(라이더 목록 표 + 「상세」 모달)이 있었지만, 그걸 엑셀로 다시 받을 방법이 없었다는 사용자 요청. 신규 `admin/api/settlement_upload_export.php`(GET `?id=`) — `Org::canAccessAgency()`로 스코핑 후 해당 업로드의 원본 저장 데이터를 시트별로 그대로 export: **정산상세**(`settlement_daily_riders`, 매칭 라이더명/코드 포함, 항상 포함) + **오더상세/시간제보험/지원금/차감내역**(각 테이블에 데이터가 있을 때만 시트 추가, 없으면 생략). 가공 없이 DB에 저장된 값을 그대로 내보내 업로드 화면에 보이는 것과 100% 일치한다. 26개 컬럼까지 있어 열 이름 계산에 `chr(65+i)` 대신 `Coordinate::stringFromColumnIndex()`(Z 이후에도 안전) 사용, 시트 작성 로직을 `settlement_export_write_sheet()` 헬퍼로 공통화(첫 시트는 기본 시트 재사용, 이후는 `createSheet()`). 업로드 상세 화면 툴바에 "엑셀 다운로드" 버튼 추가(`promotion_template.php`와 동일한 `send_download_response()` 패턴). 검증: 실제 업로드(오더상세 884행 포함)를 curl로 로그인 세션째 다운로드 → PhpSpreadsheet로 재오픈해 4개 시트(정산상세 24행·오더상세 885행·시간제보험 5행·지원금 4행, 전부 헤더+데이터 행수 일치) 확인, 다른 업로드(차감내역 있는 건)로 5번째 시트도 정상 확인, 데이터 없는 테이블(이 업로드는 차감내역 0건)은 시트 자체가 안 생기는 것 확인. 스코핑: 자기 대리점 업로드=200, 타 대리점 업로드=403 확인. |
| 2026-08-05 (9) | 🐛 **직전 항목의 회귀 — 기간 선택 버튼 텍스트가 JS 초기화 전엔 비어 보임.** `inc/dashboard_range_picker.php`가 `data-kt-dashboard-range-display` span을 서버에서 빈 채로 렌더링하고 텍스트 채움을 전적으로 JS(`initDashboardRange()`)에 의존하고 있었음 — 스크립트 로드 지연·캐시·오류 등 어떤 이유로든 JS가 그 시점에 안 돌면 버튼이 텅 비어 보이는(사용자 리포트: "본사에서 대시보드에 기간 안나와") 구조였다. 서버가 이미 `$periodFrom`/`$periodTo`를 알고 있으므로 **span 초기 텍스트를 PHP에서 바로 채우도록 수정**(JS는 이후 상호작용만 담당, 있으면 덧씌우고 없어도 최초 표시는 보장됨). `fetch()`로 받은 원본 HTML에 텍스트가 이미 포함되는 것으로 확인. |
| 2026-08-05 (8) | **대시보드 기간을 고정 "이번 주"→Metronic daterangepicker 자유 선택으로 전환.** 사용자가 Metronic 데모(`website-analytics.html`)의 `data-kt-daterangepicker` 위젯을 참고로 요청. 전역 자동초기화(`scripts.bundle.js`의 `createDateRangePickers`)는 영문 라벨·`최근 30일` 고정 시작값이라 그대로 못 쓰고, **별도 속성 `data-kt-dashboard-range`로 충돌을 피해** `assets/js/admin-datepickers.js`에 전용 초기화(`initDashboardRange()`) 추가 — 서버가 내려준 현재 선택 기간을 그대로 반영해 시작하고, 프리셋(오늘/어제/최근 7일/최근 30일/이번 주/이번 달/지난 달, 한국어)을 고르거나 직접 캘린더로 선택해 "적용"하면 `?from=YYYY-MM-DD&to=YYYY-MM-DD` 쿼리로 대시보드를 다시 불러온다. ① 신규 `dashboard_period_from_get()`(`inc/helpers.php`) — `$_GET[from/to]` 검증(형식·`from>to` 스왑), 없으면 기존 기본값(이번 주 월~오늘) 유지. ② `AdminDashboard::load()`/`OrgDashboard::load()`에 `?string $from, ?string $to` 파라미터 추가, **직전 기간 비교도 고정 7일→선택한 기간과 같은 일수만큼**으로 일반화(예: 30일 범위 선택 시 그 직전 30일과 비교). ③ 신규 공용 파셜 `inc/dashboard_range_picker.php`(대리점·본사·총판 대시보드 3곳 공통 트리거 마크업). ④ 화면 문구 전반에서 "이번 주"를 "기간"/"선택 기간"으로 일반화(예: "이번 주 정산 합계"→"기간 정산 합계", "전주 비교 없음"→"직전 기간 비교 없음") — 리스크 알림·큰 금액 출금(최근 14일 고정)·당월 차감(당월 고정)은 선택 기간과 무관한 별개 지표라 그대로 유지. 검증: 본사 계정에서 "최근 30일" 프리셋 선택 → URL에 `from=2026-07-09&to=2026-08-07` 반영 → 정산 합계 82만원이 원시 SQL 합계(822,258)와 일치, 대리점 계정에서도 동일 프리셋 선택 시 자기 스코프 합계(55만원/154건)가 원시 SQL과 정확히 일치 확인. 한국어 프리셋 8종(오늘~직접 선택) 렌더 확인, 콘솔 에러 없음. |
| 2026-08-05 (7) | **플랫폼 수수료 분배 스냅샷 + 「플랫폼 수수료 내역」 화면 신규(§5.4).** 사용자가 "플랫폼 수수료가 DB에 잘 쌓이고 있나"를 물어서 확인한 결과, `pg_payments.service_fee`(총 수수료액)는 결제마다 정확히 기록되고 있었지만 **본사/총판/대리점 몫 분배는 저장되지 않고** 매번 `org_fee_config`의 *현재* 요율로 재계산되고 있었다 — 대리점 수수료 %가 나중에 바뀌면 과거 결제건의 "그때 몫"도 조용히 달라지는 구조적 결함(직전 항목의 대시보드 "본사/총판 몫" 집계도 이 문제를 그대로 갖고 있었음). ① `pg_payments`에 `hq_amount`/`distributor_amount`/`agency_amount`(금액) + `hq_pct`/`distributor_pct`/`agency_pct`(요율) 6컬럼 추가 → **결제 시점에 스냅샷 저장**(`PgPayment::record()`가 `PgFeeConfig::breakdownForAgency()`를 결제 순간에 한 번 계산해 그대로 박아넣음). 기존 행은 컬럼 없던 시절 값이라 "현재 설정값 근사치"로 마이그레이션 시 1회 백필(마커: `hq_pct=distributor_pct=agency_pct=0`인 success 건만 대상, 멱등). ② `OrgDashboard`의 "플랫폼 수수료 내 몫" 집계를 재계산 방식에서 **저장된 `hq_amount`/`distributor_amount` 합산**으로 교체 — 이제 요율이 바뀌어도 과거 주차 숫자가 흔들리지 않는다. ③ 신규 화면 `settlement/platform-fee`(「플랫폼 수수료 내역」, 정산 업로드 메뉴 하위) — 기간·대리점(본사/총판만)·상태 필터, 요약 카드(결제건수·조달원금·수수료합계·**내 몫**[대리점=대리점몫/총판=총판몫/본사=본사몫, 로그인 레벨에 따라 자동 전환]), 결제별 상세 표(일시·대리점·라이더·조달원금·수수료·본사/총판/대리점 분배·카드·상태·실패사유). 조회는 `Org::agencyScopeClause`로 스코핑(대리점은 자기 것만, 총판은 하위만). 검증: 테스트 카드로 실제 `PgPayment::chargeForRider()` 1건 실행(net 100,000원, 대리점 수수료 4%=4,000원 → 본사1,000/총판1,000/대리점2,000 정확히 분할·저장 확인) → 대리점 계정 로그인 시 자기 결제 1건 + "내 몫(대리점 몫)" 2,000원 정상 표시 → 본사 계정 로그인 시 대리점 컬럼·필터 노출 + "내 몫(본사 몫)" 1,000원 정상 표시 → 대시보드의 본사 몫도 동일하게 1,000원으로 일치(스냅샷 기반이라 두 화면이 서로 다른 계산식이어도 같은 숫자) 확인 후 `pg_payments`/`agency_wallet_ledger`/`agency_cards`를 PK 지정 삭제 + 대리점 지갑 잔액 원복, 전체 라이더 지갑 정합성 0건 재확인. |
| 2026-08-05 (6) | **본사·총판 전용 대시보드 신규(§5.6).** 기존 대시보드는 활성 라이더·정산합계·배달건수 등 **라이더 운영 지표**를 대리점 스코프로 합산해 보여줬는데, 본사·총판이 실제로 관리하는 대상은 라이더가 아니라 **대리점**이라 "전체 합계 하나"만 보이고 어느 대리점이 문제인지 알 수 없었다. ① 신규 `inc/OrgDashboard.php` — 스코프 내 **대리점별 한 줄**(활성 라이더·이번주 배달건수·정산액·지갑잔액·출금대기·미수금잔액·최근 정산일·미반영/미매칭 뱃지)을 정산액 순으로 반환. ⚠️ 지표를 한 쿼리로 조인하면 라이더×정산×출금이 서로 곱해져(fan-out) 합계가 부풀려지므로 **지표별로 GROUP BY 쿼리를 따로 돌려 PHP에서 병합**한다. 여기에 **총판별 롤업**(본사 전용), **손이 필요한 대리점**(미반영 업로드/미매칭 행/출금대기가 지갑잔액 초과/중지 상태를 자동 감지), **플랫폼 수수료 내 몫**(`pg_payments.service_fee`를 `PgFeeConfig::breakdownForAgency()`의 요율 비중으로 안분 — 본사는 hq몫, 총판은 distributor몫)을 추가. ② 신규 화면 `admin/views/dashboard_org.php`, `dashboard.php` 최상단에서 조직 레벨로 분기(대리점 계정은 기존 화면 그대로 — 라이더 운영 관점이 대리점에는 여전히 적합). 총판은 총판 컬럼·총판별 롤업·리스크 알림·조직관리 버튼이 자동으로 빠진다. ③ 재사용 위해 `AdminDashboard`의 `riskAlerts()`/`largeWithdrawals()`/`formatPeriodLabel()`을 public으로 전환. 검증: 본사 로그인 시 대리점 3곳·총판 2곳 집계가 원시 SQL과 일치(라이더 30/3, 강남 미매칭 21행·미반영 1건 등), 이번 주 정산 데이터가 없어 0으로만 뜨는 걸 확인하기 위해 **이번 주 날짜의 임시 정산행 2건을 삽입**해 정렬·합계·전주대비 델타까지 교차검산(강남 143만 + 강서 57만 = 200만, 건수 160, 델타 +143.2% = (200만−822,258)/822,258 원시계산과 정확히 일치) 후 **PK 지정 삭제로 원복**(전체 행 수 185 복귀, 지갑 정합성 0건). 총판 계정은 하위 2곳만·시흥 제외 스코핑 확인, 대리점 계정은 기존 화면 그대로임을 확인. |
| 2026-08-05 (5) | **정산 엑셀 열기 암호 — 대리점별 리스트 화면으로 재설계.** 사용자가 `system/settlement-excel` 화면이 암호 1세트만 보여주는 걸 지적 — 실제로는 `settlement_excel_config.org_id`가 이미 대리점별 저장을 지원하는데(마이그레이션으로 추가돼 있었음) **화면이 로그인한 계정 자신의 스코프 1개만 보여줘서** 본사가 전체를 볼 방법이 없었다. 게다가 직전 항목(3)에서 `system/*` 전체를 super 전용으로 잠그며 **대리점 자체 암호 설정 기능이 의도치 않게 막혀** 있었음(사용자 확인 후 원복 결정). ① `inc/auth.php`의 `admin_can_access_route()`에 `system/settlement-excel` 전용 예외 추가 — 이 라우트만 `system/*` 일괄차단보다 먼저 걸려 `RolePermission`의 `settlement` 영역 조회권한(또는 manager)을 따르도록 분리(다른 시스템관리 화면은 여전히 super 전용 그대로). ② `SettlementExcelConfig::listAgencyRows()` 신규 — `Org::agencyScopeClause()`로 조회범위 내 대리점 목록 + 대리점별 저장값을 함께 반환(총판 로그인 시 자기 하위 대리점만, 본사는 전체). ③ 화면 3분기: **대리점 계정**은 기존과 동일한 자기 암호 단일 폼(자체설정 유지), **본사**는 전역 기본 폼 + 스코프 내 대리점 전체를 테이블로(대리점별 3플랫폼 입력 + 개별 저장 버튼), **총판**은 전역 기본 없이 자기 하위 대리점 리스트만. ④ API(`settlement_excel_config.php`)도 `org_id`(`"global"`|대리점id) 파라미터를 받아 대상별 저장으로 재작성 — `Org::canAccessAgency()`로 대상 대리점이 스코프 밖이면 거부, `"global"` 저장은 본사만. 서버 요구사항 카드를 `inc/settlement_excel_requirements_card.php`로 분리(3곳에서 재사용). 브라우저 검증: 본사 로그인 시 대리점 3곳 리스트+전역 기본 모두 노출, 대리점 리스트 중 한 곳 암호 저장→DB 반영 확인 후 PK로 원복, 대리점 계정(`settlement01`) 로그인 시 자기 암호만 단일 폼으로 노출, 총판 계정(`operation01`, `settlement` 영역 조회만 가능·쓰기 불가로 시딩됨) 로그인 시 하위 대리점 2곳만 읽기전용(저장 버튼 없음)으로 정확히 스코핑됨을 확인. |
| 2026-08-05 (4) | **총괄 관리자 역할(`manager`) 추가.** 사용자 요청 — "관리자 한 명이 대리점·총판 업무 전체를 담당하는 경우"를 위한 역할. `admins.role` ENUM에 `manager` 추가(`super`/`admin`/`operation`/`settlement`/`manager`), `OrgAccount::ASSIGNABLE_ROLES`·`Organization::ACCOUNT_ROLES`(대표계정이 자기 조직에 부여 가능한 역할 목록)에도 추가해 본사뿐 아니라 총판·대리점 대표계정도 자기 조직 서브계정에 이 역할을 줄 수 있음. `admin_can_access_route()`/`admin_can_write()`에서 `manager`는 `role_permissions` 테이블 조회를 건너뛰고 **항상 true**(super와 동일하게 전체 화면 조회·쓰기) — 단 **`system/*`(시스템관리)는 여전히 코드 레벨에서 super 전용으로 고정**돼 있어 manager도 접근 불가(super와의 유일한 차이). 콘텐츠(공지·배너) 쓰기는 기존 "본사만" 조직레벨 제약이 manager에도 그대로 적용됨(대리점 소속 manager는 콘텐츠 조회만 가능). `role_permissions` 표에는 manager 행이 없음(설정 불필요 — 항상 전권이라 「권한 관리」 화면에 노출 안 함). 브라우저 검증: 대리점 소속 manager 계정으로 정산·차감·프로모션·출금·콘텐츠·라이더 전 메뉴 노출+쓰기 가능, `system/admins` 등 시스템관리는 여전히 403 차단 확인 후 테스트 계정 PK 삭제. |
| 2026-08-05 (3) | **역할별 권한 관리 화면 신규(§4.1 RBAC DB화) + 권한 불일치 버그 2건 수정.** 사용자가 대행수수료·미수금·프로모션 권한을 물어본 게 계기 — 답변 과정에서 실제 코드 확인 결과 **미수금 화면(`deduction/debts`)은 `settlement` 역할만 진입 가능한데 저장 API는 `operation` 역할만 허용**(화면 접근권과 쓰기권이 서로 다른 역할을 가리켜 사실상 super 외엔 아무도 저장 못 함), **프로모션(`promotion`)은 라우트 접근 규칙 자체가 없어 super 외 전원 화면 진입 차단**(API는 `settlement` 역할 허용하도록 짜여 있었지만 화면에 못 들어가 무의미) 두 버그를 발견·즉시 수정(`admin/api/debt_action.php`의 쓰기 가드 `riders`→`deduction`, `inc/auth.php` 라우트 규칙에 `promotion`→`['super','settlement']` 추가). ① 이어서 사용자가 "역할별 권한관리 화면이 필요, 시스템관리는 최고관리자만 고정하고 나머지는 화면에서 역할별로 설정 가능하게"를 요청 — 기존 `inc/auth.php`에 하드코딩돼 있던 라우트별/영역별 역할 매핑을 **DB 테이블화**. 신규 `role_permissions`(role, area, can_view, can_write) + `inc/RolePermission.php`(super 제외 3역할×7영역 조회·저장, 기존 하드코딩 규칙과 동일한 기본값으로 시드) → **34개 테이블**. `admin_can_access_route()`/`admin_can_write()`를 이 테이블 조회로 교체 — **`system/*`(시스템관리) 전체는 이 표와 무관하게 코드로 super 전용 고정**(요청대로), `system/orgs`(본사 조직관리)·`system/team`(대표계정 자기관리)·`withdrawal/settings`(대리점 자기설정)는 role이 아니라 조직레벨 기반이라 기존 특례 로직 유지. ② 신규 화면 `system/permissions`(「권한 관리」, super 전용) — 영역×역할 표에 조회/쓰기 체크박스(쓰기 체크 시 조회 자동 체크, 조회 해제 시 쓰기 자동 해제), `admin/api/role_permissions.php`로 저장. ③ **사이드바 메뉴를 `admin_can_access_route()` 기준으로 조건부 렌더링**하도록 전 메뉴 항목(정산업로드·선공제·미수금·프로모션·출금·콘텐츠·라이더 아코디언 7곳 + 시스템관리 전체)을 `<?php if (...) : ?>`로 감싸 — 조회 권한 없는 메뉴는 아예 안 보임(이전엔 직접 URL 접근만 막히고 메뉴 링크는 그대로 보여서 클릭 시 403 페이지로 튀는 UX였음). 브라우저 검증: super 로그인 시 전 메뉴 정상, 권한관리 화면에서 operation 역할에 미수금 조회+쓰기를 켜고 저장 → DB 반영 확인 → operation01 재로그인 시 미수금 메뉴가 나타나고 화면 진입·저장 버튼(readonly 아님) 정상 노출 → 시스템관리·프로모션은 여전히 차단(403) 확인 → 테스트 값 원복. |
| 2026-08-05 | **🔴 대규모 재편 — 수수료 명명 통일 · 팀지역별 업로드 · 대리점별 수수료 설정 · 쿠팡ID 다중 · 초기비번 0000 · 보증금 표시 (갑 지시 7건).** ① **명명 통일**: 건별 40/80원 = **정산수수료**, PG 결제 1~3% = **플랫폼 수수료**(구 영업대행수수료), 고용·산재·시간제 = **보험료**, 대여금/리스/선지급 = **미수금**. 메뉴·라우트 제목·화면 문구 전역 반영(`system/pg-fee`→"수수료 설정", `deduction/debts`→"미수금 원장"). ② **팀지역별 업로드**(하루 여러 건): `settlement_uploads.team_name`/`region_name` 정식 컬럼 승격(기존 `stored_path` JSON에서 11건 백필) + `settlement_rider_cycles.team_region` 신규, **UNIQUE를 (rider,date,platform)→(rider,date,platform,team_region)로 확장**. 중복 업로드 판정도 팀지역 포함으로 변경 — 같은 날 다른 팀지역이면 업로드 허용. 업로드 폼에 팀/지역 입력(파일명에서 자동 채움, 수동 수정 가능). ⚠️ **검증 중 실데이터 사고 발견**: 같은 "팀도깨비 서울_강서남부"가 **유니코드 정규화 형식 차이(NFD 조합형 23건 / NFC 완성형 128건)**로 서로 다른 값으로 저장돼 있었음 → 그대로 두면 새 UNIQUE가 무력화돼 중복 정산 발생. intl 확장이 없어 한글 음절 합성 알고리즘을 직접 구현(`normalize_hangul_nfc()` in helpers.php)하고 저장 시점 정규화 + 기존 데이터 백필(151건 하나로 통합). ③ **정산수수료·플랫폼 수수료를 대리점별로 본사가 설정**: `org_fee_config`를 조직당 "내 몫" 1개 → **대리점 행 하나에 `hq_pct`/`distributor_pct`/`agency_pct` 3개**로 재편(기존 값 이관 백필). 즉 대리점마다 본사·총판 몫까지 다르게 줄 수 있음(예전엔 본사·총판 값이 전 하위에 공유). 화면은 `pg_fee_config.php`를 「수수료 설정」으로 전면 개편 — 대리점 카드마다 정산수수료(경과일 기준·건당 단가·보증금)와 플랫폼 수수료(3분할, 합계 실시간 계산)를 각각 저장. ④ **쿠팡ID 다중 보유**: 팀지역마다 ID가 달라 한 라이더가 여러 개를 가질 수 있음 → 연결 시 **덮어쓰기(UPDATE)를 중단하고 행 추가**로 변경(`rider_action.php`·`settlement_register_rider.php` 양쪽), 완전 중복만 `UNIQUE(rider_id,platform,external_id)`로 차단. 라이더 상세 UI를 목록형(ID별 삭제 버튼 + 추가 폼)으로 교체. 매칭은 어느 ID로도 동일 라이더를 찾는 것 확인. ⑤ **초기비번 0000 + 최초 로그인 강제 변경**: `riders.must_change_password` 신규, `RiderAuth::INITIAL_PASSWORD`/`applyInitialPassword()` 추가. 신규 등록·관리자 초기화 시 항상 0000 + 플래그 ON, 라우터에서 **변경 전까지 비밀번호 변경 화면 외 전 라우트 차단**, 변경 시 플래그 해제 후 홈 이동. 0000→0000 재설정은 거부. 관리자 초기화 모달에서 임의 비번 입력란 제거(0000 고정 안내로 교체). ⑥ 라이더 홈에 **보증금 잔액·지갑 잔액** 표시. 검증: 같은 라이더가 같은 날 2개 팀지역 정산 시 사이클 2건 생성 + 재반영 시 중복 없음, 대리점별 수수료 독립 저장(강남 7% 변경이 강서에 영향 없음), 0000 로그인→강제 차단→변경 후 해제 전 과정 브라우저 확인, 관리자 30개·라이더 전 화면 렌더 정상. **정리 중 발견·복구**: 검증용 사이클 삭제 후 지갑 78,656원 과다 잔존 → 원복(전체 27명 정합성 0건 확인). |
| 2026-08-04 (3) | **정산 수수료 내역 — 기간 조회 강화 + 기간 합계·항목별 합계 추가.** 기존에도 from/to 필터는 있었으나 합계가 전혀 없어 "이 기간에 총 얼마"를 알 수 없었음. ① `SettlementLedger`에 `sumAdmin()`(건수·오더·gross·지원금·payout·수수료·net 합계)·`feeBreakdownAdmin()`(fee_code별 합계, 대여금·리스·선지급은 `is_debt` 플래그로 구분) 신규 — 둘 다 `buildListWhere()`를 재사용해 목록과 **완전히 동일한 필터 조건**으로 집계. ⚠️ **중요**: 목록은 표시 상한(500행)·클라이언트 페이징으로 일부만 보여주지만 **합계는 필터 전체를 대상으로 별도 쿼리**한다(화면에도 그 사실을 명시) — 보이는 행만 더하면 틀린 값이 나옴. ② 화면: 기간 바로가기 버튼(오늘/최근 7일/이번 달/지난 달/최근 90일, 현재 조회 기간과 일치하면 활성 표시), 요약 카드 4종(플랫폼 지급 합계+지원금·수수료 합계·지갑 반영 합계·조회 기간), **「상세 내역 합계(항목별)」 카드 신규**(원천세/고용보험/산재보험/시간제보험/대행수수료 등 종류별 건수·합계·비중, 대여금·리스는 "차감(원금상환)" 배지로 분리하고 수수료 합계에서 제외), 목록 tfoot에 기간 합계 행. 검증: `sumAdmin()` 결과가 원시 SQL 집계와 완전 일치(133건/15,327,674/427,709/14,916,206), **항목별 합계 총합 = 사이클 `total_fee_amount` 합계**(427,709) 정합성 확인, 대여금 fee_item 임시 삽입 시 [차감]으로 정확히 분리되는 것까지 확인 후 PK 지정 삭제로 정리. 브라우저: 3월 기간 조회 시 6,839,802 − 189,372 = 6,650,430 및 항목별 합(74,469+60,186+54,717=189,372) 교차검산 일치. |
| 2026-08-04 (2) | **대시보드 전수 점검 → URL 조립 버그 3건 발견·수정(라이더 로그아웃 404 포함).** 사용자가 "대시보드는 다 정상인지" 요청해 전체 점검: 30개 라우트 전부 `php -l` + 실제 렌더 통과, 관리자 권한별(본사/대리점) 대시보드 렌더 정상. 점검 중 **`admin_url()`/`rider_url()`가 query-URL 모드에서 이미 `?route=...`를 포함하는 문자열을 반환하는데, 그 뒤에 다시 `. '?xxx='`를 이어붙인 3곳**을 발견 — 결과 URL이 `?route=a?xxx=b`(물음표 2개)가 되어 두 번째 `?`가 그냥 리터럴 문자로 취급되고 `route` 파라미터 값 자체가 깨져 404("요청한 경로가 없습니다")로 빠짐. ① `admin/views/dashboard.php` — "최근 정산 업로드 이력" 표의 파일명 링크(`settlement/upload-detail?id=N`) 클릭 시 항상 404. 브라우저로 재현 확인 후 수정. ② `admin/views/fee_report.php`(전전 항목에서 만든 신규 화면) — 라이더명 링크(`riders/detail?id=N`)도 같은 버그로 새로 만들면서 그대로 재도입했음. ③ **`rider/index.php`의 로그아웃 리다이렉트**(`login?out=1`) — 이건 하드코딩 라우트가 아니라 **모든 라이더의 모든 로그아웃 클릭마다 발생하는 문제**라 영향도가 가장 큼(다만 `out` 파라미터 자체는 아무 데서도 안 읽어서 기능 손실은 없었고, 그냥 로그인 화면 대신 404가 뜨는 UX 버그). 세 곳 다 `$url .= str_contains($url, '?') ? '&' : '?';` 안전 이어붙이기 패턴으로 수정(다른 화면들이 이미 쓰던 검증된 패턴). 전체 코드베이스에서 `admin_url()`/`rider_url()` 뒤에 안전장치 없이 `. '?'`/`. '&'`를 바로 붙이는 다른 사례 재검색 결과 없음(0건) 확인. 브라우저로 3곳 전부 수정 후 정상 이동 재확인(업로드 상세 진입, 로그아웃 → 로그인 화면). |
| 2026-08-04 | **「수수료·차감 통합 조회」에도 동일 클라이언트 페이징 적용.** `admin/views/fee_report.php`의 라이더별 집계 테이블에 `assets/js/table-paginate.js`(직전 항목에서 만든 컴포넌트) 재사용 — 테이블에 `id="feeReportTable"`, 빈 상태 행에 `data-tp-skip` 추가하고 초기화 스크립트 한 줄 삽입. 이 테이블은 사이클 단위가 아니라 **라이더 단위 집계**라 행 수가 대리점 라이더 총원 규모로 자연히 제한돼 있어(정산 수수료 내역처럼 날짜×라이더로 계속 불어나는 구조가 아님) 별도 안전 상한(row cap)은 추가하지 않음. `tfoot`(합계 행)은 별개 섹션이라 페이징 대상에서 자동 제외됨. 브라우저 검증: 27명 전원 1페이지 표시(페이지 버튼 자체가 안 뜸 — 총원이 페이지 크기 이하라 정상), 빈 결과 시 "결과 없음" + tfoot 미출력 정상. |
| 2026-08-03 (7) | **정산 수수료 내역 화면에 페이징 추가 → 이후 "datatables.js처럼" 요청으로 클라이언트 페이징으로 교체.** 1차: 서버 사이드 페이징(`SettlementLedger::listAdmin()`에 `offset` 지원, 신규 `countAdmin()`, `settlement_history.php`의 기존 page/perPage 링크 패턴 재사용) — 이 과정에서 `admin_url()`이 query-URL 모드에서 이미 `?route=...`를 포함하는데 `$queryParams`에도 `route`를 중복으로 넣던 기존 패턴(같은 걸 쓰는 `settlement_history.php`에도 동일하게 존재, 값이 같아 무해하지만 지저분함)을 새 코드에서 제거. 2차(사용자가 "datatables.js 같이 깔끔한 페이징"을 요청): 이 프로젝트엔 jQuery는 있지만(`plugins.bundle.js`) DataTables 라이브러리는 없고, **CDN 의존성이 전혀 없는 프로젝트**(구글폰트 1건 제외)라 새 외부 라이브러리를 끌어오는 대신 신규 `assets/js/table-paginate.js`(의존성 없는 순수 JS 재사용 컴포넌트, 다른 목록 화면에도 적용 가능) 작성 — 번호 페이지 버튼(현재 페이지 주변 5개+처음/끝+말줄임표) · 페이지당 건수 선택(20/30/50/100) · "총 N건 중 X~Y" 표시를 **새로고침 없이** 구현. 서버는 필터(기간·검색어)에 맞는 결과를 안전 상한(500건, `listAdmin()` 자체 상한과 동일)까지 한 번에 내려주고 클라이언트가 페이지만 나눔 — 상한 초과 시 안내 문구로 기간을 좁히도록 유도. 페이지 이동/건수 변경/빈 결과 3가지 모두 브라우저 검증(URL 안 바뀜, 60~90번째 정상 표시, 페이지당 100건 전환 시 2페이지로 재계산, 결과 0건일 때 "결과 없음"+안내행 정상). |
| 2026-08-03 (6) | **관리자 「수수료·차감 통합 조회」 화면 신규(`settlement/fee-report`).** 그동안 출금 시점 정산수수료(`withdrawal_requests.withhold_other`)는 DB에 저장만 되고 **관리자가 볼 화면이 없었다**(출금 목록 행 hover 툴팁에만 노출). 신규 `admin/views/fee_report.php` + `admin/api/fee_report_detail.php` — 라이더별로 **발생 시점 2가지**를 한 화면에 집계: ① 출금 시점(정산수수료, 출금 신청일 기준) ② 정산 반영 시점(대행수수료·원천세·보험, 정산일 기준). 기간·라이더 검색 필터, 요약 카드 4종, 합계 행, 라이더별 「내역」 모달(건별 시계열 — 출금/정산반영 배지 구분, 출금 건은 수수료 계산식 메모까지 표기). 멀티테넌시 스코프 적용(타 대리점 계정 접근 차단 검증 완료). **설계 판단 — 대여금·리스는 "수수료"가 아님**: 사용자 요청("대여금 리스 부분도 수수료가 발생하면")에 따라 확인한 결과, `rider_debts`/`rider_debt_entries`에 **수수료 컬럼 자체가 없고** 실제 데이터에도 loan/lease 수수료 0건 — 대여금·리스·선지급은 **원금 상환 차감**이지 수수료가 아니다. 따라서 별도 열·별도 합계로 분리하고 화면에 그 사실을 명시(수수료 구조는 §8-A #3으로 갑 확인 대기 중이라 부과 로직은 손대지 않음). |
| 2026-08-03 (5-2) | ⚠️ **또 실데이터 삭제 사고 — 검증 데이터 정리 시 값 조건 매칭의 함정.** (6) 화면 검증용으로 만든 출금 1건을 `DELETE FROM withdrawal_requests WHERE rider_id=116 AND withhold_other=12720`으로 지웠는데, **사용자가 실제로 완료 처리한 출금(id=4)의 수수료가 우연히 같은 12,720원**이라 2건이 함께 삭제됨(`withdrawal_request_cycles`도 FK CASCADE로 소실). 발견 경로: 정리 후 정합성 검사에서 지갑(50,000) ≠ 사이클합계(1,059,604) 불일치. 복구: `audit_logs`의 EXPORT(이체파일 다운로드)·UPDATE(출금 처리 완료, target_id=4) 기록과 **사이클의 `withdrawn_amount` 점유 실적**(합계 1,009,604 = 실지급 996,884 + 수수료 12,720)으로 원본을 역산해 id=4 그대로 재삽입 + 사이클 연결 7건 복원(건수는 `proratedOrders` 동일 규칙으로 안분). 수수료 검산으로 메모 정정(정산일이 5개월 전이라 전 건 경과 구간 → 318건×40원=12,720원). 최종 검산: 지갑 = 사이클합계 − 출금점유, 전체 27명 불일치 0. **교훈 갱신: PK 특정 삭제 원칙을 "금액·플래그 등 값 조건으로 지우지 말 것"까지 확장** — 검증 데이터는 생성 시 반환받은 id를 변수로 들고 있다가 그 id로만 삭제할 것. [[logic-md-canonical]] |
| 2026-08-03 (5) | **확정업로드 "연결/등록" 모달 버그 2건 수정(사용자 실사용 리포트).** ① `admin/views/settlement_upload_detail.php`의 연결/등록 모달이 `agency_id`를 요청에 담지 않고 있어, **대리점 계정이 아닌 본사/총판 계정**으로 로그인하면(대리점 자동귀속이 없어 `agency_id` 필수) "대리점을 선택하세요" 오류가 무조건 발생하던 버그. 미리보기 단계 모달(`settlement_upload.php`)은 `previewState.agencyId`를 보내고 있었는데, 확정업로드용으로 옮겨 만들면서 빠뜨렸음. 업로드 자체가 이미 특정 대리점 소속이므로 `$upload['agency_id']`를 JS로 내려 요청에 포함하도록 수정. ② 연결/등록 성공 후 행을 "매칭"으로 갱신하는 `markRowMatched()`에서 `Cannot read properties of null (reading 'querySelector')` — `td.innerHTML`을 먼저 덮어써서 `activeBtn`이 DOM에서 분리된 뒤에 `activeBtn.closest('tr')`을 호출해 null이 됨(분리된 노드는 `closest()`가 조상 탐색을 못 함). `tr` 참조를 innerHTML 교체 **전에** 미리 잡아두도록 수정. 실 브라우저(본사 관리자 계정, admin_id=1)로 미매칭 테스트 행 생성 → 기존 라이더 검색·연결까지 재현 후 두 버그 모두 해결 확인(콘솔 에러 없음, 행이 "매칭"+재연결 버튼으로 정상 갱신). ⚠️ 검증 위해 admin_id=1 비밀번호를 `Admin1234!`로 임시 변경했음(운영 반영 전 재변경 필요). |
